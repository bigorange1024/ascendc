---
description: PQC 算子 I/O=算法、计算期中间态不得外传、汇报禁甩 ID、profiling 嵌采只回传关键文件；强制 alwaysApply
alwaysApply: true
---

# PQC 算子 I/O · 中间态驻留 · 汇报 · Profiling（强制）

> 用户 2026-09-13 钉死。违反则**不算**完成算法/合格交付；对拍绿或防挂绿**不豁免**。

## 1. 输入输出必须与算法一致

- 生产路径 I/O **必须与 FIPS 203 算法一致**；代码组织可参考 `examples/stable/...`，**不得**为测试方便改写 I/O 语义。
- **Encrypt（K-PKE.Encrypt）**
  - **允许输入**：`ek`（或等价公开拆件）、`m`、`coins`/`r`；**另允许**计算开始前一次性上传的静态 **LUT**（与种子无关的 NTT/INTT 等辅助表）。
  - **允许输出**：仅密文 **`c`**。
- **Encaps（ML-KEM.Encaps）**
  - **允许输入**：`ek`、`m`；**另允许**同上静态 **LUT**（开场一次）。
  - **允许输出**：仅 **`c`** 与共享密钥 **`K`**。
- **禁止**把计算过程中应生成的中间量当作输入，包括但不限于：`Â`、`y`、`e1`、`e2`、`μ`、`ŷ`、`û`、`u`、`v`、物化 `Âᵀ`、预计算噪声 bin、Encaps 生产路径预填 `r` 等。
- **禁止多段输入 / 中途输入**：不得在某一 kernel/launch 完成后再 H2D 一批业务输入。凡允许的输入（含 LUT）必须在**开始计算前一次就位**。
- **禁止**用 Host mid 喂中间态或伪输入来换 launch 数或对拍绿——属**反工程规范**。

## 2. 计算期中间态不得外传再传回（密码边界）

- 算子从**开始计算到完成计算**：**不允许**将中间数据外传到 Host（或其它不必要域）再传回继续算。
- 仅**计算结束后**允许回传最终结果：Encrypt → `c`；Encaps → `c`/`K`。
- 设备内衔接（GM/UB/L0–L2）不得借 Host「搬出去再搬回来」；不得物化转置到 Host。
- 离线 debug dump **不得**写成生产路径。
- 本条同时是**性能与保密**要求：中间态外泄扩大攻击面；本仓库既有笔记/stable 行为已体现，本 Rule 强制执行。

## 3. 实验汇报：必须说清实验，禁止只甩 ID

- 每次进度/结果汇报，正文须说明：**实验是什么**（如 Encrypt / Encaps）、**门禁是什么**（正确性 / 防卡死 / profiling）、**结果如何**。
- **禁止**只报用例代号或 job id（如 `EN15`、`EP06`、`kem_hang30_…`）当作正文。
- 代号若需要，仅作括号备注：例「**Encrypt**（cann-ntt 2-launch）防卡死 23/30」。

## 4. Profiling：嵌在已有上机轮次，只回传关键文件

- 多轮上机（防卡死/正确性）时，在其中**一轮**启用 profiling 即可；**禁止**全部轮次跑完再单独开 profiling 专场占卡。
- **边测边传**：关键结果就绪即回传，不等整场结束。
- **默认可回传**：`op_summary` / 必要的 `op_statistic` / `task_time` 等 CSV、极短 SUMMARY、用户点名的性能图。
- **默认禁止回传**：整包 `PROF_*` raw、巨型 job 目录、全量 build/payload 日志、core、无必要大 tgz。
- 专场 profiling + 巨包回传 = **浪费算力与时间**，视为违规操作。

## 5. 自检（动手 / 汇报前）

- [ ] 生产 I/O 是否仅算法输入 + 可选开场 LUT，输出是否仅最终结果？
- [ ] 是否存在中途 H2D 业务数据或 Host 喂 `e1`/`e2`/`μ` 等中间量？
- [ ] 计算期是否存在中间态 D2H 再 H2D？
- [ ] 汇报是否写明 Encrypt/Encaps 等实验名而非只甩 ID？
- [ ] 若已多轮上机：profiling 是否嵌采且只回传关键小文件？
