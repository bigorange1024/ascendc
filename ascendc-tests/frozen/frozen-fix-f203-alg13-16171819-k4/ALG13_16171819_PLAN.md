# Alg.13 行 16–19（k=4）— ByteEncode₁₂ on AIV

**目录**：`ascendc-tests/frozen/frozen-fix-f203-alg13-16171819-k4/`  
**上游**：`fix-f203-alg13-161718-k4`（行 16–18 已验收）

## 语义（本探针范围）

| 行 | 内容 | 实现 |
|----|------|------|
| 16–17 | NTT(s), NTT(e) | 既有 Tag5T MIX |
| 18 | t̂ ← Â∘ŝ + ê | `AivHatLine18` |
| 19–20 | `ByteEncode₁₂(t̂)` → `ek_polyvec`；`ByteEncode₁₂(ŝ)` → `sk_polyvec` | `AivByteEncode1319` + `byte_encode12_aiv.hpp`（设备嵌 C） |

**不含**：`ρ` 拼接完整 `ek`、IND-CCA `dk` 扩展、INTT — 仅 polyvec 级 12-bit 编码对拍。

## GM / bin

| bin | 形状 | 说明 |
|-----|------|------|
| `output/t_hat.bin` | [4,256] int32 | 行 18 |
| `output/ek_polyvec.bin` | 1536 uint8 | `ByteEncode₁₂(t̂)` |
| `output/sk_polyvec.bin` | 1536 uint8 | `ByteEncode₁₂(ŝ)`，ŝ=dst 前 4 行 |
| `golden/*.bin` | 同上 | C ref `byte_encode12_ref.c` |

## mixPass

| 值 | 阶段 |
|----|------|
| 0 | S1+S2+S3+18+19 |
| 4 | 仅行 18 |
| 5 | 仅行 19（需 `dst_preset` + `t_hat_preset`） |
