% 从已验证能力到合法派生 — 面向 Agent 预研的形式方法教材草案
% 编译：bash ../../scripts/xelatex-clean.sh 从已验证能力到合法派生-面向Agent预研的形式方法教材草案.tex
% 维护：单文件持续改写（无日期后缀）；工作分支 research/formal-lang-dag
\documentclass[UTF8,a4paper,11pt]{ctexart}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
% 第3章起：教材式编号环境（定义 3.1、定理 3.1、例 3.1 …）
\theoremstyle{definition}
\newtheorem{definition}{定义}[section]
\newtheorem{example}{例}[section]
\theoremstyle{plain}
\newtheorem{theorem}{定理}[section]
\newtheorem{lemma}{引理}[section]
\newtheorem{proposition}{命题}[section]
\newtheorem{corollary}{推论}[section]
\newtheorem{property}{性质}[section]
\theoremstyle{remark}
\newtheorem{remark}{注}[section]
\renewcommand{\proofname}{证明}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{xurl}
\usepackage{longtable}
\usepackage{array}
\usepackage{caption}
\usepackage{float}
\usepackage{listings}
\usepackage{enumitem}
\usepackage[most]{tcolorbox}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,shapes.geometric}
\geometry{margin=2.2cm}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\hypersetup{colorlinks=true,linkcolor=blue,urlcolor=blue}
\lstset{basicstyle=\ttfamily\small,breaklines=true,frame=none,columns=fullflexible}
\setlist[itemize]{leftmargin=1.6em}
\setlist[enumerate]{leftmargin=1.8em}
% 图/表按「节」编号：图 2.1、表 3.2 …
\numberwithin{figure}{section}
\numberwithin{table}{section}
\captionsetup{font=small, labelfont=bf, skip=6pt}
% 仓库锚点：允许在 - _ / 处断行，避免撑进邻列造成叠字
\newcommand{\repo}[1]{{\footnotesize\ttfamily\path{#1}}}
% FIPS 算法框：黑字可读 + 参数/Input/Output + 与标准一致的步骤编号
\newenvironment{fipsalgo}[1]{%
  \begin{tcolorbox}[
    enhanced, breakable,
    colback=gray!4, colframe=gray!55, coltext=black,
    boxrule=0.5pt, arc=2pt,
    left=6pt, right=6pt, top=4pt, bottom=4pt,
    title={#1},
    fonttitle=\bfseries\color{black},
    coltitle=black,
    attach boxed title to top left={yshift=-2mm,xshift=4mm},
    boxed title style={colback=white, colframe=gray!55, boxrule=0.4pt, coltitle=black}
  ]
  \small\color{black}
}{%
  \end{tcolorbox}
}
% 算法框内：参数集 / Input / Output 行（FIPS 原文措辞）
\newcommand{\fipsioparam}[1]{\par\noindent\textbf{参数集}（\texttt{ml\_kem\_1024}）：#1\par\vspace{2pt}}
\newcommand{\fipsioin}[1]{\noindent\textbf{Input：}#1\par}
\newcommand{\fipsioout}[1]{\noindent\textbf{Output：}#1\par\vspace{4pt}}
\newenvironment{fipssteps}{%
  \begin{enumerate}[leftmargin=2.2em, label=\textbf{\arabic*:}, itemsep=2pt, topsep=2pt]
}{%
  \end{enumerate}
}

\title{从已验证能力到合法派生\\
{\large 面向 Agent 的预研开发形式方法\\（教材式草案·持续修订）}}
\author{ascendc 工程 · 专题分支 \texttt{research/formal-lang-dag}}
\date{}

\begin{document}
\maketitle

\begin{abstract}
\textbf{愿景}：把「层层递进、先验证底层再拼高层」的工程直觉，提升为一套可教学、可门禁、可对照实验的形式方法——
使 Agent（以及人类协作者）在复杂预研任务中，\textbf{只沿已验证能力及其合法组合}生长实现，
从机制上压缩跳层发明与幻觉空间；最终服务一般预研开发，而非仅服务某一密码标准的算子清单。

\textbf{写法}：仿教材而非论文——先从特例讲故事，再抽象数学对象，再复盘与前瞻；
证明与完备性后置，允许边做边补。\textbf{地位}：\texttt{docs/research/} 调研草稿，非定稿、非交付验收依据。

\textbf{压力测试床}：ML-KEM-1024 链上的 \texttt{pke.keygen} / \texttt{kem.keygen} / \texttt{kem.encaps}，
并以 \texttt{kem.decaps} 为理论前瞻试金石；\texttt{*-correctness-*} 作为「只求正确、不计优化」的对照组标本。
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{愿景、读者与本文怎么读}

\subsection{愿景（一句话）}
我们想表达的是：预研开发\textbf{不应当}变成「对着算法说明书自由发挥写核」。
我们应当只在\textbf{已经拿到证书}的能力之上，按规则组合出更复杂的任务；
静态 DAG 只是「引理库谁依赖谁」的一张投影图，真正的作业过程还要另说。

\subsection{我们希望最终得到什么}
\begin{enumerate}
  \item 一套\textbf{教材式}叙述：新人 / 新 Agent 能从特例读懂「图从哪来、树怎么长」；
  \item 一套\textbf{可操作门禁}：任务启动必须先交依赖闭包与缺项，再写码；
  \item 一套\textbf{对照实验协议}：方法论指导下的实现 vs correctness 标本 vs 人工指导路径；
  \item （成型后）可解释的接受/拒绝轨迹，以及必要的证明——\textbf{不}作为本稿前置条件。
\end{enumerate}

\subsection{研究路线：从特殊到一般}
\begin{center}
\begin{tabular}{@{}L{2.8cm}L{6.2cm}L{5cm}@{}}
  \toprule
  阶段 & 做什么 & 刻意后置 \\
  \midrule
  特例引出 & 工程故事 $\to$ 图与语法树 & 完备公式 \\
  套公式 & 用数学\textbf{复述}已见现象 & 证明 \\
  改公式 & 缺口反过来改理论 & 为漂亮删工程事实 \\
  套别例 & Encaps 复盘；换域；Decaps 前瞻 & 一次塞太多缺口 \\
  迭代方法论 & 门禁与流程收敛 & 急进 Rule/Skill \\
  成型后 & 证明、可解释性 & 过早锁死抽象 \\
  \bottomrule
\end{tabular}
\end{center}

\subsection{阅读地图（教材顺序）}
\begin{enumerate}
  \item 第~\ref{sec:hook}~章：\textbf{完整例子}（KEM.KeyGen：算法原文、我们有什么、怎样做出来、引出课题）；
  \item 第~\ref{sec:mathbg}~章：\textbf{方法论数学背景}
        （§3.1 导论；§3.2--§3.4 各用一大节写一件工具的数学；再工程体现、回扣、枢纽与总结——\textbf{不含}密码/NTT 代数）；
  \item 第~\ref{sec:math}--\ref{sec:gates}~章：\textbf{装进工程对象与门禁}（节点/边/证书；Agent 工作流）；
  \item 第~\ref{sec:replay}--\ref{sec:forward}~章：\textbf{复盘与前瞻}（用理论看实现）；
  \item 第~\ref{sec:correctness}~章：\textbf{correctness 对照组}；
  \item 第~\ref{sec:open}~章：未决议事项与维护约定。
\end{enumerate}

讨论过程的流水账见同目录 \texttt{形式语言DAG预研讨论纪要.md}（单文件持续刷新）。

% ============================================================
\section{一个完整例子：我们怎样做出 ML-KEM-1024 的 KEM.KeyGen}
\label{sec:hook}

本章用一个\textbf{已经做完}的真实任务当例子。
我们先说清楚「要算什么」，再贴 FIPS~203 里的算法原文，
然后交代我们手里已经有什么、前期做过哪些实验、最后怎样拼出 KEM.KeyGen。
例子讲完之后，我们再\textbf{提问}——这些问题应当能把读者自然带到后文的研究课题里。

\subsection{我们要交付什么}

我们的目标是：在 Ascend AI Core 上用 AscendC 实现
\textbf{FIPS~203 ML-KEM-1024 的密钥生成}（\texttt{ML-KEM.KeyGen}，即 Algorithm~19）。

读者可以把交付物理解成下面这张黑盒表（细节以 stable 目录 customspec 为准）：

\begin{center}
\begin{tabular}{@{}lll@{}}
  \toprule
  方向 & 内容 & 说明 \\
  \midrule
  输入 & \texttt{seed\_d.bin}（4\,B） & Host 写入；设备侧派生 $d$、$z$ \\
  输入 & NTT LUT（静态） & 与 PKE KeyGen 同表 \\
  输出 & \texttt{ek\_kem.bin}（1568\,B） & 与 \texttt{ek\_PKE} 相同 \\
  输出 & \texttt{dk\_kem.bin}（3168\,B） & 与 liboqs 展开布局一致 \\
  \bottomrule
\end{tabular}
\end{center}

我们\textbf{不}把 $d$、$z$、中间 $\hat{A}$、$\hat{s}$ 等当作交付文件落盘。
验收方式是：\texttt{run.sh} 跑 CPU 孪生 + SIM，输出与 golden / liboqs 对拍一致。

\subsection{FIPS~203 原文：三层算法怎样套在一起}

标准把 KEM 密钥生成拆成三层。
下面给出\textbf{与 FIPS~203 原文一致的参数、Input/Output 与步骤编号}。
正文引用 NIST FIPS~203 Algorithm~13 / 16 / 19；本文实例固定为 \texttt{ml\_kem\_1024}（$k{=}4$ 时 $\lvert\mathit{ek}\rvert{=}1568$\,B，$\lvert\mathit{dk}\rvert{=}3168$\,B）。

\begin{fipsalgo}{Algorithm 19 \texttt{ML-KEM.KeyGen()}（FIPS~203 §7.1）}
\fipsioparam{$n{=}256$，$q{=}3329$，$k{=}4$，$\eta_1{=}\eta_2{=}2$，$d_u{=}11$，$d_v{=}5$。}
\fipsioin{（无显式输入；$d$、$z$ 在算法内由 RBG 采样，见步骤 1--2。）}
\fipsioout{封装密钥 $\mathit{ek} \in \mathbb{B}^{384k+32}$；\\
           解封密钥 $\mathit{dk} \in \mathbb{B}^{768k+96}$。}
\begin{fipssteps}
  \item $d \leftarrow\$ \mathbb{B}^{32}$
  \item $z \leftarrow\$ \mathbb{B}^{32}$
  \item \textbf{if} $d = \mathrm{NULL}$ \textbf{or} $z = \mathrm{NULL}$ \textbf{then}
  \item \quad\textbf{return} $\bot$ \hfill{\footnotesize RBG 失败指示}
  \item \textbf{end if}
  \item $(\mathit{ek}, \mathit{dk}) \leftarrow \texttt{ML-KEM.KeyGen\_internal}(d, z)$
  \item \textbf{return} $(\mathit{ek}, \mathit{dk})$
\end{fipssteps}
\end{fipsalgo}

\begin{fipsalgo}{Algorithm 16 \texttt{ML-KEM.KeyGen\_internal}$(d, z)$（FIPS~203 §6.1）}
\fipsioparam{$n{=}256$，$q{=}3329$，$k{=}4$，$\eta_1{=}\eta_2{=}2$，$d_u{=}11$，$d_v{=}5$。}
\fipsioin{随机种子 $d \in \mathbb{B}^{32}$；随机种子 $z \in \mathbb{B}^{32}$。}
\fipsioout{封装密钥 $\mathit{ek} \in \mathbb{B}^{384k+32}$；\\
           解封密钥 $\mathit{dk} \in \mathbb{B}^{768k+96}$。}
\begin{fipssteps}
  \item $(\mathit{ek}_{\mathrm{PKE}}, \mathit{dk}_{\mathrm{PKE}}) \leftarrow \texttt{K-PKE.KeyGen}(d)$
  \item $\mathit{ek} \leftarrow \mathit{ek}_{\mathrm{PKE}}$
  \item $\mathit{dk} \leftarrow \mathit{dk}_{\mathrm{PKE}} \Vert \mathit{ek} \Vert H(\mathit{ek}) \Vert z$
  \item \textbf{return} $(\mathit{ek}, \mathit{dk})$
\end{fipssteps}
\end{fipsalgo}

\noindent 其中 $H = \mathrm{SHA3\text{-}256}$（FIPS~203 记号 $H$）。

\begin{fipsalgo}{Algorithm 13 \texttt{K-PKE.KeyGen}$(d)$（FIPS~203 §5.1）}
\fipsioparam{$n{=}256$，$q{=}3329$，$k{=}4$，$\eta_1{=}\eta_2{=}2$，$d_u{=}11$，$d_v{=}5$。}
\fipsioin{随机种子 $d \in \mathbb{B}^{32}$。}
\fipsioout{加密密钥 $\mathit{ek}_{\mathrm{PKE}} \in \mathbb{B}^{384k+32}$；\\
           解密密钥 $\mathit{dk}_{\mathrm{PKE}} \in \mathbb{B}^{384k}$。}
\begin{fipssteps}
  \item $(\rho, \sigma) \leftarrow G(d \Vert k)$ \hfill{\footnotesize 域分离：字节 33 为模块维 $k$}
  \item $N \leftarrow 0$
  \item \textbf{for} $i \leftarrow 0$ \textbf{to} $k-1$ \textbf{do}
  \item \quad\textbf{for} $j \leftarrow 0$ \textbf{to} $k-1$ \textbf{do}
  \item \quad\quad$\hat{A}[i,j] \leftarrow \texttt{SampleNTT}(\rho \Vert j \Vert i)$
  \item \quad\textbf{end for}
  \item \textbf{end for}
  \item \textbf{for} $i \leftarrow 0$ \textbf{to} $k-1$ \textbf{do}
  \item \quad$s[i] \leftarrow \texttt{SamplePolyCBD}_{\eta_1}(\texttt{PRF}_{\eta_1}(\sigma, N))$;\quad $N \leftarrow N+1$
  \item \textbf{end for}
  \item \textbf{for} $i \leftarrow 0$ \textbf{to} $k-1$ \textbf{do}
  \item \quad$e[i] \leftarrow \texttt{SamplePolyCBD}_{\eta_1}(\texttt{PRF}_{\eta_1}(\sigma, N))$;\quad $N \leftarrow N+1$
  \item \textbf{end for}
  \item $\hat{s} \leftarrow \texttt{NTT}(s)$
  \item $\hat{e} \leftarrow \texttt{NTT}(e)$
  \item $\hat{t} \leftarrow \hat{A} \circ \hat{s} + \hat{e}$ \hfill{\footnotesize NTT 域矩阵–向量乘加}
  \item $\mathit{ek}_{\mathrm{PKE}} \leftarrow \texttt{ByteEncode}_{12}(\hat{t}) \Vert \rho$
  \item $\mathit{dk}_{\mathrm{PKE}} \leftarrow \texttt{ByteEncode}_{12}(\hat{s})$
  \item \textbf{return} $(\mathit{ek}_{\mathrm{PKE}}, \mathit{dk}_{\mathrm{PKE}})$
\end{fipssteps}
\end{fipsalgo}

\textbf{读者只需抓住一条结构链}：
$\text{Alg.19} \Rightarrow \text{Alg.16} \Rightarrow \text{Alg.13}$。
Alg.19 比 Alg.13 \textbf{多出来的}主要是步骤 1--2 的 $z$、Alg.16 步骤 3 的拼接——
\textbf{并不是}把 NTT 或矩阵乘从头再写一遍。

\paragraph{本仓工程差异（验收已锁定，写在算法旁便于对照）}
\begin{itemize}
  \item 我们可复现跑分时，Host 只提供 4\,B \texttt{seed\_d}；$d$ 与 $z$ 在设备 UB 内派生（对应 Alg.19 步骤 1--2 的确定性扩展）。
  \item 我们对拍 liboqs 时，$\mathit{dk}$ 为 3168\,B 展开式
        $\mathit{dk\_pke}\Vert\mathit{ek}\Vert H\Vert z$，与 FIPS 代数最小形态一致，但字节布局与偏移以 customspec 为准。
\end{itemize}

\subsection{动手之前，我们手里已经有什么}

到做 KEM.KeyGen 之前，本仓库并不是从一张白纸开始。
我们已经在 AscendC 上做过大量单点与分段实验，并且多数有 CPU+SIM 对拍记录。
下面这张\textbf{资产清单}（表~\ref{tab:ch2-assets}）把「当时手里有什么」写清楚；文字说明与表格对照阅读。

\paragraph{四类家底（文字）}
\begin{enumerate}
  \item \textbf{平台层（P0）}：我们验证过 DataCopy、TPipe/TQue、MIX 核同步、MMAD tiling 等。
  \item \textbf{密码原语层（P1）}：我们验证过 NTT（poly-batch）、basemul、SHAKE/SHA3、CBD、ByteEncode$_{12}$、SampleNTT 等。
  \item \textbf{PKE 全链（P2）}：我们已有交付级 stable PKE KeyGen（2 launch，KAT 通过）。
  \item \textbf{对照标本}：我们保留 \texttt{*-correctness-*}，只求可执行与字节正确，不计优化。
\end{enumerate}

\begin{table}[H]
\centering\small
\caption{资产清单（锚点以活跃 INDEX 为准）}
\label{tab:ch2-assets}
\begin{tabular}{@{}c L{3.1cm} L{5.4cm} L{2.6cm} L{2.3cm}@{}}
  \toprule
  层 & 能力（简述） & 仓库锚点（示例） & 证书 & 备注 \\
  \midrule
  P0 & DataCopy / 搬运 & \repo{docs/notes/ascendc-DataCopy…} & 文档+探针 & 平台知识库 \\
  P0 & MMAD + tiling & \repo{pass-int8-matmul-cube-…} 等 & CPU+SIM & 闭合条件见 notes \\
  P0 & TPipe / 细同步 & KeyGen prep 技术总结 & CPU+SIM & 翻车高发区 \\
  \midrule
  P1 & NTT 三段式 poly-batch & \repo{pass-fix-…-vec-k4-v2} & CPU+SIM & ML-KEM 活跃基线 \\
  P1 & SampleNTT / Alg.7 & \repo{pass-fix-f203-alg7-sample-ntt-k4} & CPU+SIM & \\
  P1 & CBD $\eta{=}2$ & \repo{pass-fix-f203-alg8-cbd-eta2-k4} & CPU+SIM & \\
  P1 & ByteEncode$_{12}$ & \repo{pass-fix-…-byteencode12-vec-k4} & CPU+SIM & \\
  \midrule
  P2 & Alg.13 行 3--7 $\hat{A}$ & \repo{pass-fix-f203-alg13-lines3-7-a-hat-k4} & CPU+SIM & \\
  P2 & Alg.13 行 8--15 $s,e$ & \repo{pass-fix-f203-alg13-lines8-15-se-k4} & CPU+SIM & \\
  P2 & Alg.13 device 全链 & \repo{pass-fix-f203-alg13-device-keygen-k4} & CPU+SIM & $\approx$542k tick \\
  P2 & \textbf{stable PKE KeyGen} & \repo{stable-fips203-mlkem-pke-keygen-k4} & 交付+KAT & \textbf{KEM 直接依赖} \\
  \midrule
  P3 & KEM KeyGen correctness & \repo{fix-f203-alg19-kem-keygen-correctness-k4} & CPU+SIM+liboqs & 对照标本 \\
  P3 & KEM KeyGen device & \repo{pass-fix-f203-alg19-kem-keygen-device-k4} & CPU+SIM & $\approx$713k tick \\
  P3 & \textbf{stable KEM KeyGen} & \repo{stable-fips203-mlkem-kem-keygen-k4} & 交付+KAT & 本例终点 \\
  \bottomrule
\end{tabular}
\end{table}

当我们决定做 KEM.KeyGen 时，
\textbf{我们并不是让 Agent 从 FIPS 伪代码一行行翻译成 AscendC}，
而是心里已经有上表这张「哪些积木是绿的」的清单。

\subsection{我们怎样探出来：工程路径与数据依赖}

这张清单不是一天长出来的。
本节用两张图把同一件事说清楚：
\textbf{图~\ref{fig:ch2-explore}}是仓库时间线（先长 PKE，再接 KEM 尾缝）；
\textbf{图~\ref{fig:ch2-fips-dataflow}}是 FIPS 调用嵌套（19 调 16、16 调 13）与数据依赖（与 §2.2 步骤编号对照）。

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  node distance=6mm and 8mm,
  box/.style={draw, rounded corners=2pt, align=center, font=\small,
              minimum width=2.6cm, minimum height=0.85cm},
  arr/.style={-{Stealth[length=2mm]}, thick}
]
  % PKE 路径
  \node[box, fill=blue!8] (v2) {P1：NTT+UB\\vec-k4-v2};
  \node[box, fill=blue!8, right=of v2] (ahat) {Alg.13 行 3--7\\$\hat{A}$ 探针};
  \node[box, fill=blue!8, right=of ahat] (se) {Alg.13 行 8--15\\$s,e$ 探针};
  \node[box, fill=blue!12, right=of se] (pke) {P2：stable\\PKE KeyGen};
  \draw[arr] (v2) -- (ahat) -- (se) -- (pke);

  % KEM 路径
  \node[box, fill=orange!10, below=12mm of pke] (corr) {correctness\\（3 launch 标本）};
  \node[box, fill=orange!14, left=of corr] (dev) {device\\2 launch+尾缝};
  \node[box, fill=orange!18, left=of dev] (sk) {stable\\KEM KeyGen};
  \draw[arr] (pke) -- (corr);
  \draw[arr] (pke) -- (dev);
  \draw[arr] (corr) -- node[above, font=\footnotesize]{oracle} (dev);
  \draw[arr] (dev) -- (sk);

  \node[font=\footnotesize, align=left, anchor=north west] at ([yshift=-2mm]v2.south west) {
    \textbf{PKE 线}：先原语 $\to$ 分段 $\to$ 集成晋级\\
    \textbf{KEM 线}：复用 PKE $\to$ Alg.16 尾缝 $\to$ 去 vendor 晋级
  };
\end{tikzpicture}
\caption{工程探路：PKE 线到 KEM 线（仓库时间线）}
\label{fig:ch2-explore}
\end{figure}

\begin{itemize}
  \item \textbf{PKE 线}：我们先把 NTT、内积等做成向量探针；再把 Alg.13 拆成 $\hat{A}$、$s,e$、device 全链，逐段 PASS；最后集成晋级 stable。
  \item \textbf{KEM 线}：我们语义上调用 Alg.13（步骤 1 即 \texttt{K-PKE.KeyGen}）；设备上在 Launch-2 尾部追加 Alg.16 步骤 2--3（$H$、拼 $\mathit{dk}$）；编排上保持 2-launch；验收上 correctness 作 oracle，device 去 vendor 后晋级 stable。
\end{itemize}

\begin{table}[H]
\centering\small
\caption{设备侧 Launch 与 FIPS 行的对应（本仓实现）}
\label{tab:ch2-launch}
\begin{tabular}{@{}L{2.0cm}L{5.5cm}L{2.2cm}L{4.0cm}@{}}
  \toprule
  Launch & 设备段 & FIPS 行 & 要点 \\
  \midrule
  L1 & \texttt{f203\_keygen\_prep} & Alg.13 步骤 1--15 & 双 AIV；$\hat{A}$、$\hat{s}$、$\hat{e}$ \\
  L2 & \texttt{mmad\_custom} + 尾缝 & Alg.13 步骤 16--21 & NTT、内积、ByteEncode \\
  L2 尾 & \texttt{KemKgTailFused} & Alg.16 步骤 2--3 & $z$、$H(\mathit{ek})$、写 $\mathit{dk}$ \\
  \bottomrule
\end{tabular}
\end{table}

若用一句话概括这次成功：
\textbf{我们把 FIPS 的三层算法，映射成「已证 PKE 链 + 一条单独想清楚的 KEM 尾缝」}，
而不是在 KEM 任务里重写 NTT。

图~\ref{fig:ch2-explore}讲「仓库里怎样长出来」；图~\ref{fig:ch2-fips-dataflow}只看标准\textbf{调用嵌套与数据}——
FIPS 里是 \textbf{Alg.19 调用 Alg.16，Alg.16 再调用 Alg.13}（不是平铺的 19$\to$13$\to$16）；
\textbf{重计算在 Alg.13}，$z$ \textbf{不进} Alg.13，只进 Alg.16 的 $\mathit{dk}$ 拼接；
$\mathit{ek}_{\mathrm{KEM}}$ 对 $\mathit{ek}_{\mathrm{PKE}}$ 是直通。

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=1.0cm, y=1.0cm,
  var/.style={draw, ellipse, minimum width=1.75cm, minimum height=0.72cm,
              font=\footnotesize, inner sep=1pt, fill=yellow!12},
  arr/.style={-{Stealth[length=2.2mm]}, thick},
  lbl/.style={font=\scriptsize},
  framelbl/.style={font=\small\bfseries, anchor=north west, inner sep=2pt}
]
  % Alg.19 外框：顶部采样带放 d,z
  \node[draw, rounded corners=3pt, fill=orange!5, minimum width=13.6cm, minimum height=9.0cm,
        anchor=north west] (a19) at (0,8.4) {};
  \node[framelbl] at ([xshift=8pt,yshift=-6pt]a19.north west)
    {Alg.19 ML-KEM.KeyGen（外层：采样并调用 16）};

  \node[var] (d) at (8.8,7.35) {$d$};
  \node[var] (z) at (11.8,7.35) {$z$};
  \node[lbl, text=gray!55!black] at (10.3,7.85) {由 Alg.19 生成};

  % Alg.16：在采样带下方，不包住 d,z
  \node[draw, rounded corners=3pt, fill=orange!12, minimum width=12.0cm, minimum height=6.4cm,
        anchor=north west] (a16) at (0.8,6.55) {};
  \node[framelbl] at ([xshift=8pt,yshift=-6pt]a16.north west)
    {Alg.16 KeyGen\_internal（被 19 调用）};

  \node[draw, rounded corners=3pt, fill=blue!14, minimum width=4.8cm, minimum height=2.4cm,
        anchor=north west, align=center, font=\small\bfseries] (a13) at (1.4,5.15)
    {Alg.13 K-PKE.KeyGen\\[3pt]（被 16 调用）\\[2pt]{\normalfont\footnotesize 重计算在此}};

  \node[var] (ekpke) at (8.8,3.7) {$\mathit{ek}_{\mathrm{PKE}}$};
  \node[var] (dkpke) at (11.8,3.7) {$\mathit{dk}_{\mathrm{PKE}}$};
  \node[var] (H) at (8.8,2.2) {$H(\mathit{ek})$};
  \node[var, fill=green!18] (ek) at (8.8,0.7) {$\mathit{ek}_{\mathrm{KEM}}$};
  \node[var, fill=green!18] (dk) at (11.8,0.7) {$\mathit{dk}_{\mathrm{KEM}}$};

  \draw[arr] (d) -- ++(0,-0.45) -| (a13.north);
  \node[lbl] at (6.8,6.35) {$d$ 入 13};
  \draw[arr] (a13.south east) -- ++(1.2,0) |- (ekpke.west);
  \node[lbl] at (6.8,3.3) {13 出};
  \draw[arr] (a13.south east) -- ++(1.2,0) |- (dkpke.west);
  \draw[arr] (ekpke) -- (H);
  \draw[arr] (ekpke) -- (ek);
  \node[lbl, anchor=east] at (8.35,1.45) {直通 ${\equiv}$};
  \draw[arr] (dkpke) -- (dk);
  \draw[arr] (H) -- ++(1.6,0) |- (dk);
  \draw[arr] (z) -- (dk);
  \node[lbl, align=left, anchor=west] at (12.7,4.0) {$z$ 旁路\\不进 Alg.13};

  \node[lbl, align=left, anchor=north west] at (0.2,-0.45) {
    $d,z$ 在 19 内、16 外（19 生成）；16 内才是调 13 与拼装
  };
\end{tikzpicture}
\caption{FIPS 调用嵌套与数据依赖（Alg.19 调 16，16 调 13；$d/z/\mathit{ek}/\mathit{dk}$）}
\label{fig:ch2-fips-dataflow}
\end{figure}

读图时抓住三点：
\begin{enumerate}
  \item \textbf{调用嵌套}：Alg.19 调 Alg.16；Alg.16 调 Alg.13。框套框表示「谁调用谁」，不是平铺的 19$\to$13$\to$16。
  \item \textbf{数据}：椭圆是字节对象；$d$ 进 Alg.13，$z$ 旁路只进 Alg.16 拼 $\mathit{dk}$；
        $\mathit{ek}_{\mathrm{KEM}}{\equiv}\mathit{ek}_{\mathrm{PKE}}$ 直通，不重做 NTT。
  \item \textbf{汇点而非树}：NTT、CBD、MMAD 等还不画进本图——它们也不专属于 KeyGen；
        本图只是更大图里的一个\textbf{汇点}。
\end{enumerate}

\subsection{从这个例子出发，我们应当问什么？}

例子讲完，真正重要的是\textbf{问题}。
如果我们的研究课题是「怎样让 Agent 预研少幻觉、少撞墙」，
那么面对 KEM.KeyGen 这个故事，我们至少应当认真问下面四组问题。

\paragraph{第一组：标准与工程之间的缝隙}
\begin{enumerate}
  \item FIPS 只告诉我们\textbf{算什么}；它并没有告诉我们，在 Ascend 上应当\textbf{先验哪几块、按什么顺序拼}。
        我们凭什么在动手前就说「可以直接站在 stable PKE 上」——凭的是目录名，还是某种可写的证书？
  \item 探针列表（INDEX）像一张「能力菜单」。
        \textbf{菜单本身}是否等于\textbf{合法拼装说明书}？若不等，缺的那一页应当长什么样？
\end{enumerate}

\paragraph{第二组：「单点都对」之后，组合还对吗}
\begin{enumerate}
  \setcounter{enumi}{2}
  \item 当 NTT 探针 PASS、PKE KeyGen PASS 之后，\textbf{谁}来保证「2-launch 接缝」「Alg.16 尾拼 $\mathit{dk}$」也一定 PASS？
        我们能否把这种「组合不免费」写进理论，而不是每次靠工程师的经验？
  \item 若我们把 correctness 路线（更多 launch、更保守同步）与 device/stable 路线（更少 launch、吃到优化）
        都称为「正确」，\textbf{我们究竟在验收什么}——仅仅是输出字节，还是也要验收「沿已证能力生长」？
\end{enumerate}

\paragraph{第三组：Agent 若只知道算法和菜单，会发生什么}
\begin{enumerate}
  \setcounter{enumi}{4}
  \item 若我们只给 Agent 一段 Alg.19 伪代码 + INDEX 里有哪些探针 PASS，
        \textbf{但不给}确定的拼装路线，搜索空间会有多大？
        我们亲眼见过：过程高度混乱、大量回退、极耗 token，最后只留下「能跑对」的 correctness 标本。
  \item 我们能否把搜索空间收束为「只允许沿已证书边生长」，同时又\textbf{不}把工程优化堵死？
        收束的规则应当写在图里，还是写在某种「合法句子」里？
\end{enumerate}

\paragraph{第四组：怎样把一次成功变成可复用的方法}
\begin{enumerate}
  \setcounter{enumi}{6}
  \item 这次 KEM.KeyGen 的成功，能否被复述成一张\textbf{闭包表}：
        依赖谁、缺了谁、哪条边有证书、哪条边被禁止？
        若下一个人（或 Agent）做 Encaps / Decaps，我们能否\textbf{先交这张表再写码}？
  \item 当上游改了 tiling 或同步壳，下游哪些节点应当自动标「脏」？
        我们是否需要一种比「记得上次 PASS」更可靠的失效传播？
\end{enumerate}

\subsection{本工程现在能回答这四组问题吗？}

四组问题都很好，但「能回答」要分层次：
\textbf{工程实践里我们已有碎片}，\textbf{形式化方法论里我们尚未闭环}。
表~\ref{tab:ch2-four-q}诚实对照（2026-07 专题讨论时的判断）。

{\small
\begin{longtable}{@{}L{2.4cm}L{4.2cm}L{4.8cm}L{3.2cm}@{}}
  \caption{四组问题：本工程已能说明与仍缺项对照}\\
  \label{tab:ch2-four-q}\\
  \toprule
  问题组 & 本工程\textbf{已能}说明的 & \textbf{仍缺}的 & 判断 \\
  \midrule
  \endfirsthead
  \caption[]{四组问题：本工程已能说明与仍缺项对照（续）}\\
  \toprule
  问题组 & 本工程\textbf{已能}说明的 & \textbf{仍缺}的 & 判断 \\
  \midrule
  \endhead
  第一组 &
  INDEX、customspec、baseline-registry 已在约束引用来源 &
  机器可读的「证书对象」；菜单$\neq$拼装说明书的显式规则 &
  \textbf{部分能答} \\
  \midrule
  第二组 &
  我们有 seam 探针/集成探针实践；correctness vs device 在 customspec 有对照 &
  「组合不免费」未写成统一理论；验收口径仍以 I/O 为主 &
  \textbf{部分能答} \\
  \midrule
  第三组 &
  correctness 历史是\textbf{反面教材}（高成本、不可控） &
  正向的「合法派生」门禁尚未工具化；Agent 仍可能绕开 &
  \textbf{能答负例；正例在建} \\
  \midrule
  第四组 &
  单任务可手写 INTEGRATION\_PLAN、闭包直觉 &
  统一闭包表 schema；dirty 传播无自动机制 &
  \textbf{部分能答} \\
  \bottomrule
\end{longtable}
}

\paragraph{逐组一句话}
\begin{itemize}
  \item \textbf{第一组}：我们能指着 stable PKE 说「KEM 应站在这里」，但还缺一张人人（含 Agent）都认的\textbf{证书页}。
  \item \textbf{第二组}：我们知道接缝要单独验（例如 2-launch、尾缝），但还没把这条经验收成\textbf{可检查的规则}。
  \item \textbf{第三组}：我们能用 correctness 证明「只给菜单会爆炸」；方法论要证明的是「给规则后能收敛」——这正是本专题要做的事。
  \item \textbf{第四组}：我们能复盘 KEM.KeyGen 的故事，但 Encaps/Decaps 还不能\textbf{先交闭包表再写码}作为硬门禁。
\end{itemize}

因此：\textbf{本工程能支撑这四组问题的提出与部分实证，但不能宣称方法论已经成立}。
后文的工作，就是把表中「仍缺的」一列逐项补上，并用 Encaps / Decaps 做检验。

\paragraph{这些问题的共同指向}

上面这些问题，表面上各不相同，但它们指向同一条研究主线：

\begin{quote}
我们能否把「已验证能力 + 合法组合 + 动态证书」整理成一套\textbf{可教、可检查、可对照实验}的形式方法——\\
让 Agent 的工作从「在菜单里瞎拼」变成「在自动机上走合法派生」？
\end{quote}

后文先用一章\textbf{方法论导论 + 数学底座}（第~\ref{sec:mathbg}~章）把工程策略说清并落到定义，
再把这些对象\textbf{装进工程语境}并写成门禁（第~\ref{sec:math}--\ref{sec:gates}~章），
最后用 Encaps 复盘、Decaps 前瞻检验这套语言是否管用。
\textbf{本章与后文均不展开} PQC / 数论变换等密码代数——那些细节见 \texttt{docs/notes/}；本专题专注方法论。

% ============================================================
\section{方法论所需的数学背景}
\label{sec:mathbg}

第~\ref{sec:hook}~章讲完了一个已经做完的工程故事，并留下四组问题。
本章按下列顺序写：
\begin{itemize}
  \item §~\ref{sec:mathbg-intro}：\textbf{导论}——先说清这套方法帮什么忙、适合什么样的工作，
        再交代第~2~章策略与三件数学工具落点；
  \item §~\ref{sec:tool-dep}--\ref{sec:tool-auto}：\textbf{三件工具各一大节}写数学
        （依赖图与闭包从集合讲起；形式语言；作业过程自动机）；
  \item 然后统一写\textbf{工程体现}、\textbf{回扣}第~2~章、\textbf{枢纽}对照，最后本章小结。
\end{itemize}
每个关键定义仍配双例：\textbf{先纯数学小例子，再回到第~\ref{sec:hook}~章的 KEM.KeyGen}。
本章\textbf{不}讨论格、MLWE、NTT 等密码代数。

\subsection{导论：场景、策略与方法论}
\label{sec:mathbg-intro}
\label{sec:mathbg-why}

若一上来就讲集合与函数，读者容易以为本章只是在堆数学名词。
本章导论真正要先说清的，也不是把第~\ref{sec:hook}~章里的流程细节再复述一遍，而是回答：

\begin{quote}
\noindent
\textbf{读者在面对什么样的事情时，用得上这套方法论？}\\
或者说：\textbf{这套方法论是为哪一类场景提出的？}
\end{quote}

第~\ref{sec:hook}~章那条路上摸到的流程策略，是发现这类场景的素材；
后文的数学工具，是把「在这类场景里该怎么想、怎么检查」说清楚。
正式定义从 §~\ref{sec:set-rel} 开始。

\subsubsection{先回答：这套方法到底在帮什么忙}

读者通常会先问两件事：
\begin{itemize}
  \item 做研究的人：它能不能让试错少走弯路、少把算力和时间耗在无效方向上？
  \item 做开发的人：它能不能让实现更稳、少在「局部都过了、整体却不成立」上反复摔跤？
\end{itemize}

本专题给出的回答是：\textbf{可以，但只在一类特定工作上。}
它不是通用项目管理，也不是单元测试或版本管理的替代品——
现代软件本来就会拆模块、做测试、管版本；那些是底线，不是本方法要解决的增量问题。

本方法真正盯住的，是下面这种局面：

\begin{quote}
\noindent
手里已经有一批「验过能用」的能力；\\
下一步不是从零发明整条链路，而是\textbf{决定还能在它们上面长什么、先补什么、什么拼法允许、什么拼法禁止}；\\
并且这个决定要能写清楚，能交给下一个人或 Agent 接着做。
\end{quote}

用磁盘速度作类比：标称读写速度是客观事实；
用户真正要的是「这个速度下，我能不能拿它做高负载读写，还是只适合备份」。
本方法服务的是后一种判断——把「已有能力」翻译成「下一步作业该怎么决策」，
而不是再去多测一个速度数字。

它\textbf{不}主要负责：把某个核写快、把某条密码公式证严、把某次对拍做绿。
那些属于能力建设本身。本方法管的是：能力已经有了之后，\textbf{怎么合法、可控地往上用}。

\subsubsection{它适合什么样的工作（以及和普通软件工程差在哪）}

若一项工作大致同时满足下面几条，本方法才有明显收益：

\begin{enumerate}
  \item \textbf{不是从零搭整楼，而是在已有「验过的砖」上继续盖。}\\
        难点在于：哪些砖还能引用、还缺哪几块、缺的要不要先单独做出来。
  \item \textbf{「每一块都测过」不够，拼起来仍可能出问题。}\\
        接缝、多段衔接、接口约定要对齐；不能默认「零件合格则整机合格」。
  \item \textbf{可试的路很多，乱试成本很高。}\\
        尤其当执行者是 Agent 时，很容易在菜单里瞎拼、跳层发明、重复踩已关掉的路。
        这时需要明确：活跃可引用的是什么、明确禁止的是什么。
  \item \textbf{下层一改，上层曾经「测通」的状态要跟着管。}\\
        不能靠人肉记「上次好像过了」；要能标出哪些节点的状态不再可信。
\end{enumerate}

这也是它和「模块 + 单测 + 版本管理」的差别：
后者保证的是代码资产与回归流程；
本方法额外管的是\textbf{预研/实现过程里「能力引用是否合法、作业是否按允许的路径推进」}——
对研究侧，价值是压缩无效试错；对开发侧，价值是减少「局部都绿、整体却不可交付」的返工。

第~\ref{sec:hook}~章的 KeyGen 就是这样一条工作：下层能力已有，要往上拼；
单点通过推不出接缝；只给菜单会乱；成功还要能交接，下层改了上层状态还要更新。
那条路上真正拉开效率差距的手段包括：
隔离活跃项与禁止项、避免无效引用、先探针再晋级、先夯实前置再往上做。
它们已部分落地（INDEX、frozen、customspec、晋级复制、CPU+SIM），
但仍多半是纪律与口头共识；本章要把它们写成可检查的方法。

\subsubsection{我们已经收敛出的架构与流程（直觉图像）}

进入公式之前，先用工程语言画一幅当前较可行的图像（细节后文用定义钉死）：

\begin{itemize}
  \item \textbf{能力库}：一张「谁依赖谁、谁已获证」的图；活跃菜单是线索，不等于合法拼装说明书。
  \item \textbf{一次任务}：不是整库拷贝，而是从目标出发展开依赖闭包，标出已有 / 缺项 / 禁止，再决定最小补洞。
  \item \textbf{接缝单独成事}：两块各自测通，拼在一起仍要单独验证；
  \item \textbf{节点状态要管}：下层接口约定一改，上层不能继续拿「上次测通」当仍然有效；
  \item \textbf{对拍与拼装路径分开看}：golden / liboqs / FIPS 管「算得对不对」；方法论管「允许怎么往上拼」。
\end{itemize}

第~\ref{sec:hook}~章的 KeyGen 故事，大体就是沿着这幅图像走出来的；
Encaps / Decaps 则是检验它能否前瞻，而不只是事后贴标签。

\subsubsection{本章为何要动用数学工具}

工程纪律可以写进 Rule，但 Rule 难教「为什么在这种场景下要这样约束」，也难在任务之间做\textbf{可对照的结构比较}。
数学在这里的角色不是炫技，而是三件事：

\begin{enumerate}
  \item \textbf{命名}：给「依赖、闭包、缺项、合法计划、接受/拒绝、脏证书」固定的名字与关系；
  \item \textbf{分层}：把数据流、能力库、一次作业过程拆开，防止把「字节对了」误当成「派生合法」；
  \item \textbf{可检查}：为后文门禁（先交闭包表再写码）和对照实验准备共同语言。
\end{enumerate}

一句话：用数学工具，把「已有能力之上，下一步作业该怎么决策」说清楚；
不是用方法论去推翻第~\ref{sec:hook}~章的故事，也不是用公式去替代探针与对拍。

\begin{remark}[导论用词纪律]
后文三件\textbf{工具}写数学名：\textbf{依赖图（及闭包）、形式语言、自动机}——与正式对象对齐。
导论里「工程里怎么用」一列只用第~\ref{sec:hook}~章已出现、或一看就懂的说法
（菜单、拼装计划、已获证、缺项、闭包表、脏标记、\texttt{frozen/} 等）。
数学记号（如 $\mathrm{Dep}^*$、$\Gamma$、$\mathrm{Missing}$、$L$、$\mathsf{Dirty}$）\textbf{不在此处充当名称}；
它们要到后文相应\textbf{定义}出现时才引入，并立刻写明「工程说法 $\leftrightarrow$ 数学记号」。
表~\ref{tab:ch3-term-bridge} 是预告对照，便于往后查，\textbf{不是}要求现在就用符号阅读。
\end{remark}

\begin{table}[H]
\centering\small
\caption{工程说法 $\to$ 后文数学记号（预告；定义处才启用符号）}
\label{tab:ch3-term-bridge}
\begin{tabular}{@{}L{3.2cm} L{3.6cm} L{6.4cm}@{}}
  \toprule
  工程说法（导论用语） & 后文记号（定义后） & 一句话 \\
  \midrule
  依赖图 & 有向无环图 $G$ & 谁依赖谁、先证谁 \\
  依赖闭包 & $\mathrm{Dep}^{*}(\cdot)$ & 完成目标理论上必须齐的全部先决 \\
  已获证能力集 & $\Gamma$ & 当前允许被引用的能力集合 \\
  缺项 & $\mathrm{Missing}_{\Gamma}(\cdot)$ & 依赖闭包里尚未获证的那部分 \\
  闭包表 & （工作对象） & 目标 / 已获证 / 依赖闭包 / 缺项的核对表 \\
  拼装计划 / 合法计划 & 字 $w$；语言 $L$ & 一次任务怎么拼；全体合法拼法 \\
  禁止拼法 & $\mathrm{Forb}$ & 不得采用的子串或关闭路线 \\
  作业过程自动机 & 以配置为状态的迁移系统 & 第三件工具的专名 \\
  作业状态（已获证、还欠什么） & 配置 $(\Gamma,F,S)$ & 自动机的状态 \\
  展开 / 探针取证 / 写入证书 & $\mathsf{Expand}/\mathsf{Probe}/\mathsf{Certify}$ & 自动机上的转移 \\
  脏标记（证书作废） & $\mathsf{Dirty}$ & 上游变更后撤回下游证书 \\
  \bottomrule
\end{tabular}
\end{table}

\subsubsection{方法论涉及哪些数学工具，落在工程的哪一面}

场景清楚之后，我们把方法论收成三件数学工具——它们分别服务「第二层判断」的不同侧面。一句话总览：
\[
\begin{array}{c}
\text{依赖图（及闭包）管「能站在谁身上 / 还缺谁」；}\quad
\text{形式语言管「怎样拼才算合法」；}\\[0.35em]
\text{自动机管「这次作业走到哪、证书何时作废」。}
\end{array}
\]
三者在仓库里分别落成\textbf{依赖图与闭包表 / 拼装说明书 / 门禁怎么往前走}；缺一，第~\ref{sec:hook}~章里的失控会重新出现。

表~\ref{tab:ch3-three-tools} 是导论核心对照；后文数学正文按这三件工具分段写细。

\begin{table}[H]
\centering\small
\caption{依赖图 / 形式语言 / 自动机：导论级对照（为何学 $\leftrightarrow$ 工程用法）}
\label{tab:ch3-three-tools}
\begin{tabular}{@{}L{2.2cm} L{4.4cm} L{6.6cm}@{}}
  \toprule
  工具（数学） & 为什么要学（控制什么） & 在工程里怎么用（落点） \\
  \midrule
  \textbf{依赖图}
  （及依赖闭包） &
  预研不是白手翻译 FIPS，而是「先有绿积木再往上长」。
  没有图就说不清：谁依赖谁、先证谁、有没有环、动手前还缺谁。 &
  顶点 $\approx$ INDEX/stable/接缝能力；
  边 $\approx$「KEM 依赖 stable PKE」等；
  拓扑序 $\approx$ 自底向上探路顺序；
  依赖闭包与缺项 $\approx$ 资产清单与\textbf{闭包表}要核的两栏；
  禁止再走的边 $\approx$ \texttt{frozen/}。 \\
  \midrule
  \textbf{形式语言} &
  INDEX 绿项是\textbf{菜单}，不是\textbf{拼装说明书}。
  单点 PASS 推不出接缝合法（组合不免费）；
  要把「合法拼法」写成可检查的计划。 &
  可用步骤 $\approx$ 已证能力与接缝动作；
  一次\textbf{拼装计划} $\approx$ 某次任务的展开顺序；
  \textbf{合法计划集合} $\approx$ 门禁承认的全部拼法；
  成员检查 $\approx$ 门禁查「这份计划是否合法」；
  \textbf{禁止拼法} $\approx$ 已关闭/禁止路线（含 frozen 判决来源）。 \\
  \midrule
  \textbf{自动机} &
  依赖图是静态库，形式语言给出合法计划集合；
  真正干活还要有\textbf{怎么一步步往前走}的规则：
  补洞$\to$探针$\to$写入证书，有时还要作废。
  没有自动机，就无法说「走到接受」与「上次 PASS 为何作废」。 &
  状态 $\approx$ 当前已获证哪些、前沿还欠什么；
  转移 $\approx$ 展开 / 探针取证 / 写入证书；
  接受 $\approx$ 目标已获证且前沿清空；
  拒绝 / 冻结 $\approx$ 踩了禁止拼法；
  \textbf{脏标记} $\approx$ 上游约定变更后下游不得假绿。 \\
  \bottomrule
\end{tabular}
\end{table}

\begin{example}[导论：用 KeyGen 故事对照三件工具]
续第~\ref{sec:hook}~章。
\begin{itemize}
  \item \textbf{依赖图}：不是「INDEX 里 NTT 绿了就直接写 KEM」，而是先画清
        stable PKE $\to$ 尾缝 $\to$ KEM，再核对\textbf{缺项}。
  \item \textbf{形式语言}：NTT PASS、PKE PASS \textbf{推不出}
        「2-launch + Alg.16 尾拼 $\mathit{dk}$」自动合法——接缝须单独写进合法拼装计划。
  \item \textbf{自动机}：从「已获证清单里已有 stable PKE」走到「\texttt{kem.keygen} 获证」是一条合法轨迹；
        若底层 I/O/布局/同步约定变更，上层应打\textbf{脏标记}，不能因「上次 PASS」假绿。
\end{itemize}
\end{example}

\paragraph{三件工具如何串起来}
一次受控作业，导论级检查顺序就是：
\[
\underbrace{\text{依赖图 / 闭包}}_{\text{还缺谁？先交闭包表}}
\;\longrightarrow\;
\underbrace{\text{形式语言}}_{\text{这计划合法吗？}}
\;\longrightarrow\;
\underbrace{\text{自动机}}_{\text{按转移往前走；获证或脏标记}}.
\]
\textbf{学了不代替什么}：不代替「核怎么写、算得对不对」——那是能力建设本身的另一条知识栈；
本章三件工具只服务：\textbf{已有能力之上，作业该怎么推进才合法、可控}。

\subsubsection{从场景回到可检查的问题（备忘）}

上面的场景，落到检查清单上，仍会变成若干可写的问题
（第~\ref{sec:hook}~章四组问题是它们在 KeyGen 上的实例）：
可信前置从何而来、组合何时完成、搜索如何收束、成功如何交接、正确性与合法性如何分工。
导论里\textbf{不}把这些问题当作本章的最高问题——最高问题是场景本身：
何时该用这套方法、它替用户把「能力事实」翻译成哪一类判断。
后文数学与门禁，是为在这些场景里把判断做可检查。

\subsubsection{最小集合、必须掌握与本章边界}

这里说的「理解和控制整个工程」，\textbf{不是}指掌握写 AscendC、跑 CANN、证 ML-KEM 正确性的全部知识；
而是特指：\textbf{预研过程是否沿已验证能力合法生长}。

我们对「最小」取工程判定：
\begin{quote}
\noindent
若删去三件工具中的某一件，第~\ref{sec:hook}~章四组问题里至少有一类会\textbf{重新失去可写的控制句柄}；
若再往本章塞进密码代数或可判定性专论，则对「合法生长」的控制\textbf{并不增加一等必要句柄}。
\end{quote}

\begin{table}[H]
\centering\small
\caption{三件工具为何构成「合法生长」控制的最小集合}
\label{tab:ch3-minimal}
\begin{tabular}{@{}c L{3.2cm} L{4.6cm} L{5.0cm}@{}}
  \toprule
  大段 & 数学工具 & 删掉它会失去什么控制 & 能否用其它工具顶替 \\
  \midrule
  一 & 依赖图与依赖闭包 &
  无法回答「谁依赖谁 / 还缺谁」 &
  否：形式语言与自动机都\textbf{预设}依赖世界已画清 \\
  \midrule
  二 & 形式语言 &
  无法区分「菜单绿」与「拼法合法」 &
  否：依赖齐备 $\neq$ 拼装计划合法 \\
  \midrule
  三 & 作业过程自动机 &
  无法描述作业如何接受，也无法作废过期证 &
  否：静态依赖图与语言都不谈过程转移 \\
  \midrule
  落地 & 对照回 KeyGen &
  术语落不回故事，学完仍不会用 &
  可缩短叙述，但\textbf{不可}从学习路径删掉 \\
  \bottomrule
\end{tabular}
\end{table}

\paragraph{必须掌握什么（验收清单）}
读完本章，至少应能\textbf{独立完成}：
\begin{enumerate}
  \item \textbf{依赖图与依赖闭包}：画出依赖图片段与探路顺序；写出依赖闭包与缺项；说明闭包表核什么。
  \item \textbf{形式语言（合法拼装）}：说明为何「单点 PASS」推不出「拼接 PASS」；说明门禁如何检查拼装计划。
  \item \textbf{作业过程自动机}：说出「展开 $\to$ 探针取证 $\to$ 写入证书」到接受的大意；解释脏标记为何使已获证清单可收缩。
  \item \textbf{枢纽}：能指出依赖、展开、进度分别由\textbf{依赖图与闭包}、\textbf{形式语言}、\textbf{作业过程自动机}哪一件管。
  \item \textbf{落地}：指着 KeyGen 故事说清：哪一段落在依赖图与闭包、哪一段落在形式语言、哪一段落在作业过程自动机。
\end{enumerate}

\paragraph{刻意不纳入本章的}
\begin{center}
\begin{tabular}{@{}L{4.2cm}L{9.0cm}@{}}
  \toprule
  不纳入本章 & 原因（一句话） \\
  \midrule
  格、MLWE、NTT 等代数 & 回答「算得对不对」，不回答「沿哪条已证边长」 \\
  AscendC API / tiling 细则 & 回答「核怎么写」，由平台工程笔记与探针承担 \\
  可判定性、复杂度专论 & 后置；门禁 v0 先要对象与规则可写 \\
  仓库字段 / 工作流细节 & 第~\ref{sec:math}--\ref{sec:gates}~章工程化，不在本章展开 \\
  \bottomrule
\end{tabular}
\end{center}

\subsubsection{本章主线一览（读图后再进数学）}
\label{sec:mathbg-spine}

正文按三件数学工具分成三大段；每一大段内部都是：\textbf{先写该工具的数学}，再写\textbf{它在工程过程中的体现}。
从下一节起，用一整大节写依赖图与依赖闭包（从集合讲到闭包）；随后两大节分别写形式语言与作业过程自动机。

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  box/.style={draw, rounded corners=2pt, align=left, font=\small,
              text width=10.2cm, inner sep=5pt},
  arr/.style={-{Stealth[length=2.4mm]}, very thick},
  lab/.style={font=\scriptsize, text=gray!45!black}
]
  \node[box, fill=blue!10] (t1)
    {\textbf{工具一：依赖图与依赖闭包}\\[2pt]
     数学：集合/关系 $\to$ 图 / DAG / 拓扑序 / 闭包 / 缺项\\[1pt]
     工程：闭包表——还缺谁？};
  \node[box, fill=orange!12, below=8mm of t1] (t2)
    {\textbf{工具二：形式语言（合法拼装）}\\[2pt]
     数学：字母表 / 字 / 语言 / 文法 / 成员判定\\[1pt]
     工程：拼装计划是否合法？禁止拼法？};
  \node[box, fill=green!12, below=8mm of t2] (t3)
    {\textbf{工具三：作业过程自动机}\\[2pt]
     数学：状态（配置）/ 转移 / 接受与拒绝 / 脏标记\\[1pt]
     工程：作业怎么走完？证书何时作废？};
  \node[box, fill=violet!10, below=8mm of t3] (t4)
    {\textbf{落地：对照回第~2~章 KeyGen}\\[2pt]
     用上面三件工具把故事再说一遍};
  \draw[arr] (t1) -- node[lab, right]{有了依赖，才能谈拼法} (t2);
  \draw[arr] (t2) -- node[lab, right]{有了合法计划，才能谈过程} (t3);
  \draw[arr] (t3) -- node[lab, right]{对照故事} (t4);
\end{tikzpicture}
\caption{第~3~章主线：三大工具段（每段＝数学定义 + 工程体现）}
\label{fig:ch3-spine}
\end{figure}

\begin{center}
\begin{tabular}{@{}L{2.8cm}L{5.8cm}L{5.2cm}@{}}
  \toprule
  & 本章建设 & 本章明确不建设 \\
  \midrule
  对象 & 依赖图与闭包、形式语言、自动机（及证书演化） & 密码方案正确性证明 \\
  体例 & 定义 / 命题 / 定理 / 引理 / 推论 + 双例 & 百科式堆砌无证明的名词 \\
  深度 & 以「后文门禁够用」为准；标准结果给证明梗概 & 可判定性、复杂度专论（后置） \\
  \bottomrule
\end{tabular}
\end{center}

\begin{remark}[阅读目标]
读完导论，应能用自己的话复述：这套方法主要帮研究和开发各解决什么问题、
它适合什么样的工作（以及和普通「模块+测试+版本管理」差在哪）、三件工具各管什么。
读完后文数学后，应能按「必须掌握」清单自检，并对照表~\ref{tab:ch3-term-bridge} 说出工程术语与数学记号的对应。
\end{remark}

下面按三件工具各开一大节写数学；写完再统一谈工程体现、回扣第~2~章、枢纽对照与本章小结。

\subsection{依赖图与依赖闭包}
\label{sec:tool-dep}

本大节专门写第一件数学工具：\textbf{依赖图}与其上的\textbf{依赖闭包}。
目标一句话：说清「谁依赖谁、先证谁」，并算出「动手前还缺谁」。
叙述从集合、关系与函数讲起，再到有向图、DAG、拓扑序，最后落到依赖闭包与缺项。
学完应能画出依赖关系、写出缺项；工程落点（闭包表等）留到后文「工程体现」统一写。

\subsubsection{集合、笛卡尔积、关系与函数}
\label{sec:set-rel}

\begin{definition}[集合与属于关系]
称由确定对象组成的汇集为\textbf{集合}。
若对象 $x$ 属于集合 $A$，记 $x\in A$；否则记 $x\notin A$。
不含任何元素的集合称为\textbf{空集}，记作 $\emptyset$。
若 $A$ 的每个元素都属于 $B$，称 $A$ 为 $B$ 的\textbf{子集}，记 $A\subseteq B$。
\end{definition}

\begin{definition}[幂集]
设 $V$ 为集合。由 $V$ 的\textbf{全部子集}组成的集合称为 $V$ 的\textbf{幂集}，记作 $2^{V}$
（亦常记作 $\mathcal{P}(V)$）。用集合造集的写法即
\[
2^{V}
\;=\;
\{\,A\mid A\subseteq V\,\}.
\]
特别地：$\emptyset\in 2^{V}$，$V\in 2^{V}$；若 $V$ 有限且 $|V|=n$，则 $|2^{V}|=2^{n}$
（这正是记号 $2^{V}$ 的来源）。

映射 $f:2^{V}\to 2^{V}$ 表示：输入是 $V$ 的某个子集，输出仍是 $V$ 的某个子集。
\end{definition}

\begin{remark}[为何是 $2^{V}$，而不是 $3^{V}$、$4^{V}$]
底数 $2$ 来自构造子集时的\textbf{二元选择}：对 $V$ 中每个元素，只有「放进该子集」或「不放进」两种可能。
因此不同子集的个数是 $2\times 2\times\cdots\times 2$（共 $|V|$ 个因子），即 $2^{|V|}$。
若改写成 $3^{V}$，一般既不等于「全部子集的集合」，也没有上述计数意义
（除非另作约定，例如讨论「每个元素三种染色」的函数集 $\{0,1,2\}^{V}$，那是另一对象）。

等价观点：把 $2$ 看成二元集 $\{0,1\}$，则 $2^{V}$ 也可理解为「全体函数 $V\to\{0,1\}$」
（特征函数：取 $1$ 表示属于子集，取 $0$ 表示不属于）；这与「全部子集」一一对应。
\end{remark}

\begin{example}[纯数学：幂集]
令 $V=\{a,b\}$。则
\[
2^{V}
=\bigl\{\,\emptyset,\;\{a\},\;\{b\},\;\{a,b\}\,\bigr\},
\qquad |2^{V}|=4=2^{2}.
\]
\end{example}

\begin{definition}[笛卡尔积]
设 $A,B$ 为集合。其\textbf{笛卡尔积}为
\[
A\times B \;=\; \{\,(a,b)\mid a\in A,\; b\in B\,\}.
\]
元素 $(a,b)$ 称为\textbf{有序对}；一般地 $(a,b)=(a',b')$ 当且仅当 $a=a'$ 且 $b=b'$。
\end{definition}

\begin{definition}[二元关系]
设 $A,B$ 为集合。若 $R\subseteq A\times B$，则称 $R$ 为从 $A$ 到 $B$ 的\textbf{二元关系}。
当 $A=B$ 时，也称 $R$ 为 $A$ 上的二元关系。
若 $(a,b)\in R$，常简记为 $a\,R\,b$。
\end{definition}

\begin{definition}[关系的定义域与值域]
对关系 $R\subseteq A\times B$，定义
\[
\mathrm{dom}(R)=\{a\in A\mid \exists b\in B,\;(a,b)\in R\},
\quad
\mathrm{ran}(R)=\{b\in B\mid \exists a\in A,\;(a,b)\in R\}.
\]
\end{definition}

\begin{definition}[函数]
设 $A,B$ 为集合。称关系 $f\subseteq A\times B$ 为从 $A$ 到 $B$ 的\textbf{函数}
（记 $f:A\to B$），若对每个 $a\in A$，存在唯一的 $b\in B$ 使得 $(a,b)\in f$。
此时记 $b=f(a)$，称 $b$ 为 $a$ 在 $f$ 下的\textbf{像}。
\end{definition}

\begin{property}[关系与函数的对比]
二元关系允许「一个 $a$ 对应多个 $b$」；函数禁止这一点。
后文「证书状态赋值」按函数理解；「依赖边」按一般关系理解。
\end{property}

\begin{example}[纯数学：课程先修关系]
令 $A=\{\text{高数},\text{线代},\text{概率},\text{机器学习}\}$，
定义 $R\subseteq A\times A$ 为
\[
R=\{(\text{高数},\text{概率}),\,(\text{线代},\text{机器学习}),\,(\text{概率},\text{机器学习})\}.
\]
则 $(\text{高数},\text{概率})\in R$，但 $(\text{概率},\text{高数})\notin R$（有序性）。
映射 $\mathrm{grade}:A\to\{A,B,C\}$ 若存在，则是函数：每门课恰一个成绩。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=2.4cm, y=1.35cm,
  every node/.style={font=\small},
  c/.style={draw, circle, minimum size=1.15cm, align=center, inner sep=1pt},
  arr/.style={-{Stealth[length=2mm]}, thick}
]
  \node[c] (calc) at (0,1) {高数};
  \node[c] (lin) at (0,0) {线代};
  \node[c] (prob) at (1.2,1) {概率};
  \node[c] (ml) at (2.4,0.5) {机器\\学习};
  \draw[arr] (calc) -- (prob);
  \draw[arr] (prob) -- (ml);
  \draw[arr] (lin) -- (ml);
\end{tikzpicture}
\caption{配图：纯数学：课程先修关系}
\label{fig:auto-01}
\end{figure}
\end{example}

\begin{example}[工程：§2 数据依赖中的关系]
令 $V_0=\{d,z,\text{Alg.13},\mathit{ek}_{\mathrm{PKE}},\mathit{dk}_{\mathrm{PKE}},H,\mathit{ek}_{\mathrm{KEM}},\mathit{dk}_{\mathrm{KEM}}\}$。
§2 图二中的箭头构成 $V_0$ 上的关系 $R_{\mathrm{KG}}$，例如
$(d,\text{Alg.13})\in R_{\mathrm{KG}}$，$(z,\text{Alg.13})\notin R_{\mathrm{KG}}$。
「把 $\mathit{ek}_{\mathrm{PKE}}$ 直通记为 $\mathit{ek}_{\mathrm{KEM}}$」可视为部分函数
$\iota:\{\mathit{ek}_{\mathrm{PKE}}\}\to\{\mathit{ek}_{\mathrm{KEM}}\}$。
下图为关系 $R_{\mathrm{KG}}$ 的示意（数据流语义；闭包节将改用依赖方向）。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=1.55cm, y=1.05cm,
  every node/.style={font=\footnotesize},
  v/.style={draw, ellipse, minimum width=1.35cm, minimum height=0.55cm, inner sep=1pt},
  op/.style={draw, rounded corners=2pt, align=center, minimum width=1.5cm, minimum height=0.55cm},
  arr/.style={-{Stealth[length=1.8mm]}, thick}
]
  \node[v] (d) at (0,2) {$d$};
  \node[v] (z) at (2.2,2) {$z$};
  \node[op, fill=blue!8] (a13) at (0,1) {Alg.13};
  \node[v] (dkp) at (-0.9,0) {$\mathit{dk}_{\mathrm{PKE}}$};
  \node[v] (ekp) at (0.9,0) {$\mathit{ek}_{\mathrm{PKE}}$};
  \node[op, fill=gray!12] (H) at (0.9,-1) {$H$};
  \node[v, fill=green!10] (ek) at (-0.9,-2) {$\mathit{ek}_{\mathrm{KEM}}$};
  \node[op, fill=orange!12] (a16) at (1.2,-2) {拼 $\mathit{dk}$};
  \node[v, fill=green!10] (dk) at (1.2,-3.1) {$\mathit{dk}_{\mathrm{KEM}}$};
  \draw[arr] (d) -- (a13);
  \draw[arr] (a13) -- (dkp);
  \draw[arr] (a13) -- (ekp);
  \draw[arr] (ekp) -- (H);
  \draw[arr] (ekp) -- (ek);
  \draw[arr] (dkp) -- (a16);
  \draw[arr] (H) -- (a16);
  \draw[arr] (z) -- (a16);
  \draw[arr] (a16) -- (dk);
\end{tikzpicture}
\caption{示意配图 2}
\label{fig:auto-02}
\end{figure}
\end{example}

\subsubsection{有向图、路与环}
\label{sec:digraph}

\begin{definition}[有向图]
\textbf{有向图}是一个有序对 $G=(V,E)$，其中 $V$ 为非空有限集（称为\textbf{顶点集}），
$E\subseteq V\times V$ 为\textbf{有向边集}。
若 $(u,v)\in E$，称有向边 $u\to v$，并称 $u$ 为该边的\textbf{起点}，$v$ 为\textbf{终点}。
\end{definition}

\begin{definition}[出邻域与入邻域]
对 $v\in V$，定义
\[
N^+(v)=\{u\in V\mid (v,u)\in E\},
\quad
N^-(v)=\{u\in V\mid (u,v)\in E\}.
\]
分别称为 $v$ 的\textbf{出邻域}与\textbf{入邻域}；
$|N^+(v)|$、$|N^-(v)|$ 称为出度、入度。
\end{definition}

\begin{definition}[有向途径、路径与环]
设 $G=(V,E)$。
\begin{enumerate}
  \item 顶点序列 $v_0,v_1,\ldots,v_k$（$k\ge 0$）称为\textbf{有向途径}，若对所有 $0\le i<k$ 有 $(v_i,v_{i+1})\in E$；
        称 $k$ 为该途径的\textbf{长度}。
  \item 若途径中顶点两两不同，则称为\textbf{有向路径}（简称路径）。
  \item 若 $k\ge 1$、$v_0=v_k$，且 $v_0,\ldots,v_{k-1}$ 两两不同，则称为\textbf{有向环}（简称环）。
\end{enumerate}
\end{definition}

\begin{definition}[可达]
若存在从 $u$ 到 $v$ 的有向途径，则称 $v$ 从 $u$ \textbf{可达}，记 $u\rightsquigarrow_G v$
（在上下文明确时可略去 $G$）。约定任意 $v$ 满足 $v\rightsquigarrow v$（长度为 $0$ 的途径）。
\end{definition}

\begin{lemma}[路径与途径]
在有限有向图中，$u\rightsquigarrow v$ 当且仅当存在从 $u$ 到 $v$ 的有向路径。
\end{lemma}
\begin{proof}
若存在途径，删去其上环段可得路径；反之，路径本身是途径。
\end{proof}

\begin{example}[纯数学：四点有向图]
令 $V=\{1,2,3,4\}$，$E=\{(1,2),(1,3),(2,4),(3,4)\}$。
则 $1\rightsquigarrow 4$，且存在两条不同路径 $1\to2\to4$ 与 $1\to3\to4$。
$4$ 的入邻域为 $N^-(4)=\{2,3\}$，出邻域 $N^+(4)=\emptyset$。
该图无环。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=2.0cm, y=1.4cm,
  every node/.style={font=\small},
  c/.style={draw, circle, minimum size=0.7cm},
  arr/.style={-{Stealth[length=2mm]}, thick}
]
  \node[c] (1) at (0,1) {$1$};
  \node[c] (2) at (-0.8,0) {$2$};
  \node[c] (3) at (0.8,0) {$3$};
  \node[c] (4) at (0,-1) {$4$};
  \draw[arr] (1) -- (2);
  \draw[arr] (1) -- (3);
  \draw[arr] (2) -- (4);
  \draw[arr] (3) -- (4);
  \node[font=\footnotesize, anchor=west] at (1.5,0) {$N^-(4)=\{2,3\}$};
\end{tikzpicture}
\caption{配图：纯数学：四点有向图}
\label{fig:auto-03}
\end{figure}
\end{example}

\begin{example}[工程：KeyGen 数据流图是有向图]
§2 图二给出顶点（如 $d$、$z$、Alg.13、$\mathit{dk}_{\mathrm{KEM}}$）与有向边。
$\mathit{dk}_{\mathrm{KEM}}$ 的入度大于 $1$（多源汇合）；
$z\rightsquigarrow \mathit{dk}_{\mathrm{KEM}}$ 但 $z\not\rightsquigarrow \text{Alg.13}$。
这与「树只有单父」不同：有向图允许\textbf{多父汇点}（见上节工程例图中「拼 $\mathit{dk}$」结点）。
\end{example}

\subsubsection{有向无环图与拓扑序}
\label{sec:dag-topo}

\begin{definition}[DAG]
若有向图 $G$ 不含有向环，则称 $G$ 为\textbf{有向无环图}（Directed Acyclic Graph，DAG）。
\end{definition}

\begin{definition}[拓扑序]
设 $G=(V,E)$，$|V|=n$。
顶点序列 $v_1,\ldots,v_n$ 称为 $G$ 的一个\textbf{拓扑序}，
若它是 $V$ 的排列，且对每条边 $(v_i,v_j)\in E$ 都有 $i<j$。
\end{definition}

\begin{theorem}[DAG 与拓扑序的刻画]
设 $G$ 为有限有向图。则下列条件等价：
\begin{enumerate}
  \item $G$ 是 DAG；
  \item $G$ 存在拓扑序。
\end{enumerate}
\end{theorem}
\begin{proof}[证明梗概]
（2）${\Rightarrow}$（1）：若有环，则环上边无法全部满足「起点下标小于终点下标」。\\
（1）${\Rightarrow}$（2）：DAG 中必有入度为 $0$ 的顶点；取其为 $v_1$，删去后再对剩余图归纳。
\end{proof}

\begin{corollary}[可按先决条件调度]
若依赖关系构成 DAG，则存在线性顺序，使得每条依赖边都从「先处理」指向「后处理」。
\end{corollary}

\begin{remark}[预研建模提示]
「先验证下层能力，再拼上层任务」要求依赖关系可拓扑排序，因而应建模为 DAG。
若出现环，通常意味着节点切分不清或误把双向约束画成了依赖。
\end{remark}

\begin{example}[纯数学：给四点图写拓扑序]
续前例 $E=\{(1,2),(1,3),(2,4),(3,4)\}$。
$1,2,3,4$ 与 $1,3,2,4$ 都是拓扑序；$2,1,3,4$ 不是（边 $1\to2$ 逆向）。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=1.35cm, y=0.9cm,
  every node/.style={font=\small},
  c/.style={draw, circle, minimum size=0.55cm},
  arr/.style={-{Stealth[length=1.8mm]}, thick}
]
  \node[font=\footnotesize] at (-0.2,1.35) {合法：$1,2,3,4$};
  \foreach \i/\x in {1/0,2/1,3/2,4/3} {\node[c, fill=blue!8] (\i) at (\x,0.7) {$\i$};}
  \draw[arr] (1) -- (2); \draw[arr] (2) -- (3); \draw[arr] (3) -- (4);
  \node[font=\footnotesize] at (-0.2,-0.15) {非法：$2,1,3,4$（$1\to2$ 逆向）};
  \node[c, fill=red!10] (a) at (0,-0.8) {$2$};
  \node[c, fill=red!10] (b) at (1,-0.8) {$1$};
  \node[c] (c) at (2,-0.8) {$3$};
  \node[c] (d) at (3,-0.8) {$4$};
  \draw[arr, red!70!black] (b) -- (a);
  \draw[arr] (b) -- (c); \draw[arr] (c) -- (d);
\end{tikzpicture}
\caption{配图：纯数学：给四点图写拓扑序}
\label{fig:auto-04}
\end{figure}
\end{example}

\begin{example}[工程：能力依赖链]
将「P0 平台事实 $\to$ P1 原语 $\to$ P2 PKE.KeyGen $\to$ P3 KEM.KeyGen」视为一条链，则它是 DAG，
且该顺序本身就是拓扑序。真实仓库图会有多父多子，但仍应保持无环。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=2.0cm, y=0.95cm,
  every node/.style={font=\small},
  box/.style={draw, rounded corners=2pt, align=center, minimum width=1.9cm, minimum height=0.65cm},
  arr/.style={-{Stealth[length=2mm]}, thick}
]
  \node[box, fill=blue!8] (p0) at (0,1.5) {P0};
  \node[box, fill=blue!8] (p1) at (1.2,1.5) {P1};
  \node[box, fill=blue!10] (p2) at (2.4,1.5) {P2};
  \node[box, fill=orange!12] (p3) at (3.6,1.5) {P3};
  \draw[arr] (p0) -- (p1) -- (p2) -- (p3);
\end{tikzpicture}
\caption{配图：工程：能力依赖链}
\label{fig:auto-05}
\end{figure}
\end{example}

\begin{remark}[DAG 不回答的问题]
能力 DAG 描述\textbf{引理库中的静态依赖投影}（谁可以依赖谁）。
它不自动给出「某次做 KEM.KeyGen 时 Agent 实际展开的那棵树」——后者是派生（见后文）。
\end{remark}

\subsubsection{闭包：从一般概念到依赖闭包与缺项}
\label{sec:closure}

前几小节已有集合、关系、有向图。这里先回答：
\textbf{数学里说的「闭包」究竟是什么？}
再落到能力依赖图上的\textbf{依赖闭包}、\textbf{已获证}与\textbf{缺项}。

\paragraph{在运算下封闭}

\begin{definition}[一元运算下的封闭性]
设 $U$ 为全集（讨论所在的大集合），$A\subseteq U$，
又设 $\varphi:U\to U$ 为一元运算（函数）。
称 $A$ 对 $\varphi$ \textbf{封闭}（或「在 $\varphi$ 下封闭」），若
\[
\forall x\in A,\quad \varphi(x)\in A.
\]
即：从 $A$ 里任取元素做一次 $\varphi$，结果仍落在 $A$ 里。
\end{definition}

\begin{definition}[二元运算下的封闭性]
设 $*\!:U\times U\to U$ 为二元运算。
称 $A\subseteq U$ 对 $*$ \textbf{封闭}，若
\[
\forall x,y\in A,\quad x*y\in A.
\]
\end{definition}

\begin{definition}[对一族运算封闭]
若给定若干运算（可混一元、二元），称 $A$ 对它们\textbf{封闭}，
当且仅当 $A$ 对其中每一个运算都封闭。
\end{definition}

\begin{example}[纯数学：封闭与不封闭]
取 $U=\mathbb{Z}$。
\begin{itemize}
  \item 偶数集 $2\mathbb{Z}$ 对加法封闭：两偶数之和仍为偶数。
  \item 奇数集对加法\textbf{不}封闭：$1+1=2$ 不是奇数。
  \item $\{0,1,2\}$ 对「加 $1$」这一元运算不封闭，因为 $\varphi(2)=3\notin\{0,1,2\}$。
\end{itemize}
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  every node/.style={font=\small},
  s/.style={draw, circle, minimum size=0.7cm},
  arr/.style={-{Stealth[length=1.8mm]}, thick}
]
  \node[s, fill=blue!12] (0) at (0,0) {$0$};
  \node[s, fill=blue!12] (1) at (1.4,0) {$1$};
  \node[s, fill=blue!12] (2) at (2.8,0) {$2$};
  \node[s, fill=orange!20] (3) at (4.2,0) {$3$};
  \draw[arr] (0) -- (1); \draw[arr] (1) -- (2); \draw[arr] (2) -- (3);
  \node[font=\footnotesize, anchor=west] at (5.1,0.15) {$\varphi(x)=x+1$};
  \node[font=\footnotesize, anchor=west] at (5.1,-0.35) {蓝集对 $\varphi$ 不封闭};
\end{tikzpicture}
\caption{配图：纯数学：封闭与不封闭}
\label{fig:auto-06}
\end{figure}
\end{example}

\begin{example}[工程直觉（尚不形式化）]
若「已具备的能力集合」$\Gamma$ 在「取直接依赖」这一操作下不封闭，
就意味着：你以为齐了，但某个绿结点还缺未列入的先决——这正是后文要引入的工程对象「\textbf{缺项}」所要抓的现象。
\end{example}

\paragraph{闭包：最小的封闭扩张}

直观想法：给定种子集合 $A$，以及若干「生成规则」（运算），
我们希望找到\textbf{既包含 $A$，又对规则封闭，又尽可能小}的集合。
这个最小集合就叫做 $A$ 的闭包。

\begin{definition}[闭包（由封闭性刻画）]
设 $U$ 为全集，$A\subseteq U$，并固定一组使子集可谈「封闭」的运算规则 $\mathcal{R}$。
称 $B\subseteq U$ 为 $A$ 关于 $\mathcal{R}$ 的一个\textbf{闭包}，若：
\begin{enumerate}
  \item $A\subseteq B$（包含种子）；
  \item $B$ 对 $\mathcal{R}$ 封闭；
  \item 若 $C\supseteq A$ 且 $C$ 对 $\mathcal{R}$ 封闭，则 $B\subseteq C$
        （$B$ 是所有「含 $A$ 的封闭集」中的\textbf{最小}者）。
\end{enumerate}
若这样的 $B$ 存在，常记作 $\mathrm{Cl}_{\mathcal{R}}(A)$，或在上下文明确时记 $\mathrm{Cl}(A)$。
\end{definition}

\begin{theorem}[闭包的存在性（有限情形够用版）]
若 $U$ 有限，则对任意 $A\subseteq U$ 与任意「从已有元素产生新元素」的规则族 $\mathcal{R}$
（形式：由已有元组确定性地加入有限个新点），$\mathrm{Cl}_{\mathcal{R}}(A)$ 存在且唯一，
并可取为
\[
\mathrm{Cl}_{\mathcal{R}}(A)
\;=\;
\bigcap\{\,C\subseteq U\mid A\subseteq C,\; C\text{ 对 }\mathcal{R}\text{ 封闭}\,\}.
\]
（任意一族含 $A$ 的封闭集，其交仍含 $A$ 且封闭；有限全集上该族非空，因 $U$ 本身封闭。）
\end{theorem}
\begin{proof}[证明梗概]
验证「含 $A$ 的封闭集」对任意交封闭，故交集是最小元。唯一性由最小性立即得到。
\end{proof}

\begin{remark}[两种看法同一个对象]
\begin{itemize}
  \item \textbf{由外向内}：所有合格封闭集的交；
  \item \textbf{由内向外}：从 $A$ 出发反复施加规则，直到不能再扩大（见下一小节迭代）。
\end{itemize}
后文算法几乎总用「由内向外」。
\end{remark}

\begin{definition}[迭代构造（一般模板）]
设规则是：对当前集合 $X$，令 $\Phi(X)$ 为「把 $X$ 对规则应用一轮后应有的元素」
（要求 $X\subseteq\Phi(X)$）。定义
\[
A_0=A,\qquad A_{k+1}=\Phi(A_k)\quad(k\ge 0),\qquad
A_\infty=\bigcup_{k\ge 0}A_k.
\]
在有限全集且 $\Phi$ 单调时，存在 $K$ 使 $A_K=A_{K+1}=\cdots=A_\infty$，且 $A_\infty=\mathrm{Cl}(A)$。
\end{definition}

\begin{example}[纯数学：对「加 $1$」的闭包]
取 $U=\{0,1,2,3,4\}$，$\varphi(x)=x+1$（约定 $\varphi(4)=4$ 以免越界），$A=\{2\}$。
则迭代：$\{2\}\to\{2,3\}\to\{2,3,4\}\to\{2,3,4\}$。
故 $\mathrm{Cl}(A)=\{2,3,4\}$——包含种子，并对 $\varphi$ 封闭的最小集。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  every node/.style={font=\small},
  s/.style={draw, circle, minimum size=0.65cm},
  arr/.style={-{Stealth[length=1.8mm]}, thick}
]
  \node[s] (0) at (0,0) {$0$};
  \node[s] (1) at (1.2,0) {$1$};
  \node[s, fill=orange!20] (2) at (2.4,0) {$2$};
  \node[s, fill=blue!15] (3) at (3.6,0) {$3$};
  \node[s, fill=blue!15] (4) at (4.8,0) {$4$};
  \draw[arr] (0) -- (1) -- (2) -- (3) -- (4);
  \node[font=\footnotesize, anchor=north] at (2.4,-0.55) {种子};
  \node[font=\footnotesize, anchor=north] at (4.2,-0.55) {闭包多出来的点};
\end{tikzpicture}
\caption{配图：纯数学：对「加 $1$」的闭包}
\label{fig:auto-07}
\end{figure}
\end{example}

\paragraph{闭包算子}

不必每次都提运算 $\mathcal{R}$，也可以直接把「取闭包」看成作用在幂集上的函数
（幂集 $2^{U}$ 的定义见 §3.2）。

\begin{definition}[闭包算子]
设 $U$ 为集合。映射 $\mathrm{Cl}:2^{U}\to 2^{U}$ 称为\textbf{闭包算子}，若对任意 $A,B\subseteq U$
（即对任意 $A,B\in 2^{U}$）：
\begin{enumerate}
  \item \textbf{扩张性}：$A\subseteq \mathrm{Cl}(A)$；
  \item \textbf{单调性}：$A\subseteq B\Rightarrow \mathrm{Cl}(A)\subseteq\mathrm{Cl}(B)$；
  \item \textbf{幂等性}：$\mathrm{Cl}(\mathrm{Cl}(A))=\mathrm{Cl}(A)$。
\end{enumerate}
称 $\mathrm{Cl}(A)$ 为 $A$ 的\textbf{闭包}；若 $A=\mathrm{Cl}(A)$，称 $A$ 为\textbf{闭集}（对该算子而言）。
\end{definition}

\begin{proposition}[闭集即「已经封闭」]
$A$ 为闭集当且仅当 $\mathrm{Cl}(A)=A$。
对由规则 $\mathcal{R}$ 导出的闭包算子，闭集恰好是对 $\mathcal{R}$ 封闭的那些子集。
\end{proposition}

\begin{property}[闭包是「补全到封闭」]
$\mathrm{Cl}(A)$ 可以理解为：从 $A$ 出发，把所有被规则逼出来的点都补上，直到不能再补。
幂等性说：补第二次不会再增加新点。
\end{property}

\begin{example}[纯数学：若干熟悉的闭包]
\begin{center}
\begin{tabular}{@{}L{3.2cm}L{5.0cm}L{5.2cm}@{}}
  \toprule
  场景 & 种子 $A$ & 闭包在补什么 \\
  \midrule
  实数区间 & $\{0,1\}$ & 对「中间点」封闭 $\to [0,1]$（拓扑闭包的朴素版） \\
  线性代数 & 若干向量 & 对加与数乘封闭 $\to$ 生成子空间 \\
  关系 & 边集 $E$ & 对「可走更长路径」封闭 $\to E^{*}$（下一小节） \\
  \bottomrule
\end{tabular}
\end{center}
\end{example}

\paragraph{关系的传递闭包：闭包的第一个正式例子}

现在回到二元关系。这是连接「一般闭包」与「图上依赖闭包」的桥梁。

以下固定有限集 $V$，关系 $R\subseteq V\times V$。

\begin{definition}[关系的复合与幂]
对 $R,S\subseteq V\times V$，定义复合
\[
R\circ S=\{\,(u,w)\mid \exists v,\;(u,v)\in R,\;(v,w)\in S\,\}.
\]
令 $R^{1}=R$，$R^{k+1}=R^{k}\circ R$，并令 $R^{0}=\Delta_V=\{(v,v)\mid v\in V\}$。
注意：$R^{0}$ 是\textbf{单独约定}的恒等关系，表示长度为 $0$ 的途径；
它\textbf{不是}把 $R$ 与自身复合得到的（复合从 $R^{1}$、$R^{2},\ldots$ 才开始）。
\end{definition}

\begin{definition}[传递闭包与自反传递闭包]
\[
R^{+}=\bigcup_{k\ge 1}R^{k},
\qquad
R^{*}=\bigcup_{k\ge 0}R^{k}=R^{0}\cup R^{+}.
\]
称 $R^{+}$ 为 $R$ 的\textbf{传递闭包}，$R^{*}$ 为\textbf{自反传递闭包}。
\end{definition}

\begin{proposition}[$R^{*}$ 确实是闭包]
在「关系集合」$U=2^{V\times V}$ 上，若规则是「若有 $(u,v),(v,w)$ 则加入 $(u,w)$，且保留全部恒等对」，
则从 $R$ 生成的闭包恰为 $R^{*}$。
特别地：$R\subseteq R^{*}$，$R^{*}$ 传递且自反，且是满足这些性质的最小关系。
\end{proposition}

\begin{theorem}[传递闭包与路径]
$(u,v)\in R^{k}$ 当且仅当存在长度恰为 $k$ 的 $R$-途径从 $u$ 到 $v$。
因而 $(u,v)\in R^{*}$ 当且仅当 $u$ 经若干步（含 $0$ 步）到达 $v$。
\end{theorem}

\begin{example}[纯数学：三点关系的传递闭包]
令 $V=\{a,b,c\}$，$R=\{(a,b),(b,c)\}$。逐项按定义展开：
\begin{align*}
R^{0} &=\Delta_V=\{(a,a),(b,b),(c,c)
\} && \text{（恒等关系；\textbf{不是}由 $R$ 复合得到）}, \\
R^{1} &=R=\{(a,b),(b,c)\}, \\
R^{2} &=R\circ R=\{(a,c)
\} && \text{（复合：经中间点 $b$）}, \\
R^{+} &=R^{1}\cup R^{2}=\{(a,b),(b,c),(a,c)
\} && \text{（传递闭包：只含正长度路径）}, \\
R^{*} &=R^{0}\cup R^{+}=\{(a,a),(b,b),(c,c),(a,b),(b,c),(a,c)\}.
\end{align*}
因此：$(a,a)$ 这类对角元来自 $R^{0}$（$0$ 步路径约定），
\textbf{不是} $R\circ R$ 算出来的；本例中 $R$ 本身也没有自环。
图中三种线分别对应三类来源：
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  every node/.style={font=\small},
  c/.style={draw, circle, minimum size=0.75cm},
  arr/.style={-{Stealth[length=2mm]}, thick}
]
  \node[c] (a) at (0,0) {$a$};
  \node[c] (b) at (2.2,0) {$b$};
  \node[c] (c) at (4.4,0) {$c$};
  % R
  \draw[arr] (a) -- (b);
  \draw[arr] (b) -- (c);
  % R^2 新增
  \draw[arr, dashed] (a) to[bend left=32] (c);
  % R^0 自环（恒等；与复合无关）
  \draw[arr, densely dotted] (a) edge[loop above] node[above, font=\footnotesize] {$R^{0}$} (a);
  \draw[arr, densely dotted] (b) edge[loop above] node[above, font=\footnotesize] {$R^{0}$} (b);
  \draw[arr, densely dotted] (c) edge[loop above] node[above, font=\footnotesize] {$R^{0}$} (c);
  \node[font=\footnotesize, align=left, anchor=west] at (5.3,0.15) {
    实线 $=R=R^{1}$\\
    虚线 $=R^{2}$ 新增 $(a,c)$\\
    点线自环 $=R^{0}$（恒等）
  };
\end{tikzpicture}
\caption{示意配图 8}
\label{fig:auto-08}
\end{figure}
若教材图只想强调「传递多出来的边」，可省略自环，但须在式子里保留 $R^{0}$，以免误以为 $(a,a)$ 来自复合。
\end{example}

\begin{example}[工程预告]
若把「$u$ 是 $v$ 的直接先决」看成关系 $R$，
则「$u$ 是 $v$ 的直接或间接先决」就是 $(u,v)\in R^{*}$。
下一小节把「目标 $T$ 的全部先决」整理成工程对象\textbf{依赖闭包}，并引入记号 $\mathrm{Dep}^*(T)$。
\end{example}

\paragraph{有向图上的依赖闭包}

从此固定有向图 $G=(V,E)$，并取边语义为依赖：
\[
u\to v \quad\text{表示}\quad\text{「$v$ \textbf{依赖} $u$」}
\quad\text{（$u$ 先决，$v$ 后继）}.
\]
与 §2 数据流箭头可能相反；\textbf{计算闭包以本节为准}。

\begin{definition}[转置图]
$G^{\top}=(V,E^{\top})$，$E^{\top}=\{(v,u)\mid (u,v)\in E\}$（箭头反向）。
\end{definition}

\begin{definition}[直接前驱 / 直接后继]
\[
\mathrm{Pred}_G(v)=N^-(v),\qquad
\mathrm{Succ}_G(v)=N^+(v).
\]
\end{definition}

\begin{definition}[祖先与后代]
若 $u\rightsquigarrow_G v$，称 $u$ 为 $v$ 的\textbf{祖先}，$v$ 为 $u$ 的\textbf{后代}
（含 $u=v$）。
\end{definition}

\begin{definition}[单点依赖闭包]
\label{def:dep-star}
\textbf{工程术语}：目标 $T$ 的\textbf{依赖闭包}——完成 $T$ 理论上必须齐备的全部先决能力（含 $T$ 自身）。\\
\textbf{数学记号}：固定依赖图 $G=(V,E)$，对目标 $T\in V$ 定义
\[
\mathrm{Dep}^*_G(T)
\;=\;
\{u\in V\mid u\rightsquigarrow_G T\}
\;=\;
\{u\in V\mid (u,T)\in E^{*}\}.
\]
恒有 $T\in\mathrm{Dep}^*_G(T)$。略下标时写作 $\mathrm{Dep}^*(T)$。
此后正文若写 $\mathrm{Dep}^*(T)$，一律读作「$T$ 的依赖闭包」。
\end{definition}

\begin{remark}[与一般闭包的对接]
取种子 $\{T\}$，规则为「若 $v$ 已在集合中，则把 $\mathrm{Pred}(v)$ 全部加入」。
则对该规则的闭包恰等于 $\mathrm{Dep}^*(T)$。
换言之：\textbf{依赖闭包 $=$ 「对取前驱封闭」的最小扩张}。
\end{remark}

\begin{proposition}[四种等价说法]
对任意 $u,T\in V$，下列等价：
\begin{enumerate}
  \item $u\in\mathrm{Dep}^*(T)$；
  \item $(u,T)\in E^{*}$；
  \item 存在路径 $u\to\cdots\to T$ 或 $u=T$；
  \item 在 $G^{\top}$ 中 $T\rightsquigarrow_{G^{\top}} u$。
\end{enumerate}
\end{proposition}

\paragraph{依赖闭包的迭代、算法与算子性质}

\begin{definition}[依赖闭包的迭代序列]
为计算 $\mathrm{Dep}^*(T)$，我们\textbf{新引入}一串辅助集合，记作
\[
D_0(T),\; D_1(T),\; D_2(T),\; \ldots
\]
（字母 $D$ 仅表示「Dependency 迭代的第几层近似」，下标 $k=0,1,2,\ldots$ 表示迭代步数；
前文并未使用过该记号，本定义即其首次出现。）

约定：
\begin{align*}
D_0(T) &= \{T\}
&& \text{（第 $0$ 步：只有目标本身）}, \\
D_{k+1}(T) &= D_k(T) \;\cup\; \bigcup_{v\in D_k(T)}\mathrm{Pred}(v)
&& \text{（第 $k{+}1$ 步：把当前集里每点的直接前驱都并进来）}.
\end{align*}
直观上：$D_k(T)$ 是「从 $T$ 沿依赖边反向走、最多 $k$ 步能摸到的点」所生成的集合
（含更近的点）；随着 $k$ 增大，集合只增不减。

这与前面「迭代构造（一般模板）」中的 $A_k$ 是同一类对象：
取种子 $A=\{T\}$、一轮算子 $\Phi(X)=X\cup\bigcup_{v\in X}\mathrm{Pred}(v)$ 时，
恰有 $D_k(T)=A_k$。
\end{definition}

\begin{theorem}[迭代收敛到依赖闭包]
对有限图 $G$，序列 $\{D_k(T)\}_{k\ge 0}$ 在有限步内稳定，且
\[
\bigcup_{k\ge 0} D_k(T) \;=\; \mathrm{Dep}^*(T).
\]
更精确地：存在 $K\le |V|$ 使对所有 $k\ge K$ 有 $D_k(T)=\mathrm{Dep}^*(T)$。
\end{theorem}
\begin{proof}[证明梗概]
$D_k(T)$ 单调递增且含于有限集 $V$，故稳定。
稳定时集合对取 $\mathrm{Pred}$ 封闭，且含 $T$，故包含一切能走到 $T$ 的顶点；
反之，任何通向 $T$ 的长度为 $\ell$ 的路径上的点在 $D_\ell(T)$ 中出现。
\end{proof}

\begin{remark}[与一般闭包模板的关系]
因此「迭代收敛到依赖闭包」就是一般闭包「由内向外」构造在依赖图上的具体化：
做够多步之后，$D_k(T)$ 不再变化，其稳定值就是 $\mathrm{Dep}^*(T)$。
\end{remark}

\begin{lemma}[反向搜索算法]
在有限图上执行：
从 $T$ 出发，在 $G^{\top}$ 中做 BFS/DFS（等价于沿 $G$ 的边反向走），
访问到的顶点集合恰为 $\mathrm{Dep}^*(T)$。
时间复杂度 $O(|V|+|E|)$（邻接表）。
\end{lemma}

\paragraph{目标集的闭包、基与缺项}

前几小节处理的是\textbf{单个}目标 $T$。
工程上一次任务往往同时盯住多个目标（或先把目标拆成若干子目标），
并且手里已有一份「哪些能力当前允许被引用」的清单——下文形式化为\textbf{已获证能力集}（数学记号 $\Gamma$）。
本小节先把依赖闭包从单点推广到有限目标集，再引入证书与缺项、生成集。

\paragraph{为何需要目标集}
若只定义单目标的依赖闭包 $\mathrm{Dep}^*(T)$，则同时要完成 $T_1,T_2$ 时，需要的先决整体是
$\mathrm{Dep}^*(T_1)\cup\mathrm{Dep}^*(T_2)$。
把它写成目标集的依赖闭包 $\mathrm{Dep}^*(\{T_1,T_2\})$，可避免每次手写大并集，也便于后文闭包表一行写多个目标。

\begin{definition}[有限目标集]
称非空有限集 $A\subseteq V$ 为一次讨论中的\textbf{目标集}。
其元素仍是顶点（能力/任务结点）；$A$ 只表示「当前要一起完成的那些目标」。
单点情形是特例：$A=\{T\}$。
\end{definition}

\begin{definition}[目标集的依赖闭包]
\label{def:dep-star-set}
\textbf{工程术语}：目标集 $A$ 的\textbf{依赖闭包}——完成 $A$ 中全部目标所需先决的并集。\\
\textbf{数学记号}：对 $A\subseteq V$，定义
\[
\mathrm{Dep}^*_G(A)
\;=\;
\bigcup_{T\in A}\mathrm{Dep}^*_G(T).
\]
并约定 $\mathrm{Dep}^*_G(\emptyset)=\emptyset$。
不致混淆时略去下标 $G$，写作 $\mathrm{Dep}^*(A)$。
\end{definition}

\begin{proposition}[目标集闭包的等价说法]
对任意 $A\subseteq V$ 与 $u\in V$，下列等价：
\begin{enumerate}
  \item $u\in\mathrm{Dep}^*(A)$；
  \item 存在某个 $T\in A$，使 $u\in\mathrm{Dep}^*(T)$（即 $u\rightsquigarrow T$ 或 $u=T$）；
  \item 存在 $T\in A$，使 $(u,T)\in E^{*}$。
\end{enumerate}
因而 $\mathrm{Dep}^*(A)$ 是「能通向 $A$ 中至少一个目标的全部祖先（含这些目标自身）」。
\end{proposition}

\begin{proposition}[$\mathrm{Dep}^*$ 作为幂集上的闭包算子]
定义 $\mathrm{Cl}:2^{V}\to 2^{V}$ 为 $\mathrm{Cl}(A)=\mathrm{Dep}^*(A)$
（此处 $2^{V}$ 为 $V$ 的幂集，见 §3.2）。则 $\mathrm{Cl}$ 满足：
\begin{enumerate}
  \item \textbf{扩张}：$A\subseteq\mathrm{Cl}(A)$；
  \item \textbf{单调}：$A\subseteq B\Rightarrow\mathrm{Cl}(A)\subseteq\mathrm{Cl}(B)$；
  \item \textbf{幂等}：$\mathrm{Cl}(\mathrm{Cl}(A))=\mathrm{Cl}(A)$。
\end{enumerate}
因此 $\mathrm{Cl}$ 是前文意义下的\textbf{闭包算子}。
进一步：$A$ 对该算子为闭集（即 $\mathrm{Cl}(A)=A$）当且仅当
$A$ 对「取直接前驱」封闭——若 $v\in A$ 则 $\mathrm{Pred}(v)\subseteq A$。
\end{proposition}
\begin{proof}[证明梗概]
（扩张）每个 $T\in A$ 满足 $T\in\mathrm{Dep}^*(T)$，故 $A\subseteq\mathrm{Dep}^*(A)$。\\
（单调）并集对包含关系显然单调。\\
（幂等）若 $u\in\mathrm{Dep}^*(\mathrm{Dep}^*(A))$，则 $u$ 能通向 $\mathrm{Dep}^*(A)$ 中某点 $v$，
而 $v$ 又能通向 $A$ 中某点，故 $u$ 能通向 $A$ 中某点，即 $u\in\mathrm{Dep}^*(A)$。\\
闭集刻画：对取前驱封闭 $\Leftrightarrow$ 含某点则含其直接前驱，归纳得含全部祖先。
\end{proof}

\begin{property}[并与交的粗界]
对任意 $A,B\subseteq V$，
\[
\mathrm{Dep}^*(A\cup B)=\mathrm{Dep}^*(A)\cup\mathrm{Dep}^*(B),
\qquad
\mathrm{Dep}^*(A\cap B)\;\subseteq\;\mathrm{Dep}^*(A)\cap\mathrm{Dep}^*(B).
\]
右式一般\textbf{不能}改成等号（「交的闭包」可以严格小于「闭包的交」）。
\end{property}

\paragraph{证书、已证书项与缺项}
闭包回答「理论上需要谁」；工程上还要问「其中哪些已经允许引用、还缺哪些」。
为此先钉死本教材中的\textbf{证书}一词（此前第~2~章资产清单、愿景段已口语出现，此处才给形式含义）。

\begin{definition}[证书]
方法论中的\textbf{证书}不是公钥基础设施里的数字证书，而是对某个顶点 $v\in V$（能力/任务结点）的\textbf{可引用判决}：
在约定验收准则下已通过验证，因而允许被后续任务当作先决引用。
\begin{itemize}
  \item \textbf{主语}是顶点 $v$，不是 Agent、也不是操作者本人；
  \item \textbf{工程载体}可以是探针 PASS 记录、stable 交付与 KAT、技术总结中的闭合条件等
        （§2 资产清单「证书」列即此类证据形态）；
  \item \textbf{数学抽象}先只关心「$v$ 当前是否被承认为已获证」，细节状态机见迁移系统一节与第~\ref{sec:math}~章。
\end{itemize}
\end{definition}

\begin{definition}[已证书集与已证书项]
\label{def:gamma}
\textbf{工程术语}：\textbf{已获证能力集}（亦称已证书集）——当前允许被后续任务引用的能力集合。\\
\textbf{数学记号}：固定讨论时刻，令 $\Gamma\subseteq V$ 表示该集合。
对任意 $v\in V$：
\begin{itemize}
  \item 若 $v\in\Gamma$，称 $v$ \textbf{已获证}，并称 $v$ 为相对 $\Gamma$ 的一个\textbf{已证书项}；
  \item 若 $v\notin\Gamma$，称 $v$ \textbf{未获证}。
\end{itemize}
口语「已经拿到证书 / 已获得证书」即指 $v\in\Gamma$。
$\Gamma$ 随预研过程可变：新顶点验收通过后可写入 $\Gamma$；既有判决因上游变更不再被承认时可移出 $\Gamma$。
本小节先把 $\Gamma$ 当作给定的集合参数；改写 $\Gamma$ 的转移名称在缺项集定义之后预习，完整转移见后文。
此后正文若写 $\Gamma$，一律读作「当前已获证能力集」。
\end{definition}

\begin{definition}[缺项集]
\label{def:missing}
\textbf{工程术语}：相对已获证清单的\textbf{缺项}——依赖闭包里还不允许引用的那些先决。\\
\textbf{数学记号}：对目标集 $A\subseteq V$ 与已获证能力集 $\Gamma\subseteq V$，定义\textbf{缺项集}
\[
\mathrm{Missing}_\Gamma(A)
\;=\;
\mathrm{Dep}^*(A)\setminus\Gamma
\;=\;
\{\,u\in\mathrm{Dep}^*(A)\mid u\notin\Gamma\,\}.
\]
当 $A=\{T\}$ 时简记为 $\mathrm{Missing}_\Gamma(T)$。
直观：$u$ 落在「完成 $A$ 的依赖闭包」里，但当前还\textbf{未获证}，故不能直接引用。
此后正文若写 $\mathrm{Missing}_\Gamma(A)$，一律读作「相对 $\Gamma$ 的缺项」。
\end{definition}

\begin{remark}[三类转移名称的预习]
完整转移关系在「迁移系统」一节定义。此处先约定三个\textbf{工程动作}的名称，并预告后文数学记号：
\begin{center}
\begin{tabular}{@{}L{3.2cm}L{2.6cm}L{7.6cm}@{}}
  \toprule
  工程动作 & 后文记号 & 本小节所需的含义 \\
  \midrule
  写入证书 & $\mathsf{Certify}$ &
  对未获证顶点 $v$：验收通过后执行 $\Gamma\leftarrow\Gamma\cup\{v\}$ \\
  探针取证 & $\mathsf{Probe}$ &
  对缺项启动探针/预研，收集可供写入证书使用的证据（本身不自动改 $\Gamma$） \\
  脏标记（作废） & $\mathsf{Dirty}$ &
  撤销部分既有证书：使某些 $v$ 不再属于 $\Gamma$（证书库非单调，详见后文） \\
  \bottomrule
\end{tabular}
\end{center}
\end{remark}

\begin{definition}[闭包已满]
若 $\mathrm{Missing}_\Gamma(A)=\emptyset$，称目标集 $A$ 相对 $\Gamma$ \textbf{闭包已满}
（亦称「依赖已齐备」）。
此时 $\mathrm{Dep}^*(A)\subseteq\Gamma$：完成 $A$ 所需的全部先决都已是已证书项。
\end{definition}

\begin{proposition}[缺项的基本性质]
对任意 $A\subseteq V$ 与 $\Gamma,\Gamma'\subseteq V$：
\begin{enumerate}
  \item $\mathrm{Missing}_\Gamma(A)\subseteq\mathrm{Dep}^*(A)$；
  \item $\mathrm{Missing}_\Gamma(A)=\emptyset \iff \mathrm{Dep}^*(A)\subseteq\Gamma$；
  \item 若 $\Gamma\subseteq\Gamma'$，则 $\mathrm{Missing}_{\Gamma'}(A)\subseteq\mathrm{Missing}_\Gamma(A)$
        （已证书集只增时，缺项只减或不增）；
  \item $\mathrm{Dep}^*(A)$ 可划分为
        $\bigl(\mathrm{Dep}^*(A)\cap\Gamma\bigr)\;\dot{\cup}\;\mathrm{Missing}_\Gamma(A)$
        （已证书部分与缺项部分不相交，并起来恰为闭包）。
\end{enumerate}
\end{proposition}

\begin{definition}[闭包表（工作对象）]
称一次任务启动时填写、供核对的检查表为\textbf{闭包表}。
数学上它至少应能填写：目标集 $A$、当前已证书集 $\Gamma$、算出的 $\mathrm{Dep}^*(A)$，以及缺项集 $\mathrm{Missing}_\Gamma(A)$
（后文还可附加禁字、接缝声明等）。
「先交闭包表再写码」指：实现之前必须先交出可核对的上述四元组（及约定扩展栏）。
\end{definition}

\begin{definition}[门禁（工作含义）]
\textbf{门禁}指预研工作流里「允许或拒绝某步动作」的检查规则
（例如：缺项非空则禁止直接写顶层实现；引用未获证依赖则拒绝）。
其数学内核后文用语言成员判定与配置转移表述；本小节只需把它当作「对着闭包表做的硬检查」。
\end{definition}

\begin{remark}[闭包表与门禁如何联动]
若 $\mathrm{Missing}_\Gamma(A)\neq\emptyset$，门禁应阻止「直接写顶层实现」，
并要求先对缺项做探针取证（$\mathsf{Probe}$），再在验收通过后写入证书（$\mathsf{Certify}$），直至相对 $\Gamma$ 闭包已满（或明确缩小目标）。
\end{remark}

\begin{definition}[能力菜单与绿项]
\textbf{能力菜单}（简称\textbf{菜单}）指仓库中列出的可命名能力清单，典型来源是各活跃 \texttt{INDEX.md}。
菜单上出现的项称为\textbf{可见能力}；若某可见能力当前属于 $\Gamma$，口语可称其为\textbf{绿项}
（「绿」只表示「此刻已获证」，不是形式状态机字母）。
菜单提供构造 $\Gamma$ 的\textbf{证据来源候选}，本身既不等于 $\Gamma$，也不蕴含 $\mathrm{Missing}_\Gamma(T)=\emptyset$。
\end{definition}

\paragraph{生成集与基（为「最小补洞」作准备）}
$\mathrm{Dep}^*(T)$ 可能很大；有时只需指出一堆「种子点」，其闭包已能覆盖整个 $\mathrm{Dep}^*(T)$。
下面先定义生成集，再说明「补洞」指什么。

\begin{definition}[生成集]
设 $T\in V$。若 $B\subseteq V$ 满足
\[
\mathrm{Dep}^*(B)=\mathrm{Dep}^*(T),
\]
则称 $B$ 为 $\mathrm{Dep}^*(T)$ 的一个\textbf{生成集}
（亦称「$B$ 生成 $\mathrm{Dep}^*(T)$」）。
特别地，取 $B=\{T\}$ 时，由定义 $\mathrm{Dep}^*(\{T\})=\mathrm{Dep}^*(T)$，故 $\{T\}$ 总是生成集。
\end{definition}

\begin{definition}[极小生成集与基]
若 $B$ 是 $\mathrm{Dep}^*(T)$ 的生成集，且 $B$ 的任何真子集都不再是生成集，
则称 $B$ 为\textbf{极小生成集}。
在有限图上极小生成集一定存在（从任意生成集不断删点直至不能再删）。
本教材把「基」暂当作极小生成集的同义语；更细的唯一性/规范基问题留待门禁章。
\end{definition}

\begin{definition}[补洞与最小补洞]
相对已证书集 $\Gamma$，把缺项逐步变成已证书项、从而使目标闭包已满的过程，称为\textbf{补洞}。
形式上一轮补洞至少包含：选取某些缺项 $u\in\mathrm{Missing}_\Gamma(T)$，经探针取证后写入证书，得到更大的已获证集合 $\Gamma'$。
若优先寻找尽可能小的缺项生成集（定义见下）作为补证对象，则称该选择问题为\textbf{最小补洞}（算法细节后置）。
\end{definition}

\begin{definition}[相对 $\Gamma$ 的缺项生成集]
工程上更关心：在已经知道 $\Gamma$ 的前提下，还需要新获证的是哪些点。
若 $B\subseteq\mathrm{Missing}_\Gamma(T)$ 满足
\[
\mathrm{Dep}^*\bigl((\mathrm{Dep}^*(T)\cap\Gamma)\cup B\bigr)=\mathrm{Dep}^*(T),
\]
则称 $B$ 为相对 $\Gamma$ 的一个\textbf{缺项生成集}：
把「已证书部分」$\mathrm{Dep}^*(T)\cap\Gamma$ 与「还打算补证的 $B$」合在一起，闭包就能盖住整个 $\mathrm{Dep}^*(T)$。
最小补洞即在缺项生成集中寻找尽可能小者。
\end{definition}

\begin{property}[菜单、$\Gamma$ 与闭包]
由能力菜单的定义：菜单提供构造 $\Gamma$ 的证据来源候选，但
\[
\text{菜单上有许多绿项}
\quad\not\Rightarrow\quad
\mathrm{Missing}_\Gamma(T)=\emptyset.
\]
必须先算 $\mathrm{Dep}^*(T)$，再与当前 $\Gamma$ 做差得到缺项。
菜单回答「可能有哪些能力」；闭包与缺项回答「为了 $T$ 必须有谁、还缺谁」。
\end{property}

\begin{example}[纯数学：四点图上的闭包与缺项]
续 $V=\{1,2,3,4\}$，$E=\{(1,2),(1,3),(2,4),(3,4)\}$（依赖语义：箭头指向依赖者）。
则 $\mathrm{Pred}(4)=\{2,3\}$，
\[
D_0(4)=\{4\},\;
D_1(4)=\{4,2,3\},\;
D_2(4)=\{4,2,3,1\}=\mathrm{Dep}^*(4).
\]
取目标集 $A=\{4\}$。若 $\Gamma=\{1,2,4\}$，则
\[
\mathrm{Dep}^*(4)\cap\Gamma=\{1,2,4\},
\qquad
\mathrm{Missing}_\Gamma(4)=\{3\}.
\]
此时 $\{3\}$ 是一个缺项生成集：补证 $3$ 之后闭包已满。
又 $\mathrm{Dep}^*(\{2,3\})=\mathrm{Dep}^*(4)$，故 $\{2,3\}$ 是 $\mathrm{Dep}^*(4)$ 的一个生成集
（且为极小：去掉 $2$ 或 $3$ 任一，闭包都到不了 $4$ 的全部祖先）。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=1.7cm, y=1.25cm,
  every node/.style={font=\small},
  c/.style={draw, circle, minimum size=0.62cm},
  arr/.style={-{Stealth[length=1.8mm]}, thick}
]
  \node[font=\footnotesize] at (0,1.55) {依赖图 $G$};
  \node[c] (1) at (0,1) {$1$};
  \node[c] (2) at (-0.7,0) {$2$};
  \node[c] (3) at (0.7,0) {$3$};
  \node[c] (4) at (0,-1) {$4$};
  \draw[arr] (1) -- (2); \draw[arr] (1) -- (3);
  \draw[arr] (2) -- (4); \draw[arr] (3) -- (4);

  \begin{scope}[xshift=3.6cm]
  \node[font=\footnotesize] at (0,1.55) {$\mathrm{Dep}^*(4)$ 与缺项};
  \node[c, fill=blue!15] (1b) at (0,1) {$1$};
  \node[c, fill=blue!15, densely dashed] (2b) at (-0.7,0) {$2$};
  \node[c, fill=orange!25] (3b) at (0.7,0) {$3$};
  \node[c, fill=blue!15, densely dashed] (4b) at (0,-1) {$4$};
  \draw[arr] (1b) -- (2b); \draw[arr] (1b) -- (3b);
  \draw[arr] (2b) -- (4b); \draw[arr] (3b) -- (4b);
  \node[font=\footnotesize, red!70!black] at (0.7,-0.55) {缺};
  \node[font=\footnotesize, anchor=west, align=left] at (1.3,0.2) {虚线边框$\in\Gamma$\\橙底$=$缺项};
  \end{scope}
\end{tikzpicture}
\caption{示意配图 9}
\label{fig:auto-09}
\end{figure}
\end{example}

\begin{example}[工程：KEM.KeyGen 的依赖闭包示意]
取依赖语义下的示意 DAG（结点名为能力，非数据）：
边表示「箭头终点依赖箭头起点」。
令 $T=\mathrm{KEM.KG}$。则
\[
\mathrm{Dep}^*(T)=\{\mathrm{P0},\mathrm{P1},\mathrm{P2},\mathrm{Tail},\mathrm{KEM.KG}\}.
\]
若当前 $\Gamma$ 已含 P0--P2，但尾缝仍未获证（$\mathrm{Tail}\notin\Gamma$），则
$\mathrm{Missing}_\Gamma(T)=\{\mathrm{Tail}\}$，目标相对 $\Gamma$ \textbf{未}闭包已满。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=2.15cm, y=1.15cm,
  every node/.style={font=\footnotesize, align=center},
  box/.style={draw, rounded corners=2pt, minimum width=1.85cm, minimum height=0.7cm},
  arr/.style={-{Stealth[length=1.8mm]}, thick}
]
  \node[box, fill=blue!8] (p0) at (0,2) {P0 平台};
  \node[box, fill=blue!8] (p1) at (0,1) {P1 原语};
  \node[box, fill=blue!12] (p2) at (0,0) {P2 PKE};
  \node[box, fill=orange!15] (tail) at (1.35,0) {Alg.16 尾缝};
  \node[box, fill=orange!20] (kem) at (0.7,-1) {P3 KEM.KG};
  \draw[arr] (p0) -- (p1);
  \draw[arr] (p1) -- (p2);
  \draw[arr] (p2) -- (kem);
  \draw[arr] (p1) -- (tail);
  \draw[arr] (tail) -- (kem);
  \node[anchor=west, align=left] at (2.3,0.6) {
    $\mathrm{Dep}^*(T)$ 含全部结点\\[2pt]
    若 $\Gamma\supset\{\mathrm{P0},\mathrm{P1},\mathrm{P2}\}$\\
    但 $\mathrm{Tail}\notin\Gamma$，则\\
    $\mathrm{Missing}_\Gamma(T)=\{\mathrm{Tail}\}$
  };
\end{tikzpicture}
\caption{配图：工程：KEM.KeyGen 的依赖闭包示意}
\label{fig:auto-10}
\end{figure}
§2 资产清单 $\approx$ 当时对依赖闭包 $\mathrm{Dep}^*(T)$ 与已获证清单 $\Gamma$ 的手工对照；
\textbf{未}显式计算缺项 $\mathrm{Missing}_\Gamma(T)$ 就开工，正是「菜单$\neq$说明书」的来源。
\end{example}


\subsection{形式语言与合法拼装}
\label{sec:tool-lang}

本大节专门写第二件数学工具：\textbf{形式语言}。
目标一句话：把「怎样拼积木」写成可检查的\textbf{合法拼装计划}（字是否属于语言、是否避开禁止拼法）。
工程上它回答：菜单绿不等于拼法合法；单点 PASS 推不出接缝合法。
工程落点留到后文「工程体现」。

\subsubsection{字母表、字与语言}
\label{sec:words-lang}
\begin{definition}[字母表与字]
\label{def:alphabet-word}
\textbf{工程术语}：\textbf{可用步骤}组成的字母表；一次\textbf{拼装计划}是步骤的有限序列。\\
\textbf{数学记号}：非空有限集 $\Sigma$ 称为\textbf{字母表}；其元素称为\textbf{字母}。
$\Sigma$ 上的\textbf{字}是有限序列 $w=a_1a_2\cdots a_n$（$n\ge 0$，$a_i\in\Sigma$）；
$n=0$ 时称为\textbf{空字}，记 $\varepsilon$。
称 $n=|w|$ 为字长。全体字之集记 $\Sigma^*$（Kleene 星）。
又记 $\Sigma^+=\Sigma^*\setminus\{\varepsilon\}$。
在本方法论中：字母 $\approx$ 已证能力或接缝动作；字 $w$ $\approx$ 某次拼装计划。
\end{definition}

\begin{definition}[前缀、后缀与因子]
设 $w,x\in\Sigma^*$。
\begin{itemize}
  \item 若存在 $y$ 使 $w=xy$，称 $x$ 为 $w$ 的\textbf{前缀}；
  \item 若存在 $y$ 使 $w=yx$，称 $x$ 为 $w$ 的\textbf{后缀}；
  \item 若存在 $y,z$ 使 $w=yxz$，称 $x$ 为 $w$ 的\textbf{因子}（子字）。
\end{itemize}
\end{definition}

\begin{definition}[语言]
\label{def:language}
\textbf{工程术语}：\textbf{合法计划集合}——门禁承认的全部拼装计划。\\
\textbf{数学记号}：若 $L\subseteq\Sigma^*$，则称 $L$ 为 $\Sigma$ 上的一个\textbf{语言}。
称 $w$ 对 $L$ \textbf{合法}（或 $w$ 属于 $L$），若 $w\in L$。
此后若写 $w\in L$，一律读作「拼装计划 $w$ 合法」。
\end{definition}

\begin{definition}[语言的连接与星闭包]
对 $x,y\in\Sigma^*$，\textbf{连接} $xy$ 表示把 $y$ 接在 $x$ 后。
对语言 $L,M\subseteq\Sigma^*$，定义
\[
LM=\{xy\mid x\in L,\,y\in M\},
\quad
L^{0}=\{\varepsilon\},\;
L^{k+1}=L^{k}L,\;
L^{*}=\bigcup_{k\ge 0}L^{k}.
\]
\end{definition}

\begin{property}[连接对合法性不必封闭]
一般地，$x\in L$ 且 $y\in L$ \textbf{不能}推出 $xy\in L$。
（这是后文「组合不免费」的纯语言版本。）
\end{property}

\begin{proposition}[$\Sigma^*$ 的通用性]
任一语言都是 $\Sigma^*$ 的子集；讨论「合法计划」即指定某个 $L\subseteq\Sigma^*$。
门禁的数学内核是：给定 $w$，判定 $w\in L$ 是否成立（成员判定，见后文）。
\end{proposition}

\begin{example}[纯数学：偶数个 $0$ 的语言（附图）]
取 $\Sigma=\{0,1\}$，
$L_{\mathrm{even0}}=\{w\in\Sigma^*\mid w\text{ 中 }0\text{ 的个数为偶数}\}$。
下图用两状态自动机识别该语言（$q_0$ 接受）：读 $0$ 翻转奇偶，读 $1$ 不动。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  every node/.style={font=\small},
  s/.style={draw, circle, minimum size=0.85cm},
  arr/.style={-{Stealth[length=2mm]}, thick}
]
  \node[s, double, fill=green!10] (q0) at (0,0) {$q_0$};
  \node[s] (q1) at (3.2,0) {$q_1$};
  \draw[arr] (q0) edge[bend left=20] node[above] {$0$} (q1);
  \draw[arr] (q1) edge[bend left=20] node[below] {$0$} (q0);
  \draw[arr] (q0) edge[loop left] node[left] {$1$} (q0);
  \draw[arr] (q1) edge[loop right] node[right] {$1$} (q1);
  \node[font=\footnotesize, anchor=west] at (4.3,0.3) {$1001\in L$（偶）};
  \node[font=\footnotesize, anchor=west] at (4.3,-0.3) {$10\notin L$（奇）};
\end{tikzpicture}
\caption{配图：纯数学：偶数个 $0$ 的语言（附图）}
\label{fig:auto-13}
\end{figure}
注意：$0\notin L$ 但 $00\in L$，故不能把「每个字母合法」误当成「连接合法」。
\end{example}

\begin{example}[工程：实现计划当作字（附图）]
将原子动作编码为字母，例如
$a=\texttt{RunPKE}$，$b=\texttt{Alg16Tail}$，$c=\texttt{WriteIO}$。
合法短计划 $w=abc$ 画成动作序列；插入未证书动作 $x$ 得 $abxc\notin L$。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  every node/.style={font=\footnotesize, align=center},
  box/.style={draw, rounded corners=2pt, minimum width=1.55cm, minimum height=0.65cm},
  arr/.style={-{Stealth[length=1.8mm]}, thick}
]
  \node[box, fill=green!10] (a) at (0,0.8) {$a$\\PKE};
  \node[box, fill=green!10] (b) at (2.2,0.8) {$b$\\尾缝};
  \node[box, fill=green!10] (c) at (4.4,0.8) {$c$\\写 I/O};
  \draw[arr] (a) -- (b) -- (c);
  \node[anchor=west] at (5.6,0.8) {$w=abc\in L$（示意）};

  \node[box, fill=green!10] (a2) at (0,-0.9) {$a$};
  \node[box, fill=green!10] (b2) at (1.8,-0.9) {$b$};
  \node[box, fill=red!15] (x2) at (3.6,-0.9) {$x$\\未证书};
  \node[box, fill=green!10] (c2) at (5.4,-0.9) {$c$};
  \draw[arr] (a2) -- (b2) -- (x2) -- (c2);
  \node[anchor=west] at (6.5,-0.9) {$abxc\notin L$};
\end{tikzpicture}
\caption{配图：工程：实现计划当作字（附图）}
\label{fig:auto-14}
\end{figure}
\end{example}

\subsubsection{文法、产生式、派生与派生树}
\label{sec:grammar}

\begin{definition}[上下文无关文法（本教材用法）]
\textbf{上下文无关文法}是四元组 $\mathcal{G}=(N,\Sigma,P,S)$，其中：
\begin{itemize}
  \item $N$ 为有限\textbf{非终结符}集；
  \item $\Sigma$ 为有限\textbf{终结符}集，$N\cap\Sigma=\emptyset$；
  \item $S\in N$ 为\textbf{开始符号}；
  \item $P$ 为有限\textbf{产生式}集，每个产生式形如 $A\to\alpha$，其中 $A\in N$，$\alpha\in(N\cup\Sigma)^*$。
\end{itemize}
\end{definition}

\begin{definition}[一步派生与多步派生]
若 $\alpha=\alpha_1 A \alpha_2$，$A\to\beta\in P$，则称 $\alpha$ \textbf{一步派生}出 $\alpha_1\beta\alpha_2$，记 $\alpha\Rightarrow\alpha_1\beta\alpha_2$。
$\Rightarrow^*$ 表示 $\Rightarrow$ 的自反传递闭包（零步或多步派生）。
\end{definition}

\begin{definition}[文法生成的语言]
\[
L(\mathcal{G}) \;=\; \{\,w\in\Sigma^*\mid S\Rightarrow^* w\,\}.
\]
\end{definition}

\begin{definition}[派生树]
对一次从 $S$ 到 $w\in\Sigma^*$ 的派生，其\textbf{派生树}（语法树）是满足以下条件的有根有序树：
根标记为 $S$；内部结点标记为非终结符；叶子从左到右连接恰为 $w$；
若内部结点标记 $A$ 且其孩子标记依次拼成 $\alpha$，则 $A\to\alpha\in P$。
\end{definition}

\begin{theorem}[派生与派生树]
对上下文无关文法，$\mathcal{G}$ 能派生出 $w$ 当且仅当存在以 $w$ 为叶子连接的派生树。
\end{theorem}

\begin{remark}[方法论中的对应]
非终结符 $\approx$ 尚未落到已证书原子步骤的目标；
终结符 $\approx$ 已证书调用或不可再分的合法动作；
产生式 $\approx$ 「目标可展开为哪些子目标 / 接缝」的改写规则。
\end{remark}

\begin{example}[纯数学：匹配括号的文法片段]
令 $N=\{S\}$，$\Sigma=\{(,)\}$，产生式 $S\to(S)\mid SS\mid\varepsilon$。
则 $()\in L(\mathcal{G})$，$()()\in L(\mathcal{G})$，而 $()\notin$ 的补——更精确地说 $)(\notin L(\mathcal{G})$。
派生树记录括号如何嵌套，而不仅仅是字母的前后顺序。
\end{example}

\begin{example}[工程：KEM.KeyGen 的改写]
直觉产生式（符号仅作说明）：
\[
\texttt{KEM.KeyGen}
\;\to\;
\texttt{PKE.KeyGen}\;\cdot\;\texttt{Alg16Tail}.
\]
若 $\texttt{PKE.KeyGen}$ 已是终结的已证书调用，而 $\texttt{Alg16Tail}$ 仍是非终结接缝，
则派生树根为 KEM.KeyGen，一侧叶子为 PKE，另一侧继续展开尾缝直至获证动作。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  every node/.style={font=\small, align=center},
  arr/.style={-{Stealth[length=2mm]}}
]
  \node (T) {KEM.KeyGen};
  \node[below left=7mm and 7mm of T] (P) {PKE.KeyGen};
  \node[below right=7mm and 7mm of T] (S) {Alg16Tail};
  \draw[arr] (T) -- (P);
  \draw[arr] (T) -- (S);
\end{tikzpicture}
\caption{配图：工程：KEM.KeyGen 的改写}
\label{fig:auto-15}
\end{figure}
\end{example}

\subsubsection{成员判定、禁字与组合不免费}
\label{sec:membership}

\begin{definition}[成员判定问题]
\textbf{工程术语}：检查一份拼装计划是否合法（门禁里的成员检查）。\\
\textbf{数学记号}：给定语言 $L\subseteq\Sigma^*$ 与字 $w\in\Sigma^*$，判断是否 $w\in L$，称为 $L$ 的\textbf{成员判定}。
\end{definition}

\begin{definition}[禁字集]
\label{def:forb}
\textbf{工程术语}：\textbf{禁止拼法}——不得出现在合法计划中的子串或关闭路线。\\
\textbf{数学记号}：设 $\mathrm{Forb}\subseteq\Sigma^*$。称语言 $L$ \textbf{避开} $\mathrm{Forb}$，若
\[
L \;\subseteq\; \{w\in\Sigma^*\mid \text{不存在 }f\in\mathrm{Forb}\text{ 作为 }w\text{ 的因子}\}.
\]
其中「因子」指存在 $x,y$ 使 $w=xfy$。
此后若写 $\mathrm{Forb}$，一律读作「禁止拼法集合」（工程来源含 \texttt{frozen/} 判决等）。
\end{definition}

\begin{definition}[组合不免费（方法论公理图式）]
称语言 $L$ 满足\textbf{组合不免费}，若存在 $x,y\in L$ 使得 $xy\notin L$。
在预研建模中我们\textbf{不假定} $L$ 对连接封闭；
若工程上需要把 $x$ 与 $y$ 顺序拼装，通常引入新的非终结符（接缝）与产生式，并要求该接缝获证。
\end{definition}

\begin{proposition}[菜单不能代替成员判定]
设 $\Sigma_0\subseteq\Sigma$ 为「菜单上可见的字母」。
仅给出 $\Sigma_0$ 不能唯一确定 $L$。
尤其，$L$ 还依赖产生式集合与禁字集 $\mathrm{Forb}$。
\end{proposition}

\begin{example}[纯数学]
取 $L=\{a,b\}\subseteq\{a,b\}^*$。则 $a,b\in L$ 但 $ab\notin L$，故组合不免费。
若希望允许 $ab$，必须把 $ab$ 显式加入 $L$，或增加产生式（例如 $S\to ab$）。
\end{example}

\begin{example}[工程]
PKE.KeyGen 计划与「某段哈希拼接」各自可能合法，
但「在新的 2-launch 边界下拼接二者」不必自动合法——这正是接缝节点
（如 Alg.16 尾缝、\texttt{KemKgTailFused}）存在的理由。
\texttt{frozen/} 文书对应 $\mathrm{Forb}$ 的工程来源之一。
\end{example}

\begin{remark}[门禁雏形]
Agent 提交「闭包表 + 展开计划 $w$」时，门禁检查的是：
$\mathrm{Missing}_\Gamma(T)=\emptyset$（全依赖已覆盖）且存在派生表明 $w\in L(\mathcal{G})$，
并且 $w$ 避开 $\mathrm{Forb}$。这是「先交闭包表，再写码」的数学表述。
\end{remark}


\subsection{作业过程自动机}
\label{sec:tool-auto}
\label{sec:transition}

本大节专门写第三件数学工具：\textbf{自动机}；在本方法论中的专名是\textbf{作业过程自动机}（亦称门禁自动机）。
目标一句话：描述一次作业如何按合法转移走到接受，以及证书何时因脏标记作废。
它接管「进度」：状态怎么变、何时接受/拒绝。
工程落点留到后文「工程体现」。

\subsubsection{迁移系统、配置与接受}
\begin{definition}[迁移系统]
\textbf{迁移系统}是三元组 $\mathcal{T}=(Q,\to,Q_0)$，其中 $Q$ 为配置集，
$\to\subseteq Q\times Q$ 为转移关系，$Q_0\subseteq Q$ 为初始配置集。
若 $(q,q')\in{\to}$，记 $q\to q'$。
它是定义自动机时常用的底料；下一款定义把它特化成作业过程自动机。
\end{definition}

\begin{definition}[作业过程自动机]
\label{def:job-process-automaton}
\textbf{数学工具名}：\textbf{自动机}。\\
\textbf{本方法论中的专名}：\textbf{作业过程自动机}（门禁自动机）。\\
固定能力库顶点集与约束库后，作业过程自动机是一个迁移系统，其
\begin{itemize}
  \item \textbf{状态}取为预研配置 $q=(\Gamma,F,S)$（定义见下）；
  \item \textbf{转移}取为展开 / 探针取证 / 写入证书 / 声明接缝 / 脏标记 / 冻结路线等合法动作；
  \item \textbf{初始状态}由开任务时的已获证清单与前沿给出；
  \item \textbf{接受}见后文「接受配置」。
\end{itemize}
此后若只说「自动机」，在本章默认指作业过程自动机。
\end{definition}

\begin{definition}[预研配置（自动机的状态）]
\label{def:config}
\textbf{工程术语}：一次作业的\textbf{预研配置}——记下已获证能力、前沿还欠什么、以及约束。\\
\textbf{数学记号}：预研配置是三元组 $q=(\Gamma,F,S)$，其中
\begin{itemize}
  \item $\Gamma\subseteq V_{\mathrm{lib}}$ 为已获证能力集；
  \item $F$ 为前沿目标的有限多重集（待展开或待获证的目标）；
  \item $S$ 为约束库（含 I/O 契约、接缝声明、禁止拼法等）。
\end{itemize}
\end{definition}

\begin{definition}[基本转移（名称可修订）]
在约束 $S$ 允许的前提下，定义以下几类转移（均为 $\to$ 的子集）。
表中先给\textbf{工程动作名}，再给后文沿用的数学记号：
\begin{center}
\begin{tabular}{@{}L{3.0cm}L{2.6cm}L{8.0cm}@{}}
  \toprule
  工程动作 & 记号 & 作用 \\
  \midrule
  展开 & $\mathsf{Expand}$ & 按产生式展开 $F$ 中某目标，更新缺项 \\
  探针取证 & $\mathsf{Probe}$ & 对缺项启动探针/预研以寻求证据 \\
  写入证书 & $\mathsf{Certify}$ & 验收通过后将结点写入 $\Gamma$ \\
  声明接缝 & $\mathsf{Compose}$ & 声明并认证接缝（落实组合不免费） \\
  脏标记 & $\mathsf{Dirty}$ & 使部分证书失效（见下一节） \\
  冻结路线 & $\mathsf{Freeze}$ & 将某路线写入禁止拼法 / 否定约束 \\
  \bottomrule
\end{tabular}
\end{center}
\end{definition}

\begin{definition}[接受与拒绝]
固定目标 $T$ 与生产 I/O 契约 $\mathcal{C}$。
\begin{itemize}
  \item 配置 $q=(\Gamma,F,S)$ 称为\textbf{接受配置}，若 $T\in\Gamma$、$F$ 为空（或仅含已放电目标），且 $\mathcal{C}$ 满足；
  \item 若转移过程试图使用 $\mathrm{Forb}$ 中的因子、或引用 $\notin\Gamma$ 的依赖边且未先 $\mathsf{Probe}/\mathsf{Certify}$，
        则进入\textbf{拒绝}（具体可建模为沉入拒绝阱状态）。
\end{itemize}
\end{definition}

\begin{definition}[合法性与幻觉（工作定义）]
\begin{enumerate}
  \item 称一次作业过程\textbf{合法}，若它对应作业过程自动机中从某初始配置到接受配置的有限转移序列，
        且每一步都符合产生式与 $S$ 中约束。
  \item 称行为构成\textbf{幻觉}，若它执行了非法转移，或在图上使用了无证书边
        （等价地：计划字 $w\notin L$ 或未避开 $\mathrm{Forb}$）。
\end{enumerate}
语义 oracle（FIPS / liboqs / golden）判定「计算结果是否正确」；
本定义判定「砖是否允许那样砌」。
\end{definition}

\begin{example}[纯数学：作业过程自动机小品]
状态 $\{q_0,q_{\mathrm{ok}},q_{\mathrm{bad}}\}$，字母 $\{a,b\}$。
$q_0\xrightarrow{a}q_0$，$q_0\xrightarrow{b}q_{\mathrm{ok}}$，其余进 $q_{\mathrm{bad}}$。
则字 $aab$ 被接受，$ba$ 被拒绝。这与「先合法前缀、再结束符」的门禁同构。
\end{example}

\begin{example}[工程]
初始已获证清单含 stable PKE 等；前沿含 \texttt{kem.keygen}。
经\textbf{展开}得到尾缝缺项 $\to$ \textbf{声明接缝/探针取证} $\to$ \textbf{写入证书}后目标获证，到达接受。
若中途使用了\textbf{禁止拼法}中的因子（已关闭或明确禁止的路线），
则触发拒绝（\textbf{冻结路线}已写入禁止拼法集合）。
\end{example}

\subsubsection{证书的非单调更新（脏标记）}
\label{sec:dirty}

\begin{definition}[单调与非单调证书库]
称证书更新机制为\textbf{单调}的，若任意转移满足 $\Gamma\subseteq\Gamma'$（证书集只增不减）。
若允许某次转移后 $\Gamma'\subsetneq\Gamma$，则称为\textbf{非单调}的。
\end{definition}

\begin{definition}[脏标记]
\label{def:dirty}
\textbf{工程术语}：\textbf{脏标记}（证书作废）——因上游约定变更，下游既有证书不再被承认。\\
\textbf{数学记号}：对 $v\in\Gamma$，若因上游接口、布局或同步约定变更，使 $v$ 的既有证书不再被承认，
则称 $v$ 被标记为 $\mathsf{dirty}$，并在转移 $\mathsf{Dirty}$ 下将 $v$ 移出 $\Gamma$
（或移入「待重证」区）。具体存储格式后文工程化。
此后若写 $\mathsf{Dirty}$，一律读作「脏标记 / 证书作废」动作。
\end{definition}

\begin{proposition}[工程引理库非单调]
本方法论采用的证书库是非单调的：存在合法的 $\mathsf{Dirty}$ 转移使 $\Gamma$ 严格缩小。
\end{proposition}

\begin{corollary}[过期证书不可当作永真公理]
门禁在引用 $v\in\Gamma$ 时，须能够回答「自获证以来上游是否触发 $\mathsf{Dirty}$」。
\end{corollary}

\begin{example}[纯数学：撤回引理]
证明系统中若发现引理 $L_1$ 的证明有漏洞而撤回，则所有依赖 $L_1$ 的定理证书失效。
这与「断言永真后只增不减」的理想图景不同。
\end{example}

\begin{example}[工程]
P1 某能力的 I/O、布局或同步约定变更后，曾基于旧约定的 P2/P3 证书应标 $\mathsf{dirty}$，
不得因「上次 PASS」而默认继续合法。
\end{example}


\subsection{工程体现：三件工具在仓库里落成什么}
\label{sec:mathbg-eng}

数学写完之后，把三件工具各自在仓库里的落点收在一处，便于对照，也避免「每讲完一小段数学就插工程」打断主线。

\subsubsection{依赖图与依赖闭包 $\to$ 闭包表、资产清单与 \texttt{frozen/}}
\label{sec:tool-dep-eng}

依赖图与依赖闭包在工程里主要落成三样东西（第~\ref{sec:hook}~章已见过雏形）：
\begin{enumerate}
  \item \textbf{资产清单}：当时认为「已获证」的能力列表（$\Gamma$ 的手工版）；
  \item \textbf{闭包表}：目标、已获证、依赖闭包、缺项四栏核对——动手前先交这张表；
  \item \textbf{\texttt{frozen/}}：明确不再允许当作依赖去走的边（禁止再引用的路线）。
\end{enumerate}
没有依赖图与闭包，就会把「INDEX 上有绿项」误当成「依赖已齐、可以开写」。

\subsubsection{形式语言 $\to$ 拼装计划、门禁与禁止拼法}
\label{sec:tool-lang-eng}

形式语言在工程里主要落成：
\begin{enumerate}
  \item \textbf{拼装计划}：一次任务打算按什么顺序、用哪些已证能力与接缝去拼；
  \item \textbf{门禁的成员检查}：这份计划是否属于合法计划集合，是否避开禁止拼法；
  \item \textbf{禁止拼法}：\texttt{frozen/} 等关闭路线的语言侧对应物。
\end{enumerate}
没有形式语言，就会把「菜单上有这些绿项」误当成「任意拼接都合法」。


\subsubsection{作业过程自动机 $\to$ 作业怎么走完、证书何时作废}
\label{sec:tool-auto-eng}

作业过程自动机在工程里主要落成：
\begin{enumerate}
  \item \textbf{作业轨迹}：从开任务配置走到「目标获证、前沿清空」的接受配置；
  \item \textbf{过程动作}：展开缺项、探针取证、写入证书、声明接缝；
  \item \textbf{脏标记}：上游 I/O、布局或同步约定变更后，下游不得继续假绿。
\end{enumerate}
没有作业过程自动机，就只能事后说「这次碰巧 PASS」，无法描述过程是否合法、证书为何作废。

\subsection{回扣第~\ref{sec:hook}~章：流程如何对应本章数学}
\label{sec:mathbg-hookback}

\paragraph{本段目标}
对照图~\ref{fig:ch3-spine}：\textbf{不再引入新定义}，按三件工具把第~\ref{sec:hook}~章 KeyGen 流程对照回去。
学完应能指着故事说清：哪一段是依赖图与闭包（还缺谁），哪一段是形式语言（拼装是否合法），哪一段是作业过程自动机（怎么走完 / 脏标记）。

第~\ref{sec:hook}~章讲的是\textbf{已经走过的}工程故事；本章给的是\textbf{用来把故事说清楚的一套语言}。
本节\textbf{不再引入新定义}，只做对照。
中间隔了不少页，第~2~章那些表和图容易对不上号。
所以下面把最关键的几张\textbf{按本章已挂钩对象重画一遍}
（已获证清单、依赖闭包、缺项、接缝、预研配置等；需要时括号附记号），
让读者一眼看出：挂上数学记号之后，原先那张工程图长什么样。
第~2~章里的原表、原图仍保留备查：表~\ref{tab:ch2-assets}、图~\ref{fig:ch2-explore}、图~\ref{fig:ch2-fips-dataflow}、表~\ref{tab:ch2-launch}。

\paragraph{总对照表（先扫一眼）}
\begin{table}[H]
\centering\small
\caption{第~2~章里的流程 / 物件，与本章对象的总对照（工程词为主）}
\label{tab:ch312-overview}
\begin{tabular}{@{}L{5.0cm}L{8.8cm}@{}}
  \toprule
  第~\ref{sec:hook}~章里的流程 / 物件 & 本章里对应什么（节号凭直觉即可） \\
  \midrule
  表~\ref{tab:ch2-assets} 资产清单里的「绿积木」 & 已获证能力集 $\Gamma$；顶点已经拿到证书（§3.5） \\
  活跃 INDEX 上的探针列表 & 能力菜单（可用步骤的线索）；\textbf{不是}合法计划集合，也\textbf{不必}等于已获证清单 \\
  图~\ref{fig:ch2-explore}：P0/P1$\to$分段$\to$stable PKE & 能力依赖图上的生长路径；拓扑序（§3.4） \\
  「做 KEM 前心里大概有张清单」 & 手工把依赖闭包与已获证清单对了一遍；缺项当时并没有显式算出来 \\
  图~\ref{fig:ch2-fips-dataflow}：19 调 16 调 13 $+$ 数据 & \textbf{调用嵌套与数据}依赖；要和能力依赖图、派生树\textbf{分开看}（§3.6） \\
  Alg.16 尾缝、2-launch 接缝、拼 $\mathit{dk}$ & 接缝 / 声明接缝；组合并不免费（§3.9） \\
  correctness 那条高成本探索路 & 手里只有菜单和伪代码；缺「拼装计划是否合法」的成员检查（§3.9） \\
  device 去掉 vendor $\to$ 再晋级 stable & 探针取证 / 写入证书；预研配置轨迹一路走到接受（§3.10） \\
  「上次 PASS 了，这次仍可能翻车」 & 证书可以往回缩；脏标记（§3.11） \\
  \bottomrule
\end{tabular}
\end{table}

\paragraph{流程一：资产清单的「数学完全体」}
表~\ref{tab:ch2-assets}在第~2~章只回答两件事：手里有什么、证书长什么样。
表~\ref{tab:ch312-assets-math}把它重画成开任务那一刻、相对目标 $T=\texttt{kem.keygen.k4}$ 的闭包对照，
多加了三列：「是否已获证？」「是否落在依赖闭包里？」「扮演什么角色」。
\begin{table}[H]
\centering\footnotesize
\caption{资产清单的数学完全体（相对 $T=\texttt{kem.keygen.k4}$ 的开任务快照）}
\label{tab:ch312-assets-math}
\begin{tabular}{@{}c L{3.4cm} c c L{4.2cm}@{}}
  \toprule
  层 & 能力（简述） & 已获证？ & 在依赖闭包？ & 角色（本章用语） \\
  \midrule
  P0 & DataCopy / MMAD / TPipe 等 & 是 & 是 & 已获证的祖先 \\
  P1 & NTT / SampleNTT / CBD / \ldots & 是 & 是 & 已获证的祖先 \\
  P2 & Alg.13 分段与 device & 是 & 是 & 已获证的祖先 \\
  P2 & \textbf{stable PKE KeyGen} & \textbf{是} & \textbf{是} & 直接先决（已获证 $\cap$ 依赖闭包） \\
  --- & \textbf{Alg.16 尾缝 / 2-launch 接缝} & \textbf{否} & \textbf{是} & \textbf{缺项} \\
  P3 & KEM KeyGen correctness & （标本） & --- & 语义对照用；\textbf{不算}合法派生的证据 \\
  P3 & KEM KeyGen device / stable & 开任务时否 & 是（目标侧） & 还等着写入证书的目标 \\
  \bottomrule
\end{tabular}\\[4pt]
{\footnotesize
开任务时可以粗略写成：已获证清单已覆盖 P0/P1/P2，
但缺项仍含尾缝，所以依赖闭包\textbf{还没齐}。
INDEX 菜单上标绿的项，只是构造已获证清单时可以去翻的证据来源，\textbf{代替不了}本表。
}
\end{table}

由此可以看清两件事：
\begin{itemize}
  \item 「站在 stable PKE 上」应写成「该能力已获证」，不能只看目录名里有没有 \texttt{stable}；
  \item 菜单、已获证清单、缺项是三样东西，谁也替不了谁（这正是第一组问题的落点）。
\end{itemize}

\paragraph{流程二：探路图的「数学完全体」}
图~\ref{fig:ch2-explore}画的是仓库时间线。
图~\ref{fig:ch312-explore-math}沿用同一骨架，标出拓扑序方向、哪些点已获证、哪里是缺项，
以及 correctness 那条旁路（沿着它走，$\notin L$）。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=2.05cm, y=1.15cm,
  box/.style={draw, rounded corners=2pt, align=center, font=\scriptsize,
              minimum width=2.15cm, minimum height=0.78cm},
  arr/.style={-{Stealth[length=1.8mm]}, thick},
  note/.style={font=\footnotesize, align=left}
]
  \node[box, fill=blue!12] (v2) at (0,1.2) {P1 NTT\\$\in\Gamma$};
  \node[box, fill=blue!12] (ahat) at (1.15,1.2) {$\hat{A}$ 探针\\$\in\Gamma$};
  \node[box, fill=blue!12] (se) at (2.3,1.2) {$s,e$ 探针\\$\in\Gamma$};
  \node[box, fill=blue!18] (pke) at (3.5,1.2) {P2 PKE\\$\in\Gamma$};
  \draw[arr] (v2) -- (ahat) -- (se) -- (pke);
  \node[note, blue!60!black] at (1.75,1.95) {拓扑序 $\longrightarrow$（先底层，后上层）};

  \node[box, fill=orange!35, densely dashed] (tail) at (3.5,0) {Alg.16 尾缝\\$\notin\Gamma$（缺项）};
  \node[box, fill=orange!20] (kem) at (3.5,-1.1) {P3 KEM.KG\\目标 $T$};
  \draw[arr] (pke) -- (tail);
  \draw[arr] (tail) -- (kem);
  \node[note, red!60!black] at (4.85,0) {$\mathrm{Missing}_\Gamma(T)$};

  \node[box, fill=gray!15] (corr) at (1.4,-1.1) {correctness\\标本};
  \draw[arr, densely dotted] (pke) -- (corr);
  \draw[arr, densely dotted] (corr) -- node[below, font=\scriptsize]{oracle$\neq L$} (kem);

  \node[note] at (0.2,-2.15) {
    实线边 $\approx$ 能力 DAG 上可以引用的依赖 / 接缝；\\
    虚线框 $=$ 开任务时还没拿到证书的缺项；灰框 $=$ 语义对照，不当合法派生的模板。
  };
\end{tikzpicture}
\caption{工程探路图的数学完全体（$\Gamma$、拓扑序、缺项、oracle 旁路）}
\label{fig:ch312-explore-math}
\end{figure}

\begin{itemize}
  \item 蓝底结点：开任务时已经在 $\Gamma$ 里；探路顺序和拓扑序一致；
  \item 虚线橙框里的尾缝：落在 $\mathrm{Dep}^*(T)$ 里，却不在 $\Gamma$ 里，所以是缺项；
  \item correctness 灰框：可以拿来当字节对照，但\textbf{不会}自动推出 $w\in L$。
\end{itemize}

\paragraph{流程三：目标、闭包与缺项（对着完全体表直接读）}
一旦选定 $T=\texttt{kem.keygen.k4}$，表~\ref{tab:ch312-assets-math}和图~\ref{fig:ch312-explore-math}合在一起就能直接读出：
\begin{align*}
\mathrm{Dep}^*(T)
&\;\supseteq\;
\{\mathrm{P0},\mathrm{P1},\mathrm{P2},\mathrm{Tail},T\},\\
\Gamma_{\text{开任务}}
&\;\supseteq\;
\{\mathrm{P0},\mathrm{P1},\mathrm{P2}\},\\
\mathrm{Missing}_{\Gamma}(T)
&\;\ni\;
\mathrm{Tail}.
\end{align*}
第~2~章当时\textbf{并没有}强制「先交出 $\mathrm{Missing}_\Gamma(T)$，再动手写码」——
闭包表要变成门禁，缺口就在这里。

\paragraph{流程四：依赖、展开与进度——三件不同的事画在一页}
图~\ref{fig:ch2-fips-dataflow}画的是\textbf{调用嵌套 $+$ 数据字节}；图~\ref{fig:ch2-explore}更接近\textbf{能力}怎么长出来。
图~\ref{fig:ch312-three-layers}把三层画在同一页，并用\textbf{三种不同图元}区分（别只看颜色）：
\begin{itemize}
  \item 层 A：\textbf{大框套小框} $=$ 谁调用谁；\textbf{椭圆} $=$ 数据；箭头 $=$ 数据流向；
  \item 层 B：\textbf{菱形} $=$ 能力顶点；实心 $=$ $\in\Gamma$，虚边 $=$ 缺项；粗箭头旁写「依赖」；
  \item 层 C：\textbf{圆} $=$ 配置状态；边上写转移名；双圆 $=$ 接受。
\end{itemize}
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  title/.style={font=\small\bfseries, anchor=west},
  note/.style={font=\footnotesize, text=gray!60!black, align=center},
  framelbl/.style={font=\small\bfseries, anchor=north west, inner sep=2pt},
  var/.style={draw, ellipse, minimum width=1.55cm, minimum height=0.65cm,
              font=\scriptsize, fill=yellow!15, inner sep=1pt},
  ability/.style={draw, diamond, aspect=2.1, font=\scriptsize, align=center,
              inner sep=1pt, minimum width=1.7cm, minimum height=1.0cm},
  st/.style={draw, circle, minimum size=1.35cm, font=\scriptsize, align=center, inner sep=1pt},
  darr/.style={-{Stealth[length=2.2mm]}, thick},
  dep/.style={-{Stealth[length=2.2mm]}, very thick, blue!55!black},
  tarr/.style={-{Stealth[length=2mm]}, thick, densely dashed, green!40!black}
]
  % ---- 层 A：d,z 属 Alg.19 生成；Alg.16 只含被调用体与拼装结果 ----
  \node[title] at (0,0) {层 A：调用嵌套 $+$ 数据依赖（$\approx$ 图~\ref{fig:ch2-fips-dataflow}）};

  % 外框 Alg.19：浅底；顶部采样带与 16 之间加分隔线
  \node[draw, rounded corners=3pt, fill=gray!3, minimum width=15.6cm, minimum height=7.4cm,
        anchor=north west] (A19) at (0.0,-0.35) {};
  \node[framelbl] at ([xshift=8pt,yshift=-5pt]A19.north west) {Alg.19（外层：采样并调用 16）};

  % 采样带底色条（仅 19 区域）
  \fill[yellow!12, rounded corners=2pt]
    (0.25,-0.95) rectangle (15.35,-1.85);
  \node[var] (dA) at (9.5,-1.40) {$d$};
  \node[var] (zA) at (12.8,-1.40) {$z$};
  \node[font=\scriptsize] at (5.5,-1.40) {采样带（属 19，\textbf{不}属 16）};

  % 分隔：采样带 / Alg.16
  \draw[thick, densely dashed, gray!50]
    (0.7,-2.05) -- (14.9,-2.05);
  \node[font=\scriptsize, text=gray!55!black] at (3.2,-2.05) {以下进入 Alg.16};

  % 中框 Alg.16：更深底色，顶边在分隔线之下
  \node[draw, rounded corners=3pt, fill=orange!18, minimum width=14.2cm, minimum height=4.2cm,
        anchor=north west] (A16) at (0.7,-2.25) {};
  \node[framelbl] at ([xshift=8pt,yshift=-5pt]A16.north west) {Alg.16（被 19 调用：调 13 $+$ 拼装）};

  \node[draw, rounded corners=3pt, fill=blue!16, minimum width=4.4cm, minimum height=1.9cm,
        anchor=north west, align=center, font=\small\bfseries] (A13) at (1.3,-3.35)
    {Alg.13\\[2pt]（被 16 调用）\\[1pt]{\normalfont\scriptsize 重计算}};

  \node[var] (ekA) at (9.5,-4.85) {$\mathit{ek}$};
  \node[var, fill=green!18] (dkA) at (12.8,-4.85) {$\mathit{dk}$};

  \draw[darr] (dA) -- ++(0,-0.7) -| (A13.north);
  \node[font=\scriptsize] at (7.6,-2.75) {$d$ 入 13};
  \draw[darr] (A13.south east) -- ++(1.6,0) |- (ekA.west);
  \node[font=\scriptsize] at (7.6,-5.35) {13 出};
  \draw[darr] (ekA) -- (dkA);
  \node[font=\scriptsize] at (11.15,-4.45) {拼装};
  \draw[darr] (zA) -- (dkA);
  \node[font=\scriptsize, align=left, anchor=west] at (13.55,-3.0) {$z$ 旁路\\不进 13};

  \node[note] at (7.5,-7.95)
    {$d,z$ 在 19 采样带（16 框外）；16 内才是调 13 与拼装。椭圆$=$字节；箭头$=$流向。\textbf{不}回答谁已获证。};

  % ---- 层 B ----
  \node[title] at (0,-8.85)
    {层 B：能力 DAG $+$ $\Gamma$（$\approx$ 表~\ref{tab:ch312-assets-math}）};
  \node[ability, fill=blue!18] (p0) at (1.5,-10.55) {P0\\$\in\Gamma$};
  \node[ability, fill=blue!18] (p1) at (4.8,-10.55) {P1\\$\in\Gamma$};
  \node[ability, fill=blue!18] (p2) at (8.1,-10.55) {P2\\$\in\Gamma$};
  \node[ability, fill=orange!28, densely dashed, thick] (tl) at (11.4,-10.55) {Tail\\$\notin\Gamma$};
  \node[ability, fill=orange!15, densely dashed] (Tb) at (14.0,-10.55) {$T$};
  \draw[dep] (p0) -- node[above, font=\scriptsize]{依赖} (p1);
  \draw[dep] (p1) -- node[above, font=\scriptsize]{依赖} (p2);
  \draw[dep] (p2) -- node[above, font=\scriptsize]{依赖} (tl);
  \draw[dep] (tl) -- node[above, font=\scriptsize]{接缝} (Tb);
  \node[note] at (7.5,-12.0)
    {图元：菱形$=$能力；实心$\in\Gamma$、虚边$=$缺项；粗蓝箭头$=$依赖/接缝。\textbf{不}回答字节路径。};

  % ---- 层 C ----
  \node[title] at (0,-12.85) {层 C：一次配置轨迹（device$\to$stable）};
  \node[st, fill=green!12] (q0) at (1.8,-14.55) {$\Gamma_0$\\$F{=}\{T\}$};
  \node[st, fill=green!12] (q1) at (5.5,-14.55) {缺项\\前沿};
  \node[st, fill=green!12] (q2) at (9.2,-14.55) {$\mathsf{Probe}$};
  \node[st, double, fill=green!22] (q3) at (12.9,-14.55) {$T{\in}\Gamma_m$\\接受};
  \draw[tarr] (q0) -- node[above, font=\scriptsize]{$\mathsf{Expand}$} (q1);
  \draw[tarr] (q1) -- node[above, font=\scriptsize]{$\mathsf{Compose}$} (q2);
  \draw[tarr] (q2) -- node[above, font=\scriptsize]{$\mathsf{Certify}$} (q3);
  \node[note] at (7.5,-16.0)
    {图元：圆$=$配置；虚线箭头$=$转移名；双圆$=$接受。回答「这次怎么走」；\textbf{不是}整库拷贝。};
\end{tikzpicture}
\caption{依赖、展开与进度（图元不同）：嵌套数据流 / 菱形能力依赖 / 圆形作业进度}
\label{fig:ch312-three-layers}
\end{figure}
读图可以记一句：\textbf{层 A 看字节与调用；层 B 看能不能引用；层 C 看这次怎么获证。}
三者若都画成圆角方框，最容易串层——本图刻意用椭圆 / 菱形 / 圆拆开。

\paragraph{流程五：尾缝与 2-launch——组合并不免费}
对照表~\ref{tab:ch2-launch}和图~\ref{fig:ch312-explore-math}：
「PKE 已经在 $\Gamma$ 里」加上「哈希、拼装片段也能对拍」，
\textbf{推不出}「接好的 KEM.KeyGen 自动就合法」。
尾缝必须当成新节点，先走 $\mathsf{Compose}/\mathsf{Probe}$，再 $\mathsf{Certify}$——
也就是说，可以找到 $x,y\in L$ 使得 $xy\notin L$（§3.9）。

\paragraph{流程六：correctness——缺的是成员判定}
图~\ref{fig:ch312-explore-math}里灰框旁路写着 $\mathrm{oracle}\neq L$：
伪代码加菜单，最多提示一下 $\Sigma$；没有成员判定时，搜索空间几乎就是 $\Sigma^*$。
correctness 能证明字节对得上，\textbf{证明不了}那条路是合法派生。

\paragraph{流程七：晋级——就是一次配置转移}
合法叙事对应一条配置轨迹（和图~\ref{fig:ch312-three-layers}的层 C 一致）：
\[
(\Gamma_0,F_0,S_0)
\;\xrightarrow{\mathsf{Expand}}\;
\cdots
\;\xrightarrow{\mathsf{Probe}/\mathsf{Compose}}\;
\cdots
\;\xrightarrow{\mathsf{Certify}}\;
(\Gamma_m,\emptyset,S_m),
\quad T\in\Gamma_m.
\]
若中途引用了禁字集 $\mathrm{Forb}$ 中的因子（已关闭或明确禁止的路线），
应直接拒绝（$\mathsf{Freeze}$ / 沉入拒绝阱）。

\paragraph{流程八：上游一变——$\mathsf{Dirty}$}
若某已获证的上游能力（例如 P1）对外约定变更——I/O、布局或同步约定不再与获证时一致——
则原先依赖该约定的下游（P2/P3）不能因为「上次 PASS」就继续留在 $\Gamma$ 里；
必须走 $\mathsf{Dirty}$（§3.11）。

\paragraph{四组问题：换成本章用语再说一遍}
\begin{table}[H]
\centering\small
\caption{§2.6 四组问题的本章重述（对照表~\ref{tab:ch2-four-q}；工程词为主，括号附已挂钩记号）}
\label{tab:ch312-four-q}
\begin{tabular}{@{}L{1.6cm}L{6.0cm}L{6.2cm}@{}}
  \toprule
  问题组 & 第~2~章里的疑惑（缩写） & 用本章对象怎么说 \\
  \midrule
  第一组 &
  凭目录名，还是凭证书，才算站在 PKE 上？菜单等于说明书吗？ &
  站得住 $\Leftrightarrow$ 相关能力已获证（$\in\Gamma$）；
  菜单 $\neq$ 已获证清单 $\neq$ 合法计划集合。 \\
  \midrule
  第二组 &
  NTT、PKE 都 PASS 了，谁来保证尾缝和 2-launch？ &
  组合并不免费：接缝还得经声明接缝并获证；
  语义对照过关 $\neq$ 拼装计划合法。 \\
  \midrule
  第三组 &
  只给伪代码和菜单，搜索为什么会炸？ &
  没有成员检查，搜索空间接近任意拼接；
  要收束，就只能沿着已获证边 / 合法计划往下长。 \\
  \midrule
  第四组 &
  能不能先交闭包表？上游改了，证书怎么作废？ &
  闭包表至少写清：目标、已获证、依赖闭包、缺项；
  上游一变，打脏标记。 \\
  \bottomrule
\end{tabular}
\end{table}

\paragraph{读完本节，带走三句话就够}
\begin{enumerate}
  \item 表~\ref{tab:ch312-assets-math}、图~\ref{fig:ch312-explore-math}、图~\ref{fig:ch312-three-layers}
        是第~2~章故事的\textbf{数学完全体}；原来的表和图仍在第~2~章备查。
  \item 最容易搅在一起的三层是：数据流、能力库、一次派生——先分开，再提问。
  \item 四组问题到本章已经能\textbf{把话说清楚}；真正变成任务启动时的硬检查，是下一章起的工程化工作。
\end{enumerate}

\subsection{枢纽：依赖、展开与进度各由哪件工具管}
\label{sec:three-layers}

三件工具的数学与工程落点都写过了。收束前再钉死一句读章枢纽：
\textbf{依赖、展开与进度是三件不同的事}，并分别由三件工具接管——
\textbf{依赖}归依赖图与依赖闭包，
\textbf{展开}归形式语言，
\textbf{进度}归作业过程自动机。
下面用三个定义把对象名钉死，并对照它们各回答什么、不回答什么。

\begin{definition}[能力 DAG（库）]
\textbf{能力 DAG} 是 DAG $G_{\mathrm{lib}}=(V_{\mathrm{lib}},E_{\mathrm{lib}})$，
顶点为可命名、可获证的能力，边为依赖（语义依赖或已声明接缝依赖）。
它描述引理库的\textbf{静态}结构，不随单次任务进度改写边集
（结点证书状态可变，见配置层）。
\end{definition}

\begin{definition}[一次任务与派生对象]
固定目标 $T\in V_{\mathrm{lib}}$ 与文法 $\mathcal{G}$（见形式语言一节）。
\textbf{一次任务}指试图使 $T$ 获证的一次预研作业。
该作业中，从开始符号出发按产生式改写所得的符号串序列称为\textbf{派生}；
其树形记录称为\textbf{派生树}。派生树是 $G_{\mathrm{lib}}$ 上依赖信息的\textbf{一次使用痕迹}，不是整库拷贝。
\end{definition}

\begin{definition}[配置轨迹]
作业过程自动机上的状态序列 $q_0\to q_1\to\cdots\to q_m$，其中每个 $q_i$ 为配置
$(\Gamma_i,F_i,S_i)$（见作业过程自动机一节），称为\textbf{配置轨迹}。
它记录证书如何增减、前沿如何消解——即「进度」。
\end{definition}

\begin{proposition}[三件工具分工]
\begin{center}
\begin{tabular}{@{}L{2.8cm}L{2.4cm}L{4.2cm}L{4.0cm}@{}}
  \toprule
  对象 & 归属工具 & 回答 & 不回答 \\
  \midrule
  能力 DAG & 依赖图与闭包 & 库中谁依赖谁 & 某次任务如何逐步展开 \\
  派生树 & 形式语言 & 该次任务的展开句子 / 树形 & 全局共享边的全貌 \\
  配置轨迹 & 作业过程自动机 & 证书如何增减；接受/拒绝 & 输出是否符合 FIPS 字节 \\
  \bottomrule
\end{tabular}
\end{center}
\noindent
\textbf{依赖归依赖图；展开归形式语言；进度归作业过程自动机。}
\end{proposition}

\begin{example}[纯数学：引理库 vs 一次证明树]
左图：引理库 DAG（共享）。右图：证明 $T$ 时实际用到的派生树（一次作业）。
结点 $L_2$ 可被许多证明共享，但右图只出现本次用到的边。
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=1.5cm, y=1.1cm,
  every node/.style={font=\footnotesize},
  c/.style={draw, circle, minimum size=0.6cm},
  arr/.style={-{Stealth[length=1.6mm]}, thick}
]
  \node[font=\small] at (0.75,1.8) {库 DAG};
  \node[c] (a) at (0,1) {$A$};
  \node[c] (b) at (1.5,1) {$B$};
  \node[c] (c) at (0.75,0) {$C$};
  \node[c] (t1) at (0.75,-1) {$T_1$};
  \node[c] (t2) at (2.2,-0.3) {$T_2$};
  \draw[arr] (a) -- (c); \draw[arr] (b) -- (c);
  \draw[arr] (c) -- (t1); \draw[arr] (b) -- (t2); \draw[arr] (c) -- (t2);

  \begin{scope}[xshift=5.0cm]
  \node[font=\small] at (0.6,1.8) {证明 $T_1$ 的树};
  \node[c, fill=orange!15] (tt) at (0.6,1) {$T_1$};
  \node[c, fill=blue!10] (cc) at (0.6,0) {$C$};
  \node[c] (aa) at (0,-1) {$A$};
  \node[c] (bb) at (1.2,-1) {$B$};
  \draw[arr] (aa) -- (cc); \draw[arr] (bb) -- (cc);
  \draw[arr] (cc) -- (tt);
  \end{scope}
\end{tikzpicture}
\caption{配图：纯数学：引理库 vs 一次证明树}
\label{fig:auto-11}
\end{figure}
\end{example}

\begin{example}[工程：三层对照图]
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=1cm, y=0.85cm,
  every node/.style={font=\footnotesize, align=center},
  box/.style={draw, rounded corners=2pt, minimum height=0.55cm},
  arr/.style={-{Stealth[length=1.6mm]}, thick}
]
  \node[font=\small] at (1.2,3.3) {库（INDEX 依赖直觉）};
  \node[box, fill=blue!8] (p1) at (0,2.4) {NTT\ldots};
  \node[box, fill=blue!10] (p2) at (2.2,2.4) {PKE};
  \node[box, fill=orange!12] (k) at (4.2,2.4) {KEM};
  \draw[arr] (p1) -- (p2) -- (k);

  \node[font=\small] at (1.2,1.5) {一次派生（本次 KeyGen）};
  \node[box, fill=orange!15] (r) at (2.0,0.7) {KEM.KeyGen};
  \node[box] (l) at (0.6,-0.3) {PKE};
  \node[box] (s) at (3.4,-0.3) {尾缝};
  \draw[arr] (l) -- (r); \draw[arr] (s) -- (r);

  \node[font=\small] at (6.8,1.5) {配置轨迹（示意）};
  \node[box] (q0) at (6.2,0.7) {$q_0$};
  \node[box] (q1) at (7.5,0.7) {$q_1$};
  \node[box, fill=green!12] (q2) at (8.8,0.7) {$q_{\mathrm{ok}}$};
  \draw[arr] (q0) -- (q1) -- (q2);
  \node[anchor=west, align=left] at (6.0,-0.5) {$\Gamma$ 增大 / $F$ 消解};
\end{tikzpicture}
\caption{配图：工程：三层对照图}
\label{fig:auto-12}
\end{figure}
\end{example}

\subsection{本章小结与下一章预告}
\label{sec:mathbg-summary}

§~\ref{sec:mathbg-intro} 导论先交代方法论面向的场景，再交代工程策略与三件工具；
随后用三大节分别写完数学：
\[
\underbrace{\text{依赖图与依赖闭包}}_{\S\ref{sec:tool-dep}}
\;\longrightarrow\;
\underbrace{\text{形式语言（合法拼装）}}_{\S\ref{sec:tool-lang}}
\;\longrightarrow\;
\underbrace{\text{作业过程自动机}}_{\S\ref{sec:tool-auto}},
\]
再写工程体现、回扣 KeyGen，并以枢纽节收束「依赖 / 展开 / 进度」的分工。
迷路时先回表~\ref{tab:ch3-three-tools} 与图~\ref{fig:ch3-spine}。
有意留白：产生式在仓库里如何形式化、闭包表文件长什么样、可判定性如何证——留给后文。

下一章（第~\ref{sec:math}~章）要把这些对象\textbf{填成工程上可写的字段}：节点属性、三类边、证书状态怎么命名，等等。

% ============================================================
\section{从数学到工程对象：节点、边与证书}
\label{sec:math}

上一章给的是一般语言；本章把它\textbf{落到}预研图上，变成一张张能填的字段。
若用语和第~\ref{sec:mathbg}~章打架，先以「工程上能不能执行」为准，再回头改数学章并记下修订。

\subsection{三个层面（复习）}
\begin{center}
\begin{tabular}{@{}L{3.2cm}L{5.5cm}L{5.3cm}@{}}
  \toprule
  概念 & 它回答什么 & 它不回答什么 \\
  \midrule
  能力 DAG & 引理库里谁依赖谁？ & 某次任务具体怎么一步步展开？ \\
  语法树 / 派生 & 这次任务展开成了哪句话？ & 全局所有共享边长什么样？ \\
  配置自动机 & 证书怎么增减？何时接受、何时拒绝？ & 代数算得合不合 FIPS？ \\
  \bottomrule
\end{tabular}
\end{center}

\subsection{节点（已验证能力）}
节点 $v$ 建议带上这些字段：
\begin{itemize}
  \item \texttt{id}：稳定名字（如 \texttt{pke.keygen.k4}）；
  \item \texttt{layer}：P0--P3（方法论分层，不必和目录树一一对应）；
  \item \texttt{iface}：I/O、dtype、形状/布局、同步约定——\textbf{能复用的是 iface，不是源码}；
  \item \texttt{cert}：路径 + CPU+SIM（必要时加 KAT）+ 版本锚点；
  \item \texttt{forbid}：否定约束；
  \item \texttt{repo}：权威目录锚点。
\end{itemize}

\paragraph{分层语言（P0--P3）}
\begin{center}
\begin{tabular}{@{}ll@{}}
  \toprule
  层 & 含义（放在本仓库语境里说） \\
  \midrule
  P3 & 顶层算子 / 算法入口（如 KEM.KeyGen、KEM.Encaps） \\
  P2 & 算法构件（如 PKE.KeyGen / Encrypt / Decrypt 全链） \\
  P1 & 领域原语（NTT、basemul、CBD、ByteEncode、SHAKE\ldots）——\textbf{当能力名用}，本章不讲它们的代数细节 \\
  P0 & 平台公理（DataCopy、向量算子、MMAD+tiling、TPipe/TQue 同步壳\ldots） \\
  \bottomrule
\end{tabular}
\end{center}

\subsection{边的三种类型}
\begin{enumerate}
  \item \textbf{语义依赖}：$u$ 要对，离不开 $v$ 的 iface（例如「先有 NTT 能力」，才谈得上 PKE.KeyGen）；
  \item \textbf{接缝（compose）}：$A$、$B$ 各自有证，$A\!\circ\!B$ \textbf{仍要}单独立成节点再获证
        （「组合并不免费」）；
  \item \textbf{否定边}：写明禁止什么（frozen 模板、未登记计算块、Host 默认走密码路径，等等）。
\end{enumerate}

\subsection{证书状态（含非单调）}
\[
\mathsf{unproven}\to\mathsf{probe}\to\mathsf{lemma}\to\mathsf{deliverable},
\quad\text{以及}\quad
\mathsf{dirty}.
\]
\texttt{dirty} 的意思是：上游 iface/tiling 一改，下游证书作废——引理库 $\Gamma$ \textbf{是可以收缩的}。

\subsection{仓库机制与数学对象的对照（凭直觉）}
\begin{itemize}
  \item 活跃 \texttt{INDEX.md}：眼下可以引用的能力菜单（$\Sigma$ 在工程上的投影，\textbf{还不是} $L$）；
  \item \texttt{frozen/} + \texttt{FROZEN.md}：否定边；
  \item \texttt{baseline-registry}：golden 允许调用的、已经验证过的计算块；
  \item \texttt{customspec}：examples 写码前的规格门禁（派生计划的一种文书形态）。
\end{itemize}

\subsection{立论（承接第~\ref{sec:mathbg}~章）}
\begin{quote}
预研算不算合法 $\approx$ 在已获证能力生成的语言 $L$ 上，
用配置自动机去搜一条接受路径；\\
\textbf{幻觉} $\approx$ 非法转移，或根本没有证书边。\\
语义对照（FIPS/liboqs/golden）管「算对没算对」；形式方法管「砖是从哪块砌过来的」。
\end{quote}

% ============================================================
\section{门禁与工作流：Agent 被允许做什么}
\label{sec:gates}

\subsection{合法性规则（v0）}
接到目标 $T$ 时，只允许做这些事：
\begin{enumerate}
  \item 把依赖闭包展开清楚（语义边 + 接缝边）；
  \item 分类：已经有 lemma/deliverable 的；还缺的；只有否定边能命中的；
  \item 缺项 $\Rightarrow$ 先做探针/exp 并拿到证书，再引用；禁止在做 $T$ 的任务里顺手发明 P0/P1；
  \item 新接缝 $=$ 新节点，必须单独认证；
  \item 验收只看 I/O 是否对上 golden/KAT，不看「是不是和参考源码长得一样」；
  \item 自包含：挂的是 iface，不要跨目录偷源码（仓库里已有的例外除外）；图上的边 $\neq$ \texttt{\#include}。
\end{enumerate}

\subsection{四步工作流}
\begin{lstlisting}
1. 目标声明  T、I/O、验收级别（probe / lemma / deliverable）
2. 闭包展开  P0--P3；ok / missing / dirty；接缝清单
3. 最小补洞  只补 missing 与新接缝；forbid 写入否定边
4. 闭环写回  新节点入图；失败则改规则或 Freeze，不默认可复用
\end{lstlisting}
\textbf{先交闭包表，再写码。} customspec / STATUS / registry 是文书形态，替代不了闭包表。

\subsection{域无关性}
把密码学专有名词抽掉之后，剩下的还是：终端（axiom）、非终端（goal）、产生式（compose）、证书（judgment）。
同一套问题也适用于非密码的 AscendC 预研；ML-KEM 不过是对照清晰、接缝又多的一块\textbf{压力测试床}。

% ============================================================
\section{复盘：理论如何对照 \texttt{kem.keygen} 与 \texttt{kem.encaps}}
\label{sec:replay}

\subsection{复盘的目的}
不是要证明「我们其实早就在按理论做」（事后贴标签没什么价值），而是：
\begin{enumerate}
  \item 检查分层和接缝能不能\textbf{用理论语言说清楚}；
  \item 找出偏离（例如曾经靠 correctness 里的 vendor、或 frozen 模板）并记成\textbf{方法论上的欠债}或\textbf{否定边}；
  \item 总结「数学原理是怎么落到目录、launch、证书上的」。
\end{enumerate}

\subsection{\texttt{kem.keygen} 校准问题单}
\begin{itemize}
  \item P3 是不是只依赖已经获证的 P2，而不是在 KeyGen 任务里重写一遍 NTT？
  \item 2-launch、Alg.16 尾、\texttt{dk} 展开这类接缝，有没有写清楚？
  \item baseline-registry 里的 golden 块，是不是都能在图上找到对应的 lemma？
  \item 否定边有没有落笔（Host 默认禁止走密码路径、禁止把 liboqs 当 AscendC 规格）？
\end{itemize}
仓库锚点（查阅用）：
\texttt{examples/stable/stable-fips203-mlkem-kem-keygen-k4/}，
\texttt{docs/specs/fips203-mlkem1024-kem-keygen-baseline-registry.md}，
以及相关的 \texttt{pass-fix-*} device 探针。

\subsection{\texttt{kem.encaps} 复盘要点（讨论共识）}
\begin{itemize}
  \item 合法路径应挂在已获证的 \texttt{pke.encrypt} 和哈希/G 等 lemma 上，而不是拿 correctness 里的 vendor 当实现模板；
  \item 和 stable Encrypt 的编译期关系、接缝关系，必须写进闭包，不能只是「觉得自己复用了」；
  \item 若实现史上走过一段混乱探索，复盘文应如实写——那是对照组和方法论的动机，不是丢脸。
\end{itemize}

\subsection{期望产出的「具象化对照表」（后续填空）}
\begin{center}
\begin{tabular}{@{}L{4cm}L{5cm}L{5cm}@{}}
  \toprule
  理论对象 & 工程上长什么样 & 证书形态 \\
  \midrule
  节点 $v$ & 探针/exp/stable 目录 & STATUS + run.sh 对拍 \\
  语义边 & 「必须先有某某能力」 & INDEX / customspec 引用 \\
  接缝节点 & 多 launch / 融合边界 & 集成探针单独 PASS \\
  否定边 & frozen / forbid 条文 & FROZEN.md、Rule \\
  字 $w\in L$ & 一次任务的闭包表+计划 & 评审通过后再写码 \\
  \bottomrule
\end{tabular}
\end{center}

% ============================================================
\section{前瞻：用理论指导尚未完成的 \texttt{kem.decaps}}
\label{sec:forward}

\subsection{为什么 Decaps 适合做试金石}
Decaps 同时依赖 Encrypt、Decrypt、KeyGen 的 I/O，还有 FO 失败路径，是典型的\textbf{多父 DAG}；
而且，如果理论只能解释已经做完的故事、生不出待办清单，方法论就停在修辞层面了。

\subsection{前瞻时 Agent（或人）至少应交出的闭包}
\begin{enumerate}
  \item 目标 I/O 与验收级别；
  \item 依赖节点清单（标好 ok / missing / dirty）；
  \item 接缝清单（对称失败、重加密比对等，要不要单独成节点）；
  \item 否定边（不能引用的 correctness vendor / frozen 实现）；
  \item 最小补洞顺序（先探针哪条缝）。
\end{enumerate}

\subsection{成功标准（分阶段）}
\begin{itemize}
  \item \textbf{弱成功}：闭包表能拿去执行；偏差能归因到缺边、假边或脏证书，并回写进本章理论；
  \item \textbf{强成功}：在门禁之下做完 device/stable 级实现，而且过程指标好过 correctness 那一段探索史
        （比较轴见下一章）。
\end{itemize}

% ============================================================
\section{对照组：\texttt{correctness} 标本的意义}
\label{sec:correctness}

\subsection{为何保留 correctness}
\texttt{*-correctness-*} 在\textbf{完全不考虑优化}的前提下，只追求能跑、结果对：
多 kernel launch、多搬运与同步——这是「一定能搭出正确积木」的保守拼法，
性能谈不上，后续 P0/P1 的工程优化也几乎接不进去。

它还记下了一段真实的 Agent 过程：故意不给确定的拼装路线时，
探索成本极高（失败、卡死、回退；短时间内曾烧掉大量 token），最后只得到「结果正确」的代码。

\subsection{在方法论里扮演什么角色}
\begin{center}
\begin{tabular}{@{}L{3cm}L{11cm}@{}}
  \toprule
  角色 & 含义 \\
  \midrule
  正确性锚 & 下界存在性：积木搭得出来，I/O 也对得上 \\
  结构反例 & 展示「只求对」会长成怎样的 launch/同步膨胀 \\
  过程对照 & 没有确定路线时，搜索要付多大代价 \\
  实验基线 & 和方法论 Agent、人工指导路径三方比较 \\
  \bottomrule
\end{tabular}
\end{center}

\textbf{纪律}：correctness 是\textbf{基线标本}，不是活跃实现模板；可读、可对拍、可对比，\textbf{禁止}当抄码来源（和 frozen「出门不带码」同一条规矩）。

\subsection{建议比较的几条轴}
\begin{enumerate}
  \item \textbf{过程}：token / 轮次 / 回退；有没有先交闭包表；非法边有没有被拦住；
  \item \textbf{结构}：launch 数、同步点、lemma 复用、接缝是否写清楚；
  \item \textbf{结果}：CPU+SIM；SIM tick 相对 correctness 是几倍。
\end{enumerate}
三档：A 旧式无路线探索 $\sim$ correctness；B 新方法论下的 Agent；C 人工指导的 device/stable。
B 不必一次就打赢 C；只要明显好过 A，方法论就过了第一关。

% ============================================================
\section{未决议事项、日程与维护}
\label{sec:open}

\subsection{刻意先不拍板的事}
\begin{itemize}
  \item 节点要不要和目录名 $1$:$1$ 对应；图存在 Markdown 表里，还是 YAML/JSON 里；
  \item 主叙事选工作流网、Petri 网、属性文法、判断式里的哪一种；
  \item 什么时候写进 Rule/Skill；可判定性和复杂度要不要单开一条理论线。
\end{itemize}

\subsection{近期填空顺序（仍然是「先特殊、后一般」）}
\begin{enumerate}
  \item 按复盘反馈改第~\ref{sec:mathbg}~章（定义写准一点、例子密一点）；把 KeyGen 合法派生轨迹压成一页纸；
  \item 第~\ref{sec:math}~章的字段表，和仓库文书（闭包表 schema）对齐；
  \item Encaps：理论轨迹对照实现轨迹；
  \item Decaps：前瞻闭包；
  \item correctness 对照实验写成可执行的协议；
  \item 结论稳了之后，再考虑迁到 \texttt{docs/notes/}。
\end{enumerate}

\subsection{文档与分支约定}
\begin{itemize}
  \item 本文件是本专题\textbf{唯一}的 TeX/PDF 工作副本（不加日期后缀），持续修订；
  \item 讨论纪要：\texttt{形式语言DAG预研讨论纪要.md}（同样单文件刷新）；
  \item 工作分支：\texttt{research/formal-lang-dag}；未经用户明确指令，\textbf{不合入} \texttt{main}；
  \item 不记录题外的知识产权保护策略讨论。
\end{itemize}

\subsection{编译}
\begin{lstlisting}
bash scripts/xelatex-clean.sh \
  docs/research/从已验证能力到合法派生-面向Agent预研的形式方法教材草案.tex
\end{lstlisting}

\end{document}
