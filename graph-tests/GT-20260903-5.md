# GT-20260903-5 — skel1 同进程多 launch · SIM 加压盯挂死

## 任务单（主控填写）

```text
【任务 id】GT-20260903-5
【目标】在 pass-toy-encrypt-fsm-l18-skel1 上做同进程连续多 launch（默认 ≥8），看 SIM 是否 SynchronizeStream 挂死/超时；不对算法正确性
【图谱】docs/rg-encrypt-l18.yaml
【必读节点】
  - Q-TOY-SIM-MULTI-LAUNCH-HANG：多 launch 是否挂
  - D-EXP-GT-20260903-5：本刀决策
  - F-SIM-LAUNCH / J-NOT-LAUNCH-COUNT：生产 2-launch 与「次数非主因」背景
  - J-FAIL-AIC-SCALAR-TRACE：TRACE 保持 DataCopy，勿退回标量
  - D-RG-SIM-PRIMARY：只认 SIM；勿 CPU 验收
【禁止】勿重试 inactive/retracted；勿 CPU 验收；勿 SyncAll@AIC-Wait；勿 frozen 抄码；勿改图谱 yaml；勿 git push / 开分支 / commit；勿并行多路 SIM；勿改 stable Encrypt
【要做】
  1. 改 main.cpp（仅 Host）：环境变量 TOY_LAUNCH_REPEAT（默认 8，可用 16）；每轮：清零 device TRACE → FillTraceOnes 若需 → H2D → ACLRT_LAUNCH mmad_custom → aclrtSynchronizeStream → D2H 打印 [toy-l18-trace] iter=i …
  2. 每轮前后 printf 标明 iter / sync done；挂死时最后一行须能定位第几轮
  3. 中文注释写清：背景=用户要求 SIM 加压看卡死；结论=同进程多 launch；未采用=外层反复整树 run.sh 当唯一加压
  4. SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4（建议 TOY_LAUNCH_REPEAT=16）；KERNEL 预算遵守用例默认
  5. 若一次绿：再跑 1 次同命令（计入 MAX_SIM_ATTEMPTS）；日志拷到 /opt/cursor/artifacts/GT-20260903-5-sim.log
  6. 更新 STATUS.md；反馈写入本文件
【不要做】不要改 kernel FSM 握手序；不要为「更绿」加 SyncAll/拆 launch；不要新建第二个用例目录（本刀改现有 skel1）
【WALL_CLOCK_BUDGET】40
【MAX_SIM_ATTEMPTS】2
【MAX_REWORK_ROUNDS】1
【DONE_DEFINITION】SIM 进程正常退出且 16 轮均有 sync done+TRACE；或明确 HANG/TIMEOUT（记挂在第几轮）；未 push
【ABORT_IF】编译失败连续 2 次；SIM 同错 2 次无新假设；超过 40 分钟；改图谱或 push；15 分钟无进展不报 BLOCKED
【反馈】填下方模板；到点也必须交
```

## 反馈（Subagent 填写）

```text
【任务 id】GT-20260903-5
【SIM 命令】TOY_LAUNCH_REPEAT=16 SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4（用例目录 pass-toy-encrypt-fsm-l18-skel1；2/2 次）
【结果】PASS
【挂点】无挂死；16/16 轮均 sync done。末轮日志：`[host] iter=15/16 sync done` + `[toy-l18-trace] iter=15 stages set=7/8 : 0 1 2 3 4 5 6`；iter=0 常见 6/8（缺槽4，同 GT-4）。wall≈51.9s / 61.8s，tick≈291k，budget=240s
【产物路径】/opt/cursor/artifacts/GT-20260903-5-sim.log ；STATUS.md；main.cpp（仅 Host 多 launch）
【是否改了图谱】否
【是否 push】否
【建议主控关注】toy 尺度同进程 16×launch 未复现 SynchronizeStream 挂死，支持 J-NOT-LAUNCH-COUNT；Encrypt 真卡死更宜盯 FSM/握手/真机 sticky，而非单纯加 launch 次数。未改 kernel FSM / 未加 SyncAll。
```

## 主控备注

- 用户明确：暂时不能上机；toy 要在 SIM 多跑看会不会卡。
- 本刀优先同进程多 launch（贴近 sticky），不是催 NPU。
