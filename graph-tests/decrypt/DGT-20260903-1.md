# DGT-20260903-1 — toy 仅 SoftSyncArrive（AIV0 写 / AIV1 忙等）

**状态**：SIM 反馈已回（PASS）  
**图谱目标**：回答 `Q-TOY-SOFTSYNC`；服务终点 Decrypt 卡死（`Q-ULT`）  
**主控**：只定规格与收反馈；**本文件由主控维护**

---

## 下发正文（给 Subagent）

```text
【任务 id】DGT-20260903-1
【目标】在 ascendc-tests 新建轻量 MIX toy：只测生产 Decrypt 的 SoftSyncArrive（AIV0 对 GM slot 标量写 1，AIV1 while==0 忙等）。SIM 能跑完或明确挂死（记末行）。不对算法正确性。
【图谱】/workspace/docs/rg-decrypt-fused.yaml（只读，禁止改）
【必读节点】
  - Q-TOY-SOFTSYNC：本刀要回答的问题
  - F-SOFTSYNC-ARRIVE / F-HOST-ZERO-SOFTSYNC：必须同构的定式
  - F-SOFTSYNC-VS-ENCRYPT-FAIL：这是 AIV↔AIV，不是 AIC 参与的双向 SoftSync
  - J-PRIMARY-SOFTSYNC：未证假设（本刀服务它）
  - J-FAIL-SYNCALL-ON-AIC-WAIT：禁止 AIC Wait 中 SyncAll（本刀甚至不要 CrossCore）
  - J-FAIL-TPIPE-MARK-IN-FUSED：完成标记若用 DataCopy，TPipe 建在 toy 短核可接受；勿改 stable fused
  - D-RG-SIM-PRIMARY：只认 SIM
  - D-NO-STABLE-AS-MAIN-LAB：禁止改 stable decrypt 当本刀实现
【禁止】改 docs/rg-*.yaml；git push / 开分支 / commit；frozen 抄码；CPU 当证据；CrossCore GATE/NTT/INTT（那是 DGT-2/3）；自造 AIC↔AIV SoftSync；并行 SIM；改 examples/stable/**
【要做】
  1. 新建 ascendc-tests/fix-toy-decrypt-fsm-softsync1/（fix- 前缀）
  2. KERNEL_TYPE_MIX_AIC_1_2；blockDim=1
  3. Host：分配 softSyncGm（≥64B）H2D 全 0；单 launch；aclrtSynchronizeStream
  4. 设备 SoftSyncArrive 必须字面同构生产：
       AIV0: s[0]=1; PipeBarrier<PIPE_ALL>
       AIV1: while (s[0]==0) {} ; PipeBarrier<PIPE_ALL>
     slot 只用 0。禁止改成 CrossCore 或 SyncAll 来「代替」忙等。
  5. AIC：本刀不要 WaitFlag。做极轻 MMAD（可对照 fix-toy-encrypt-fsm-ntt1 的 16×32×32）后返回。禁 SyncAll。
  6. SoftSync 通过后：AIV0 用 DataCopy 写完成标记到 out/trace（学 pass-toy-encrypt-fsm-l18-skel1 GT-4：每槽 32B；禁 Duplicate(int8)）。Host 打印 slots。便于区分「挂在 busy-wait」vs「跑完」。
  7. 工程壳：对照活跃 ascendc-tests/fix-toy-encrypt-fsm-ntt1 的 CMake/run.sh/camodel_sim_log；KERNEL_COMPUTE_BUDGET_SEC 默认 180（防挂死）
  8. 详细中文注释（文件头/函数头/SoftSync 紧上方写：背景=隔离 Decrypt 忙等；结论=本 toy 要测它会否挂；未采用=用 CrossCore 代替 SoftSync）
  9. 写 AscendC API 前查 library/documents/CANN-AscendC算子开发接口参考-查阅索引.md；缺则查 PDF 并在索引顶部追加一行
  10. 验收只跑：SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4
      PASS=退出码 0 且 Host 见到完成标记；HANG/TIMEOUT=记最后日志行与是否已过 SoftSync
  11. STATUS.md + 更新 ascendc-tests/INDEX.md 非 ML-KEM 表一行；在本文件追加 §反馈
【不要做】GATE/NTT/INTT；liboqs；golden 数值对拍；改 stable；改图谱
【WALL_CLOCK_BUDGET】40 分钟
【MAX_SIM_ATTEMPTS】2
【MAX_REWORK_ROUNDS】1（本下发为第 0 轮）
【DONE_DEFINITION】SIM 一次结束（0 或明确 124/挂死已记末行）；STATUS+反馈已写；未 push；未改 yaml
【ABORT_IF】编译连续失败 2 次；SIM 同挂点 2 次且无新假设；超 40min；改图谱或 push；从 frozen 抄码；卡住 15min 无进展仍不报 BLOCKED
【反馈】填下方模板；到点也必须交
```

---

## 反馈（Subagent 填写）

```text
【任务 id】DGT-20260903-1
【SIM 命令】cd ascendc-tests/fix-toy-decrypt-fsm-softsync1 && SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4
【结果】PASS
【挂点】无（SoftSyncArrive 通过；双 AIV TRACE s0=0x50F700 s1=0x50F701；softSyncGm slot0=1；Total tick 3979；wall_sec≈0.834）
【产物路径】ascendc-tests/fix-toy-decrypt-fsm-softsync1/（STATUS.md）；/opt/cursor/artifacts/DGT-20260903-1-sim.log
【是否改了图谱】否
【是否 push】否
【建议主控关注】Q-TOY-SOFTSYNC 在隔离 toy 上 SIM 能跑完 → J-PRIMARY-SOFTSYNC（忙等即永久挂）本刀未证成；下一刀可叠 GATE（Q-TOY-SOFT-GATE / DGT-2）或看生产融合路径其它挂点
```
