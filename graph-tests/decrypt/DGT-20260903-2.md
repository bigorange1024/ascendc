# DGT-20260903-2 — SoftSyncArrive 后再一轮 GATE 4↔8

**状态**：subagent SIM 反馈已回（PASS）  
**图谱目标**：回答 `Q-TOY-SOFT-GATE`  
**主控**：只定规格与收反馈；**本文件由主控维护**

---

## 下发正文（给 Subagent）

```text
【任务 id】DGT-20260903-2
【目标】轻量 MIX toy：生产同构 SoftSyncArrive(slot0) 之后再一轮 GATE 4↔8（同构 Decrypt prep 段末：双 AIV SET(4)→AIC WAIT(4)/SET(8)→双 AIV WAIT(8)）。SIM 跑完或明确挂死。不对算法正确性。
【图谱】/workspace/docs/rg-decrypt-fused.yaml（只读）
【必读节点】
  - Q-TOY-SOFT-GATE
  - F-SOFTSYNC-ARRIVE / F-FSM-GATES / F-TOY-SOFTSYNC-SIM-PASS
  - J-TWO-GATE-DIFF（未证；本刀只测一轮 GATE）
  - J-FAIL-SYNCALL-ON-AIC-WAIT：AIC Wait(4) 期间禁 SyncAll
  - J-FAIL-INTT-FLAGS-57：本刀无 INTT
  - D-RG-SIM-PRIMARY
【禁止】改 docs/rg-*.yaml；git push/开分支/commit；frozen 抄码；CPU 当证据；NTT/INTT；第二轮 GATE；改 stable；并行 SIM；AIC Wait 中 SyncAll；用 CrossCore 代替 SoftSync
【要做】
  1. 新建 ascendc-tests/fix-toy-decrypt-fsm-soft-gate1/
  2. 工程壳可从 fix-toy-decrypt-fsm-softsync1 复制 CMake/run.sh/aic_func 等，再改语义（那是活跃 toy，非 frozen）
  3. KERNEL_TYPE_MIX_AIC_1_2；CrossCore 通道 <2, PIPE_MTE2>；flag 字面量 4 与 8
  4. AIV 序：SoftSyncArrive(slot0) → SET(4) → WAIT(8) → DataCopy TRACE 完成标记
  5. AIC 序：WAIT(4) → 极轻 MMAD 16×32×32 → SET(8) →（可选）DataCopy AIC TRACE 槽；WAIT 期间禁 SyncAll
  6. Host：softSyncGm 清零；单 launch；打印 TRACE
  7. 中文注释：SoftSync 与 GATE 两处写背景/结论/未采用
  8. 查 API 索引；缺则写回（CrossCoreSetFlag/WaitFlag 已有 GT 记录可复用）
  9. 验收：SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4；KERNEL_COMPUTE_BUDGET_SEC 默认 180
  10. STATUS.md；更新 ascendc-tests/INDEX.md 一行；本文件追加 §反馈；SIM 日志复制到 /opt/cursor/artifacts/DGT-20260903-2-sim.log
【不要做】NTT/INTT/第二轮 GATE；改图谱
【WALL_CLOCK_BUDGET】40 分钟
【MAX_SIM_ATTEMPTS】2
【MAX_REWORK_ROUNDS】1
【DONE_DEFINITION】SIM 结束（0 或 124/挂死记末行）；STATUS+反馈；未 push；未改 yaml
【ABORT_IF】编译失败 2 次；同挂点 2 次；超 40min；改图谱/push；frozen 抄码；15min 无进展不报 BLOCKED
【反馈】填下方模板
```

---

## 反馈（Subagent 填写）

```text
【任务 id】DGT-20260903-2
【SIM 命令】cd ascendc-tests/fix-toy-decrypt-fsm-soft-gate1 && SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4
【结果】PASS
【挂点】无；SoftSyncArrive(slot0)→GATE 4↔8 全过；synchronize 返回；TRACE s0=0x005F6800 s1=0x005F6801；softSyncGm slot0=1；tick=5073；wall_sec≈1.035；budget=180s；rc=0
【产物路径】ascendc-tests/fix-toy-decrypt-fsm-soft-gate1/（STATUS.md）；/opt/cursor/artifacts/DGT-20260903-2-sim.log
【是否改了图谱】否
【是否 push】否
【建议主控关注】Q-TOY-SOFT-GATE 可记 F-pass：SoftSync+单轮 GATE 在 SIM 不挂。J-TWO-GATE-DIFF 仍未测（需第二轮 GATE 或 fused NTT/INTT 刀）。
```
