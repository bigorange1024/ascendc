# DGT-20260903-3 — Decrypt 同核融合骨架（双 SoftSync + 双 GATE + NTT/INTT 1/3）

**状态**：Subagent SIM 反馈已回填（PASS）  
**图谱目标**：回答 `Q-TOY-FUSED-SKEL`  
**主控**：只定规格与收反馈；**本文件由主控维护**

---

## 下发正文（给 Subagent）

```text
【任务 id】DGT-20260903-3
【目标】轻量 MIX toy：同构 Decrypt fused 握手骨架（不对 Alg.15 正确性）：
  SoftSync slot0 → GATE 4/8 → NTT flag 1/3 → SoftSync slot1 → GATE 4/8 → INTT flag 1/3
SIM 跑完或明确挂死。带 DataCopy TRACE（GT-4 式 32B 槽），便于看卡在哪段。
【图谱】/workspace/docs/rg-decrypt-fused.yaml（只读）
【必读节点】
  - Q-TOY-FUSED-SKEL / Q-HANG-LOCI
  - F-SOFTSYNC-ARRIVE / F-FSM-GATES / F-NTT-FLAGS-13 / F-INTT-FLAGS-13
  - F-TOY-SOFT-GATE-SIM-PASS（上一刀一轮 GATE 已绿）
  - J-TWO-GATE-DIFF / J-PRIMARY-SOFTSYNC（未证）
  - J-FAIL-SYNCALL-ON-AIC-WAIT；J-FAIL-INTT-FLAGS-57（INTT 只用 1/3，禁 2/5/7）
  - J-FAIL-TPIPE-MARK-IN-FUSED：toy 短核可用局部 TPipe DataCopy TRACE；勿改 stable
【禁止】改 yaml；git push/分支/commit；frozen 抄码；CPU 验收；改 stable；AIC Wait 中 SyncAll；INTT flag 5/7；真 SHAKE/真 NTT 数学
【要做】
  1. 新建 ascendc-tests/fix-toy-decrypt-fsm-fused-skel1/
  2. 壳可从活跃 toy 复制：
       - SoftSync：fix-toy-decrypt-fsm-soft-gate1
       - NTT/GATE/INTT 握手：pass-toy-encrypt-fsm-l18-skel1（活跃，非 frozen）
     语义必须按本任务 Decrypt 序，不要照抄 Encrypt 单 GATE。
  3. KERNEL_TYPE_MIX_AIC_1_2；通道 <2,PIPE_MTE2>
  4. AIV 序（双 AIV 均走握手；仅 AIV0 写 SoftSync slot）：
       SoftSyncArrive(slot0) → SET(4)/WAIT(8) → SoftSyncClear(slot0)
       → SET(1)/WAIT(3)（NTT；中间 AIC MMAD）
       → SoftSyncArrive(slot1) → SET(4)/WAIT(8) → SoftSyncClear(slot1)
       → SET(1)/WAIT(3)（INTT；禁 Wait(2)）
       每段后 AIV0 DataCopy TRACE 槽（槽位自定，Host 打印）
  5. AIC 序：
       WAIT(4)/SET(8)  （prep GATE）
       WAIT(1)/轻 MMAD/SET(3)  （NTT）
       WAIT(4)/SET(8)  （su GATE）
       WAIT(1)/轻 MMAD/SET(3)  （INTT；无 ST_MMAD=2 Wait）
       AIC TRACE 用 GT-4：ones 模板 DataCopy，禁标量写 AIC 槽当唯一路径
  6. Host：softSyncGm 清零；TRACE 清零；单 launch；打印已置位槽
  7. 桩计算：极轻 MMAD 即可；禁真 unpack/su_dot/SHAKE
  8. 中文注释：文件头写流水对齐 fused_entry；SoftSync/GATE/NTT/INTT 各段背景结论未采用
  9. API 索引查阅并写回（若有新用法）
  10. SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4；KERNEL_COMPUTE_BUDGET_SEC 默认 300（骨架比前两刀长）
  11. STATUS.md；INDEX.md 一行；本文件 §反馈；日志 /opt/cursor/artifacts/DGT-20260903-3-sim.log
【不要做】改 stable fused；数值 golden
【WALL_CLOCK_BUDGET】45 分钟
【MAX_SIM_ATTEMPTS】2
【MAX_REWORK_ROUNDS】1
【DONE_DEFINITION】SIM 结束（0 或 124/挂死记末行+最后 TRACE 槽）；STATUS+反馈；未 push；未改 yaml
【ABORT_IF】编译失败 2 次；同挂点 2 次；超 45min；改图谱/push；frozen 抄码
【反馈】填下方模板
```

---

## 反馈（Subagent 填写）

```text
【任务 id】DGT-20260903-3
【SIM 命令】cd ascendc-tests/pass-toy-decrypt-fsm-fused-skel1 && SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4
【结果】PASS
【挂点】无（SynchronizeStream 返回；mark0=0x00F05E00；softSync 双槽 Clear=0）
【产物路径】ascendc-tests/pass-toy-decrypt-fsm-fused-skel1/ ；日志 /opt/cursor/artifacts/DGT-20260903-3-sim.log ；Total tick 13056；TRACE 7/8（s4 AIC prep WAIT4 未落；s0–s3/s5/s7=1；s6=0x01010101）
【是否改了图谱】否
【是否 push】否
【建议主控关注】Q-TOY-FUSED-SKEL：同核 Soft×2+GATE×2+NTT/INTT1/3 在 SIM 可跑完，非挂死主因；AIC TRACE 首槽偶发漏写属可见性噪声，勿据此判挂点。J-TWO-GATE-DIFF / J-PRIMARY-SOFTSYNC 本刀未证「生产卡死根因」。
```
