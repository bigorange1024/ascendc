# DGT-20260903-4 — fused-skel 同进程多 launch SIM 加压

**状态**：已完成（SIM ×16 PASS）  
**图谱目标**：回答 `Q-TOY-MULTI-LAUNCH`  
**主控**：只定规格与收反馈；**本文件由主控维护**

---

## 下发正文（给 Subagent）

```text
【任务 id】DGT-20260903-4
【目标】在已绿的 pass-toy-decrypt-fsm-fused-skel1 上加同进程多 launch 加压（清 TRACE+softSync → launch → Sync 循环），看 SIM 是否 SynchronizeStream 挂死。不对正确性。
【图谱】/workspace/docs/rg-decrypt-fused.yaml（只读）
【必读节点】Q-TOY-MULTI-LAUNCH；F-TOY-FUSED-SKEL-SIM-PASS；D-RG-SIM-PRIMARY；J-FAIL-SYNCALL-ON-AIC-WAIT（勿改握手）
【禁止】改 yaml；push/分支/commit；改 SoftSync/GATE/NTT 握手语义；改 stable；并行 SIM
【要做】
  1. 只改 ascendc-tests/pass-toy-decrypt-fsm-fused-skel1/（Host + 必要注释/run.sh 头）
  2. 环境变量 TOY_LAUNCH_REPEAT，默认 16；每轮：H2D 清 TRACE 与 softSyncGm → launch 全骨架 → Sync → 打印 TRACE
  3. 对照 pass-toy-encrypt-fsm-l18-skel1 的 TOY_LAUNCH_REPEAT 写法（活跃 toy）
  4. 验收：TOY_LAUNCH_REPEAT=16 SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4
     PASS=16 轮均 synchronize done 且退出码 0
  5. KERNEL_COMPUTE_BUDGET_SEC 提到 600（16 轮）
  6. 更新 STATUS.md；INDEX.md 该行备注多 launch；本文件 §反馈；日志 /opt/cursor/artifacts/DGT-20260903-4-sim.log
【不要做】新开目录；第二进程外层（那是下一刀若需要）
【WALL_CLOCK_BUDGET】40 分钟
【MAX_SIM_ATTEMPTS】2
【DONE_DEFINITION】16 轮 SIM 结束或明确挂死记第几轮+TRACE；未改 yaml
【ABORT_IF】编译失败 2 次；超 40min；改图谱/push
【反馈】填下方模板
```

---

## 反馈（Subagent 填写）

```text
【任务 id】DGT-20260903-4
【SIM 命令】TOY_LAUNCH_REPEAT=16 SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4
【结果】PASS
【挂点】无；iter=0..15 共 16 轮均 FULL fused sync done；exit=0；wall_sec≈22.847；Total tick=181191
【产物路径】/opt/cursor/artifacts/DGT-20260903-4-sim.log ；STATUS.md ；INDEX.md 该行已注多 launch
【是否改了图谱】否
【是否 push】否
【建议主控关注】同进程×16 无 SynchronizeStream 挂死 → Q-TOY-MULTI-LAUNCH 本骨架下暂否；若仍疑跨进程/外层加压才需下一刀第二进程
```
