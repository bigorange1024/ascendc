# EP03 — Encaps 设备哈希（可后置 · SIM-only）

> 目录：`graph-tests/enc_cann_ntt/EP03-encaps-device-hash/`  
> **可后置**：若本战役时间紧，可用 Host SHA3 对齐并在 FEEDBACK 标 `DEFERRED_HOST`；强完成不阻塞于本刀设备化。

## 目标

设备侧 `H(ek)` / `G` vs Host max=0（若做）。

## 验收

cpu+SIM；hash max=0；或书面 DEFERRED。
