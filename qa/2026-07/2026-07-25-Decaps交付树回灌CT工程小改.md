# 2026-07-25 — Decaps 交付树回灌 CT 工程小改

关键字：`Decaps` · **无 `-ct`** · `verify || exit` · **中文注释** · `M_FILE` · **未改** `decaps_1session`

## 决策

将 CT 专题（`-ct`）中**值得回灌**、且**不改算法/不改 SIM 默认**的小改落到 main 交付三树：

| 目录 |
|------|
| `ascendc-tests/pass-probe-f203-alg21-kem-decaps-device-k4` |
| `examples/incubating/exp-fips203-mlkem-kem-decaps-k4` |
| `examples/stable/stable-fips203-mlkem-kem-decaps-k4` |

**customspec（examples）**：仍以各目录既有 `*-实现方案-customspec.*` 为准；本轮**无**形状/launch/session 语义变更。

**未搬**：CT 默认 `decaps_2session`（相对 T2 定论为回退）。

## 变更

1. `run.sh`：`verify_kem_decaps.py || exit $?` + 验收注意（`M_FILE` / 勿并行 SIM）
2. `scripts/gen_data*.py` / `verify_kem_decaps.py`：文档头与分段注释（Gate E3 / `M_FILE`↔`golden_v`）
3. 自研面 `kem/` + `main_kem_decaps*` + `data_utils.h`：按 CT 注释密度回灌，措辞改为交付 **1-session**
4. `STATUS.md`：追加「工程回灌 2026-07-25」节

## 证据

- 默认仍 `export ASCENDC_SIM_HOST_MODE=decaps_1session`
- 逻辑与回灌前同构（注释/门禁为主）；CPU/SIM 全量复验可作为后续可选门禁

## 证据（同日）

| 项 | 结果 |
|----|------|
| `stable-…-kem-decaps-k4` CPU | `[verify] PASS` / `[SUCCESS]` |
| 同目录 `SIM_DIRECT=1` sim | `[verify] PASS`；E tick **763966**；根无 stray dump |
| SIM 默认 | 日志 `sim_2session=0`（1-session） |

