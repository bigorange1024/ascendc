# 2026-07-26 — 教材第7章流程图与对照图

关键字：第7章 · TikZ · **ml-kem-1024 迁移** · **768 P0+P1 落地** · **W2 D13/D14/D15 全绿** · 冒烟绿 · 幽灵 none

## 决策

用户：第7章缺流程图；对比倾向用图。只改教材插图与少量引导句，不改判决结论。

## 变更

`docs/research/…教材草案.tex` `sec:forward` 增图：

| 图 | 内容 |
|----|------|
| `fig:ch7-overview` | 三件工具 → CT → 补洞 → 写码 → 验收 → 判决 |
| `fig:ch7-dep` | Decaps 多父依赖 + Forbidden |
| `fig:ch7-fill` | 补洞流水线 |
| `fig:ch7-timeline` | 文书/代码时间线 |
| `fig:ch7-lanes` | A/B/C 三条过程泳道 |
| `fig:ch7-metrics` | 五指标条形对照 |
| `fig:ch7-tick` | B/C SIM tick 条形 |

B/C 细表缩短为脚注级；对比以图为主。PDF 已 `xelatex-clean` 通过。

## 图 7.2 精简（同日）

按用户意见：`fig:ch7-dep` 去掉对拍用 KeyGen/Encaps、correctness vendor（Forbidden）等与实现无关节点；只保留 Decrypt / Encrypt / Keccak → $G$ / FO → Decaps。

## 图示原则修订（同日续）

用户：前两图用折线/曲线去交叉；单线串行不必画流程图；要画就想清楚非图表难表达什么（泳道关联 / 欧拉重叠 / 时序依赖）；目录用概念分层、勿写死真路径。

| 图 | 改法 |
|----|------|
| `fig:ch7-dep` | 改自左向右分层（模块→接缝→目标）；正交折线，无斜穿 |
| `fig:ch7-fill` | FO 后共用竖段再左右折到合法/拒绝；正文点明图意在分叉 |
| `fig:ch7-timeline` | 删单线时间轴 → 概念目录树（非真路径） |
| `fig:ch7-lanes` | 删三平行串行 → 泳道 + 禁抄/复用/共享终态跨道关系 |
| `fig:ch7-metrics` | 轴名改中性量（搜索空间/整路返工/资源空转/禁源依赖/前置文书）；开销雷达：越大=该量越大，面积大=负担重 |

## 写作指导落盘（同日）

新建 [`docs/research/教材与调研文档写作指导.md`](../../docs/research/教材与调研文档写作指导.md)：归纳文风、图示、指标偏好；约定大版本后同步更新。当前教材叙事思路可暂保留。

## WSL 复现 Cloud liboqs roundtrip：main vs CT Decaps（同日续）

对照 Cloud（[`2026-07-25` 纪要](2026-07-25-Decaps交付树回灌CT工程小改.md)：`CPU_TRIALS=5 SIM_TRIALS=1`；只换 `DECAPS_DIR`）。HEAD=`9de1ebf`；本机 Mem **3.7 Gi**。日志：`/tmp/wsl_kem_rt/`。

| 组 | 条件 | 结果 |
|----|------|------|
| **A** main `…-decaps-k4` | Cloud 同令 | **CPU×5 + SIM×1 PASS**（EXIT=0）；`…/20260726_161047_8145` |
| **B** CT 首次连续 RT | 紧接 A 后 | CPU `cpu_t1`：**FAIL** `K(decaps) max=227`（`m'`≠`m`，假拒）；同 fixture **单独**再跑 Decaps CPU → **PASS** |
| **B2** CT 清树重测 | 删 CT `build/input/output` 后同令 | **CPU×5 PASS**；SIM 在 KeyGen+Encaps 后进 Decaps Phase-D（`decaps_2session`）→ **segfault EXIT=139**（dmesg：`ascendc_kem_decaps_bbit` signal 11）；`…/20260726_170031_15443` |
| **B3** CT 单独 Decaps SIM | 用 B2 `sim_t1` fixture + 已有 KG/ENC 输出；默认 `2session` | **PASS**（K max=0；tick Phase-E≈**754886**） |

### 解读

| 项 | 说明 |
|----|------|
| main 交付树 | WSL **能**对齐 Cloud 端到端 RT |
| CT 方法论树 | **正确性**在 WSL 可绿（CPU×5；单独 SIM 绿）；**连续** KeyGen→Encaps→CT-Decaps SIM 在 3.7 Gi WSL 上 fragile（segfault / 偶发假拒），**不**据此判 CT 算法错 |
| 与 Cloud 差 | Cloud 报告 CT CPU×5+SIM×1 全绿；WSL 连续 SIM 未跑通端到端，单独 Decaps SIM 可补证 |

命令（与 Cloud 相同）：

```bash
CPU_TRIALS=5 SIM_TRIALS=1 bash scripts/stable_kem_liboqs_roundtrip.sh
DECAPS_DIR=$PWD/examples/stable/ml-kem/ml-kem-1024/stable-fips203-mlkem-kem-decaps-ct-k4 \
  CPU_TRIALS=5 SIM_TRIALS=1 bash scripts/stable_kem_liboqs_roundtrip.sh
```

## P0 分类复测（HANDOFF 照做；同日续）

按 [`AGENT_HANDOFF.md`](../../AGENT_HANDOFF.md) P0（`CPU_TRIALS=3 SIM_TRIALS=1`；HEAD=`69190b4`）。日志：`/tmp/wsl_kem_rt/{main_rt,ct_rt,p0_report}.log|txt`。

| 步骤 | 结果 |
|------|------|
| **2) 交付树** `…-decaps-k4` | **EXIT=0**；CPU×3 + SIM×1 全 PASS；fixture `…/20260726_175815_1481`；无 tcache/segfault |
| **3) 交付单独 Decaps** | **跳过**（连续 RT 已绿） |
| **4) CT** `…-decaps-ct-k4` | **EXIT=0**；CPU×3 + SIM×1 全 PASS；fixture `…/20260726_184309_6528`；含 reject；无 segfault |
| **4b) CT 单独 Decaps** | **跳过**（连续 RT 已绿） |

### 分类结论（本轮）

| 树 | 连续 SIM | 记法 |
|----|----------|------|
| 交付（`1session`） | **绿** | 本轮未触发宿主机崩 |
| CT（`2session`） | **绿** | 本轮未复现此前 segfault；结合早先「连续红 / 单独绿」仍归 **宿主机偶发**，**非**算法回归 |

**未**改 `stable_kem_liboqs_roundtrip.sh` / stable 核。


## 工程结构：迁入 `ml-kem/ml-kem-1024`（同日续）

用户确认：768 方法论按前提 1–3（不做 4）之前，先改工程结构。

### 决策

| 项 | 结论 |
|----|------|
| 新建路径 | `ascendc-tests/ml-kem/ml-kem-1024/` · `examples/incubating/ml-kem/ml-kem-1024/` · `examples/stable/ml-kem/ml-kem-1024/` |
| 迁入 | 当前活跃 ML-KEM 探针 / `exp-*` / `stable-*`（**frozen 不搬**） |
| 本层保留 | `add_custom`、`pass-shake*`、`pass-toy*`、`pass-merged-kyber*`；`frozen/` 原地 |
| 索引 | 顶层 INDEX 改枢纽；详表落在各 `ml-kem/ml-kem-1024/INDEX.md`；预留 `ml-kem-768` 占位说明 |

### 变更摘要

- `git mv` 活跃用例入参数组目录；`scripts/` 默认 `KEYGEN_DIR`/`ENCAPS_DIR`/`DECAPS_DIR` 等已指新路径
- 软链 / `run.sh` `REPO_ROOT` 按深度修正（优先 walk-up 或 `REPO_ROOT` 绝对）
- 刷新 `README.md` 结构表、各级 `INDEX.md`、`frozen/INDEX.md` 继任链接
- CT customspec 内错误 `pass-probe` 路径改为 `pass-fix` 新路径并重编 PDF

### 下一刀

开 **ML-KEM-768** 方法论实验（P0 参数卡+CT → … → exp 层 KEM 三算子）；目录将落在各树 `ml-kem/ml-kem-768/`（尚未创建）。

## 迁移后验收：冒烟 + 幽灵清扫（同日续）

**目标**：确认 (1) 迁入 `ml-kem/ml-kem-1024/` 后用例可跑；(2) 无幽灵目录。

### 路径修复（阻塞 → 已修）

嵌套加深后固定 `parents[N]` / 相对软链失效；已改为 walk-up 仓库根（`AGENTS.md`+`scripts/`），并修正：

- Decaps `run.sh`：`host_golden` / `thirdparty` → `${REPO_ROOT}/examples/stable/ml-kem/ml-kem-1024/…`
- device encaps/decaps：`STABLE_ENC` / `prepare_dec_shim` 补 `ml-kem/ml-kem-1024`
- CMake：`REPO_ROOT` / `SHARED_ROOT` walk-up
- 清理误插未用 helper；修 `golden_encrypt_prep.py` 损坏行

提交：`96c0a23`（`fix: ml-kem 嵌套后 REPO/SHARED 路径 walk-up`）。

### 幽灵

| 检查 | 结果 |
|------|------|
| `bash scripts/cleanup-ascendc-test-ghosts.sh` | **`[cleanup-ghosts] none`**（迁移时已删根下 `fix-f203-alg{19,20,21}-*-correctness-k4` 空壳） |
| 旧扁平活跃路径 | **不存在**（`ascendc-tests/pass-fix-*`、`examples/*/exp|stable-fips*`） |

### 冒烟 `/tmp/mlkem_migrate_smoke3/`（CPU + `SIM_DIRECT=1` sim）

**`TOTAL_FAIL=0`**；用例根无 stray dump。

| 用例 | CPU | SIM |
|------|-----|-----|
| `add_custom` | 绿 | 绿 |
| `pass-fix-…-vec-k4-v2` | 绿 | 绿 |
| stable PKE keygen/encrypt/decrypt | 绿 | 绿 |
| stable KEM keygen/encaps/decaps/decaps-ct | 绿 | 绿 |
| device alg19/20/21(+ct) | 绿 | 绿 |
| `exp-fips203-compress-unified-int-vec-k4` | 绿 | 绿 |
| `stable_kem_liboqs_roundtrip`（CPU×1+SIM×1） | — | **绿**（`EXIT=0`） |

### 结论

迁移路径修复后**有条件完成**（代表性全量 CPU+SIM + roundtrip）；可开下一刀 768 方法论。

## ML-KEM-768 从 0→exp 计划草案（同日续）

产出调研草稿（**未写码**）：[`docs/research/MLKEM-768-从0到exp完整实现计划.md`](../../docs/research/MLKEM-768-从0到exp完整实现计划.md)。

- 终点：incubating KEM 三算子（`-k3`）；不做零垫、本阶段不做 stable
- 自补必建：W0 编码压缩 → W1 SampleNTT+polyvec6 NTT → W2 PKE device → W3 KEM device(+ct) → W4 exp
- 待用户拍板：分核选项 T-B 默认、是否保留 D19–D21、CT、跳过 PKE exp 等（计划 §9）

## ML-KEM-768：P0+P1 落地（同日续）

用户拍板后完成 **P0+P1**（仍无 kernel）：

| 决议 | 内容 |
|------|------|
| 1 | T-B polyvec6 + Â prep |
| 2 | 保留 D19–D21 |
| 3 | 要求 reject/CT |
| 4 | PKE exp 也做 |
| 5 | `-k3` |
| 6 | 本轮只做 P0+P1 |

### 产出

- 参数卡：`docs/specs/fips203-mlkem768-parameter-card.md`
- P1 表：`docs/specs/fips203-mlkem768-p1-gap-and-cases.md`
- registry×6 草稿；`scripts/check_mlkem768_sizes.sh`（liboqs 宏 OK）
- 目录壳：`ascendc-tests/ml-kem/ml-kem-768/`（13）· `examples/incubating/ml-kem/ml-kem-768/`（7，含 PKE×3+KEM×3+ct）
- **未**建 `examples/stable/ml-kem/ml-kem-768/`

下一刀：授权后 P2/W0。

## ML-KEM-768：P2/W0 积木实现与验收（同日续）

用户授权「开始」后完成 **W0/B1–B3**（仅 `ascendc-tests`，无 incubating 写码）。

| ID | 目录 | CPU | SIM | 备注 |
|----|------|-----|-----|------|
| B1 | `pass-fix-f203-compress-decompress-du10-dv4-k3` | PASS | PASS | d∈{4,10} 编排；tick 见 STATUS |
| B2 | `pass-fix-f203-byteencode-decode-d-k3` | PASS | PASS | encode/decode + encode12；修 decode→`../encode`、encode12 walk-up→1024 v2 |
| B3 | `pass-fix-f203-alg8-cbd-eta2-k3` | PASS | PASS | polyvec6 / AIV 分片 `{0,1,3}`\|`{2,4,5}`；SIM tick **14949** |

### 路径坑

- 嵌套后 decode golden 曾指向扁平 sibling `pass-f203-byteencode-d-vec-k4` → 改为同树 `encode/`
- encode12 复用 v2 gen_data：不可写相对 `../..` sibling；walk-up 至 `ml-kem/ml-kem-1024/.../vec-k4-v2`
- encode12 **刻意**保留 k4 几何作 d=12 算法探针；k=3 打包留给 KeyGen

### 下一刀

P2/W1（B4–B6）；须再授权。incubating-768 仍禁无 customspec 写码。

## ML-KEM-768：P2/W1 积木实现与验收（同日续）

用户「继续下一步」后完成 **W1/B4–B6**（仅 `ascendc-tests`；无 incubating 写码）。参数卡 §3.1 tiling 已锁并遵守。

| ID | 目录 | CPU | SIM | SIM tick |
|----|------|-----|-----|----------|
| B4 | `pass-fix-f203-alg7-sample-ntt-k3` | PASS | PASS | **80783**（默认 (0,0)；CPU 全矩阵另绿） |
| B5 | `pass-fix-f203-stage123-ntt-intt-polyvec6-k3` | PASS ntt+intt | PASS ntt+intt | NTT **26651** / INTT **26672** |
| B6 | `pass-fix-f203-alg11-12-multiply-inner-k3` | PASS mul+inner | PASS mul+inner | multiply **9416** / inner **21881** |

### 实现要点

- B4：derand / G 内 `k=3`；设备 derand 字符须为 `'3'`（曾误留 `'4'`）
- B5：自活跃 polyvec8 改 `kK=6`、连续 AIV 3+3；Cube pad≠假 poly
- B6：inner `blockDim=2` 写 **2+1**；SIM 链接勿在 host 头放双用 `GetPRange`（已内联 Init）

### 下一刀

P2/W2（D13–D15）；须再授权。incubating-768 仍禁无 customspec 写码。

## ML-KEM-768：W0/W1 SIM tick 登记（同日续）

用户要求先记性能再继续。已写入 [`qa/active_sim_regress_summary.md`](../../active_sim_regress_summary.md)「ML-KEM-768（k=3）积木 — W0 / W1」，并回填 B4–B6 `STATUS.md` tick 表。

| ID | 默认 SIM tick |
|----|----------------|
| B1 compress/decompress | d4: 3196/3304；d10: 3420/3247 |
| B2 encode/decode(+12) | 5423/9351；6539/6572；encode12 17511 |
| B3 CBD η=2 | **14949** |
| B4 SampleNTT | **80783** |
| B5 NTT/INTT | **26651** / **26672** |
| B6 multiply/inner | **9416** / **21881** |

口径：Cloud / `SIM_DIRECT=1` / 910B4 / `Total tick`；非 msprof；非生产性能门禁。

## ML-KEM-768：P2/W2 D13 KeyGen device（同日续）

用户授权实现 `ascendc-tests/ml-kem/ml-kem-768/pass-fix-f203-alg13-device-keygen-k3/`，要求从活跃 k4 KeyGen 复制并按参数卡 §3.2 改成 k3；未使用 `**/frozen/**` 源码。

| 项 | 结果 |
|----|------|
| 几何 | Â `[9,256]`（5+4），`s‖e [6,256]`，S0 `[12,256]`，mat_c `[48,128]`，Inner `P_OUT=S_VEC=3`（AIV 2+1） |
| I/O | `ek_pke=1184B`，`dk_pke=1152B`；ByteEncode12 `3×384` 后 fuse `ek‖ρ` |
| CPU | `KEYGEN_VERIFY=1 bash run.sh -r cpu -v Ascend910B4` → **PASS**（ek/dk golden） |
| SIM | `KEYGEN_VERIFY=1 SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` → **PASS**，Total tick **373426**，Model RUN TIME **64139.8 ms** |
| stray | 用例根无 `core*.dump` / `profile_*_log*.toml` / `OPPROF_*` |

实现修复要点：prep 的 `aHatQue` 原按 PRF Y `[6,128]` 分配 768B，复用为 `â[256] int32` 时 CPU 报 UB 越界；改为按 `max(PRF_Y, 1024B)` 分配，不改变任何 GM 锁定形状。

下一刀：D14 Encrypt / D15 Decrypt device（仍按参数卡 §3.2，D14 2 launch、D15 fused 1 launch）。

## ML-KEM-768：P2/W2 D14 Encrypt device（同日续）

用户授权实现 `ascendc-tests/ml-kem/ml-kem-768/pass-fix-f203-alg14-pke-encrypt-device-k3/`，要求从活跃 k4 Encrypt device 复制并按参数卡 §3.2 改成 k3；未使用 `**/frozen/**` 源码，未改锁定参数。

| 项 | 结果 |
|----|------|
| 几何 | prep：Â `[9,256]`（5+4）+ `r‖e1‖e2 [7,256]`；compute：NTT(r) k=3，INTT **batch=4**（`u0,u1,u2,v`），S0 `[8,256]`，mat_c `[32,128]`，AIV 连续 2+2 |
| I/O | `ek_pke=1184B`，`m=32B`，`coins=32B`，`c=1088B`（c1=960 d10，c2=128 d4） |
| CPU | `bash run.sh -r cpu -v Ascend910B4` → **PASS**（`[cmp] c max=0`） |
| SIM | `SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` → **PASS**，Total tick **507605**，Model RUN TIME **95781.9 ms** |
| stray | 用例根无 `core*.dump` / `profile_*_log*.toml` / `*.dump` |

实现修复要点：golden 改为本目录自生成 k3 `ek/m/coins/c`；PRF/CBD 改 7 poly；tail pack 改 d10/d4；INTT 不再沿用 k4 `kInttBatch=8`，而是 batch4 真 polyvec4。

下一刀：D15 Decrypt device（按参数卡 §3.2 fused 1 launch，复用 D14 c 布局与 d10/d4 unpack）。

## ML-KEM-768：P2/W2 D15 Decrypt device（同日续）

用户授权实现 `ascendc-tests/ml-kem/ml-kem-768/pass-fix-f203-alg15-pke-decrypt-device-k3/`，要求从活跃 k4 Decrypt device 复制并按参数卡 §3.2 改成 k3；未使用 `**/frozen/**` 源码，未改锁定参数。

| 项 | 结果 |
|----|------|
| 几何 | 单 fused launch，host `blockDim=1`；NTT/INTT k=3，AIV 2+1；`ŵ` pad 到 k=3 polyvec 前缀后复用 Stage123 |
| I/O | `dk_pke=1152B`，`c=1088B`（c1=960 d10，c2=128 d4），`m=32B` |
| CPU | `bash run.sh -r cpu -v Ascend910B4` → **PASS**（`[verify] PASS max=0 (32 bytes)`） |
| SIM | `SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` → **PASS**，Total tick **222032**，Model RUN TIME **34228.8 ms** |
| stray | 用例根无 `core*.dump` / `profile_*_log*.toml` |

实现修复要点：`dk/c` 尺寸、`du/dv`、unpack Decompress 偏置/shift、host golden、NTT/INTT tiling 与 AIV 分片均 retarget 到 k3；`vendor_sync.sh` 改为自包含检查，避免后续运行时把 k4 源覆盖回 k3。

### W2 结论

D13 KeyGen、D14 Encrypt、D15 Decrypt 已全部 CPU + `SIM_DIRECT=1` sim 绿；`qa/TODO.md` 关闭 **T768-W2**。本轮尚未接 D14→D15 roundtrip 脚本，独立 device golden 验收已满足本任务最小口径。

## ML-KEM-768：P2/W3 D19 KEM KeyGen device（同日续）

用户要求实现 `ascendc-tests/ml-kem/ml-kem-768/pass-fix-f203-alg19-kem-keygen-device-k3/`，从活跃 k4 D19 结构出发，并复用活跃 D13 k3 PKE 几何；未使用 `**/frozen/**` 源码，未改参数卡 §3.3 锁定值。

| 项 | 结果 |
|----|------|
| Launch | **2**：prep AIV_ONLY `blockDim=2` → compute MIX `blockDim=1`，Alg.16 尾内嵌，无第 3 launch |
| I/O | `ek_kem=1184B`，`dk_kem=2400B = dk_pke(1152)‖ek(1184)‖H(ek)(32)‖z(32)` |
| PKE 几何 | 复用 D13：Â `[9,256]`、polyvec6、Inner 2+1、ByteEncode12 `3×384` |
| CPU | `bash run.sh -r cpu -v Ascend910B4` → **PASS**（`ek_kem`/`dk_kem` max=0） |
| SIM | `SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` → **PASS**，Total tick **510775**，Model RUN TIME **78785.6 ms** |
| stray | 用例根无 `core*.dump` / `profile_*_log*.toml` / `*.dump` |

实现修复要点：`scripts/liboqs_kem_ref` 仍固定 1024，D19 k3 golden 改为复用 D13 k3 PKE oracle 后按 KEM 展开布局拼接；设备侧 `z` 前缀改为 `exp-mlkem-f203-kem-k3:SEED_Z=`；旁路 A 的 prep 改为 CBD 6 poly，禁止沿用 k4 8 行。

下一刀：D20 Encaps device，按参数卡 §3.3 接 D14 k3 Encrypt；当前未发现额外参数阻塞。
