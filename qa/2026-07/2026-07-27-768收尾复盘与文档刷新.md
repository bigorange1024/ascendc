# 2026-07-27 — ML-KEM-768 收尾确认 · 512 W2 D13/D14/D15 · 512 W3 D20/D21/D21ct · 正确性/性能汇报 · 文档刷新

关键字：**768 W0–W4 + glue 有条件完成** · AscendC-only roundtrip · 正确性/性能汇报 · 实现过程复盘 · **教材第8章** · **P/W 分列** · **512 计划草案** · **512 W1 全绿（B4/B5/B6）** · **512 W2 D13/D14/D15 PASS** · **512 W3 D20/D21/D21ct PASS** · 清理本地旧分支 · 文档/索引刷新

## 决策 / 确认

- 用户确认：768 主线（探针 W0–W3 + incubating W4 + glue）已按休息前一并授权完成。
- **本阶段仍不建** `examples/stable/ml-kem/ml-kem-768/`（须日后 `#交付#`）。
- 清理本地旧分支 `cursor/ntt-onnx-pat-retry-5334`（远端已 gone）；工作分支仍为 `research/formal-lang-dag` · PR [#12](https://github.com/bigorange1024/ascendc/pull/12)。

## 完成口径（有条件完成至 incubating）

| 层 | 状态 |
|----|------|
| 探针 W0–W3 | CPU + `SIM_DIRECT=1` sim 全绿（含 D21ct accept/reject） |
| incubating W4 | E13–E15、E19–E21ct：均有 customspec；CPU+SIM 全绿 |
| glue | 六份 `fips203-mlkem768-*-baseline-registry` 已按绿线补登记；[`scripts/exp_kem768_liboqs_roundtrip.sh`](../../scripts/exp_kem768_liboqs_roundtrip.sh) **AscendC-only** CPU×1+SIM×1 PASS（含 reject spot-check） |
| tick 登记 | [`qa/active_sim_regress_summary.md`](../active_sim_regress_summary.md) |

### Roundtrip 说明

- 有 RT：`bash scripts/exp_kem768_liboqs_roundtrip.sh`
- 名带 liboqs，**实为 AscendC-only**（仓内 liboqs helper 仍硬编码 1024）；非 liboqs↔AscendC 交叉。

## 正确性与性能（汇报摘要）

**正确性**：各用例 I/O 对拍 max=0；CT reject=`J(z‖c)` 且 ≠ accept；AscendC KEM 闭环绿。  
**未做**：liboqs-768 交叉 KAT、NPU、stable。

**性能（SIM Total tick，Cloud/910B4，非 msprof）**：

| 类 | 代表 tick |
|----|-----------|
| D13/D14/D15 | 373426 / 507605 / 222032 |
| D19/D20/D21 | 510775 / 592129 / 818285 |
| D21ct accept/reject | 826458 / 823002 |
| E* | 与对应 D* 同量级（见 incubating INDEX） |

相对同结构 k4 device 约低 **18%–31%**（维数变小符合预期）；作登记对照，非优化 SLA。

## 实现过程复盘（摘要）

- **有效路径**：参数卡锁定（T-B / §3.1–§3.3）→ 波次授权 → 积木→device→exp→glue。
- **关键成功因素**：禁零垫；奇数维分核写死（inner 2+1、polyvec6、INTT batch4、Â[9]）；只从活跃 INDEX 复制，不碰 frozen。
- **典型坑**：嵌套路径、derand 字符 `'4'` 残留、`GetPRange` host/device 双用符号、exp SIM 缺本地 multiply include、roundtrip 名实分离。
- **债务（可选）**：liboqs-768 helper、device KAT、D14↔D15 PKE RT、stable-768。

## 文档刷新（同日）

刷新：`AGENT_HANDOFF.md`、`AGENTS.md`、`README.md`、`qa/TODO.md`、本纪要与月/根 INDEX、`docs/research/MLKEM-768-…计划.md`、`docs/INDEX.md`、`docs/specs` 参数卡/P1/INDEX、`examples/` 与 `ascendc-tests/` 各级 INDEX。

## 教材第8章（同日续）

用户要求把 768「分析→计划→实现/测试/修改→结果」完整复盘写入教材。

| 项 | 说明 |
|----|------|
| 落点 | `docs/research/从已验证能力到合法派生-面向Agent预研的形式方法教材草案.tex` 新节 `\label{sec:mlkem768}`（阅读地图称第8章） |
| 内容 | 为何当第二试金石；相对 1024 补缺；参数卡/决议/波次；W0–W4+glue；典型坑与改法；正确性/tick；与第7章对照；有条件完成判决 |
| 图 | 总览、波次依赖、device tick 条形；PDF 已 `xelatex-clean.sh` |
| 同步 | `docs/research/INDEX.md`、讨论纪要待办/修订记录、写作指导修订记录 |

## 教材第8章题外话（同日续）

用户要求把配额/墙钟体感写进第8章「题外话」：

- 对照：昔日公司/家里 Agent 各做 Encaps correctness ≈ 各 1 天，合计吃掉约 2/3 月配额；昨晚 768 计划→测完约 41%→55% 配额、墙钟 <4h。
- 写法：标明**非对照实验、不作倍数宣称**；点明省的是无效派生；与 correctness 标本、第7章臂 A 连读。
- 节标：`sec:mlkem768-aside`；PDF 已重编。

## 领导汇报简报（同日续）

产出 Beamer（无封面、**14 页**）：[`docs/reports/Agent预研形式方法-方法论精髓与效果-简报.{tex,pdf}`](../../docs/reports/Agent预研形式方法-方法论精髓与效果-简报.pdf)。

- 受众：数学博士 / 密码专家；不讲 PQC 与基础数学。
- **再修订**：每页标题改为「主题：…」；页末固定「本页价值：…」一句，点明该页作用。

## 纪要更名（同日续）

`docs/research/形式语言DAG预研讨论纪要.md` → `形式语言与自动机预研讨论纪要.md`；已同步 `docs/research/INDEX.md`、教材草案两处指针、五指标对照文。

## 领导汇报反馈（同日续）

领导汇报后意见：方法论展示方式需改进；领导用 DAG 网页展示研究与解决问题过程，思路较直观。已在 `qa/TODO.md` 新增 **T-WebViz**（待规划：探索用网页更直观展示依赖 DAG、门禁流、闭包演化等）。

## 文档：P 与 W 分列（同日续）

- 用户指出：`P2-W0` / `P2/W0` 合并写法易与方法论 **P0–P3** 混淆。
- **决议**：权威用例表 **P、W 分两列**；**P**=方法论阶段，**W**=P2/P3 内工程子波次；映射见 [`fips203-mlkem768-p1-gap-and-cases.md` §0](../../docs/specs/fips203-mlkem768-p1-gap-and-cases.md)。
- 已刷新：P1 表、完整计划 §4.2/§5.2/§8、参数卡 §6/§8、两树 `INDEX`、教材第8章表、Beamer tick 表、部分 `STATUS.md`。

## ML-KEM-512：完整实现计划草案（同日续）

用户确认：512 做**更激进**的方法论压力床——单 AI Core、参照 768 闭环、锁卡后尽量无介入推进至 incubating + liboqs KAT/RT + 文档刷新。

| 产出 | 路径 | 状态 |
|------|------|------|
| 完整计划 | [`docs/research/MLKEM-512-从0到exp完整实现计划.md`](../../docs/research/MLKEM-512-从0到exp完整实现计划.md) | **P0 决议已锁** |
| 参数卡 | [`docs/specs/fips203-mlkem512-parameter-card.md`](../../docs/specs/fips203-mlkem512-parameter-card.md) | **已锁** |
| 索引 | `docs/research` / `docs/specs` / `docs/INDEX` / 两树 `ml-kem/INDEX` / HANDOFF / TODO | 已挂 |

**已锁默认**：S-1 单 cube；T-B2 polyvec4；`-k2`；**CBD‑η3 为相对 768 的缺项**；D19–D21+CT；PKE exp；本阶段 **liboqs-512 交叉必达**；禁 stable-512。

## 用语统一：缺项 / 补缺（同日续）

用户纠正（两轮）：
1. 勿把「洞」当缺项同义词另立定义；
2. 过程名不叫「补洞」，应叫 **补缺**（与缺项对齐）。

| 旧 | 新 |
|----|-----|
| 补洞 / 最小补洞 / 补洞图 | **补缺** / **最小补缺** / **补缺图** |
| 洞 / 新洞 / 洞级 | **禁止**；对象一律称 **缺项** |

已刷新：512/768 计划与参数卡、768 P1、教材 `.tex`、领导简报 `.tex`、HANDOFF / TODO / INDEX / 当日纪要。

教材定义现为：**缺项**、**补缺**、**最小补缺**、**闭包**、**已获证**；「补缺图」= P1 缺项对照表。

## liboqs KEM 交叉胶水：参数组可切换（同日续）

用户要求先改 round-trip，服务后续 512/768 真交叉。

| 改动 | 说明 |
|------|------|
| `scripts/liboqs_kem_ref.c` | 改用 `OQS_KEM` 泛型；`--param` / `MLKEM_PARAM=512\|768\|1024` |
| `scripts/mlkem_param.py` + `liboqs_kem_fixture.py` | 长度表 + fixture 可切换 |
| `stable_kem_liboqs_roundtrip.sh` / `liboqs_kem_vs_ascendc.sh` | 透传 `MLKEM_PARAM` |
| `exp_kem768_liboqs_roundtrip.sh` | 默认仍 AscendC-only；`USE_LIBOQS=1` → 真 liboqs-768 交叉 |
| `smoke_liboqs_kem_params.sh` | **512/768/1024 fixture 自洽全绿**（无 AscendC） |
| 未改（仅改名） | PKE helper 仍仅 1024：`liboqs_pke_ref_mlkem1024.c`；本阶段不对其他参数组测 PKE |

## 768 真 liboqs 交叉 RT（同日续）

命令：`USE_LIBOQS=1 bash scripts/exp_kem768_liboqs_roundtrip.sh`（CPU×1 + SIM×1；incubating E19/E20/E21）

| 阶段 | CPU | SIM |
|------|-----|-----|
| KeyGen ek/dk vs liboqs | PASS max=0（1184/2400） | PASS max=0 |
| Encaps c/K vs liboqs | PASS max=0（1088/32） | PASS max=0 |
| Decaps accept + SSA | PASS | PASS |
| Decaps reject ≠ accept | PASS | PASS |

证据：`[SUCCESS] stable KEM ↔ liboqs … root=/workspace/output/exp_kem768_liboqs_rt`；墙钟约 **11 min**。

**下一刀**：落 512 P0 目录壳 / sizes → P1 → W0。

## liboqs_pke_ref 改名标明仅 1024（同日续）

用户：不对其他参数组测 PKE；`liboqs_pke_ref.c` 至少改名。

| 旧 | 新 |
|----|-----|
| `scripts/liboqs_pke_ref.c` | `scripts/liboqs_pke_ref_mlkem1024.c` |
| 产物 `liboqs_pke_ref` | `liboqs_pke_ref_mlkem1024`（构建后仍软链旧名） |
| `build_liboqs_pke_ref.sh` | 真构建：`build_liboqs_pke_ref_mlkem1024.sh`；旧名作转发入口 |

逻辑未改；encrypt/decrypt 链接债仍为 T18。

### 相关引用审计（同日续）

| 需要改 | 已做 |
|--------|------|
| `.gitignore` 补 `liboqs_pke_ref_mlkem1024` | ✓ |
| `AGENTS.md` / `qa/TODO` T18 / thirdparty / registry / notes | ✓ |
| 用例内同名 `build_liboqs_pke_ref.sh`（实为编 `keygen_ref`） | 活跃树改名为 `build_liboqs_pke_keygen_ref.sh` + kat 引用同步；头注释标明勿与仓根全 PKE 混淆 |
| `liboqs_pke_vs_ascendc.sh` 头注释标仅 1024 | ✓ |
| 旧日纪要（保留旧名正文 + 改名备注） | `qa/2026-06-29`、`2026-07-01`、`2026-07-10` 已加「改名备注（2026-07-27）」指向新名 |
| **不改** | `**/frozen/**` |
| **仍名 `liboqs_pke_keygen_ref`** | 用例内 KeyGen-only `.c`/二进制（本就不是仓根全 PKE helper） |


## ML-KEM-512：P0+P1 落地与开写授权（同日续）

用户：`$规格说明书$` + `【预研代码】` 授权；先推送后开写。

| 交付 | 状态 |
|------|------|
| `scripts/check_mlkem512_sizes.sh` | PASS（对拍 liboqs 宏） |
| `ascendc-tests/…/ml-kem-512/INDEX.md` | 探针表壳 |
| `examples/incubating/…/ml-kem-512/INDEX.md` | exp 表壳（写码须 customspec） |
| registry×6 | P0 骨架 |
| `docs/specs/fips203-mlkem512-p1-gap-and-cases.md` | **P1 定稿** |
| `sample_poly_cbd3` / `fips203_sample_poly_cbd3_row` | shared 补缺（对齐 liboqs cbd3 抽取） |

**下一刀**：W0 优先 B3b `pass-fix-f203-alg8-cbd-eta3-k2`；W4 前先写 customspec。

## ML-KEM-512：W0 B3b CBD-η3 探针 PASS（同日续）

新建 `ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg8-cbd-eta3-k2/`，不触碰 `examples/`，不使用 `frozen/`。锁定参数：k=2、η=3、ROWS=4、`prf_out[4,192]`、`src[4,256]`、AIV0 `{0,2}` / AIV1 `{1,3}`。Golden 使用 `golden_se_sampling.sample_poly_cbd3`，与共享 C `fips203_sample_poly_cbd3_row` 同语义。

验收：

| 命令 | 结果 |
|------|------|
| `bash run.sh -r cpu -v Ascend910B4` | PASS；`[verify] src[4,256] PASS (4096 bytes exact match)` |
| `SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` | PASS；`Total tick: 13566`；用例根无 stray dump |

索引已刷新：`ascendc-tests/ml-kem/ml-kem-512/INDEX.md` B3b CPU/SIM 标 PASS。


## ML-KEM-512：W0 B3b CBD-η3 探针绿（同日续）

目录：[`pass-fix-f203-alg8-cbd-eta3-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg8-cbd-eta3-k2/)

| 验收 | 结果 |
|------|------|
| CPU | PASS（`src[4,256]` exact） |
| `SIM_DIRECT=1` sim | PASS；**Total tick: 13566**；根无 stray dump |

Golden：`sample_poly_cbd3`；布局 polyvec4；AIV0 `{0,2}` / AIV1 `{1,3}`。

**后续同日**：B3a CBD-η2 已补齐，见下一节。

## ML-KEM-512：W0 B3a CBD-η2 探针绿（同日续）

目录：[`pass-fix-f203-alg8-cbd-eta2-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg8-cbd-eta2-k2/)

| 验收 | 结果 |
|------|------|
| CPU | PASS（`src[4,256]` exact；4096 bytes） |
| `SIM_DIRECT=1` sim | PASS；**Total tick: 11377**；根无 stray dump |

Golden：`sample_poly_cbd2`；锁定布局 `prf_out[4,128]` → `src[4,256]`；polyvec4；AIV0 `{0,2}` / AIV1 `{1,3}`。

**后续同日**：B1 Compress/Decompress 与 B2 ByteEncode/Decode_d 已补齐；下一刀转 W1。

## ML-KEM-512：W0 B2 ByteEncode/Decode_d 探针绿（同日续）

目录：[`pass-fix-f203-byteencode-decode-d-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-byteencode-decode-d-k2/)

| 子项 | SIM Total tick |
|------|----------------|
| encode d=4 | **5407** |
| decode d=4 | **9340** |
| encode d=10 | **6629** |
| decode d=10 | **6561** |
| encode12 d=12 | **17613** |

| 验收 | 结果 |
|------|------|
| CPU | PASS；encode/decode `max=0`，encode12 五项对拍 PASS |
| `SIM_DIRECT=1` sim | PASS；根无 stray dump |

说明：`encode12/` 仍复用 1024 Tag5T v2 的 k4 几何作为 d=12 **算法探针**；512 KeyGen 的真实 k2 打包留给后续 D13/KeyGen 锁定。索引已刷新：`ascendc-tests/ml-kem/ml-kem-512/INDEX.md` B2 CPU/SIM 标 PASS。


## ML-KEM-512：W0 全绿 B1/B2/B3a/B3b（同日续）

用户授权继续。W0 四探针均 **CPU + `SIM_DIRECT=1` sim PASS**（根无 stray dump）。

| ID | 目录 | SIM tick（摘录） |
|----|------|------------------|
| B1 | `pass-fix-f203-compress-decompress-du10-dv4-k2` | c4/c10=3156/3442；dec4/dec10=3236/3317 |
| B2 | `pass-fix-f203-byteencode-decode-d-k2` | enc4 5407 / dec4 9340 / enc10 6629 / dec10 6561 / enc12 17613 |
| B3a | `pass-fix-f203-alg8-cbd-eta2-k2` | **11377** |
| B3b | `pass-fix-f203-alg8-cbd-eta3-k2` | **13566**（先绿） |

**后续同日**：W1 B5 polyvec4 NTT/INTT 已补齐，B4/B5/B6 全绿，见下文。

## ML-KEM-512：W1 B4 SampleNTT k2 探针绿（同日续）

目录：[`pass-fix-f203-alg7-sample-ntt-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg7-sample-ntt-k2/)

| 验收 | 结果 |
|------|------|
| CPU 默认 `(j,i)=(0,0)` | PASS；`d1/d2/a_hat max_abs_diff=0` |
| `SIM_DIRECT=1` sim 默认 `(0,0)` | PASS；**Total tick: 80235**；根无 stray dump |
| CPU matrix | PASS；`(j,i)∈{0,1}²` 全 4 组 |

实现口径：仅从活跃 768 B4 探针参数化；锁定 `k=2`、`...-k2:SEED_D=`、`G(d||2)` 与 2×2 SampleNTT matrix；未建 `examples/` 或 stable-512。

## ML-KEM-512：W1 B6 MultiplyNTTs + Inner 探针绿（同日续）

目录：[`pass-fix-f203-alg11-12-multiply-inner-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg11-12-multiply-inner-k2/)

| 子项 | 锁定几何 |
|------|----------|
| `multiply/` | Alg.11/12 `MultiplyNTTs(f,g)` 单对；与 k 无关 |
| `inner/` | `P_OUT=S_VEC=2`，输出 `t_hat[2,256]` |
| AIV split | AIV0 → `t_hat[0]`，AIV1 → `t_hat[1]`；不沿用 k3 的 2+1，不使用 `P_OUT/2` 临时推导 |

| 验收 | 结果 |
|------|------|
| `bash run.sh -r cpu -v Ascend910B4` | PASS；multiply 256 系数 exact；inner `t_hat[2,256] max_abs_diff=0` |
| `SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` | PASS；根无 stray dump |

SIM Total tick：multiply **9290**；inner **12603**。

## ML-KEM-512：W1 B5 Stage123 NTT/INTT polyvec4 探针绿（同日续）

目录：[`pass-fix-f203-stage123-ntt-intt-polyvec4-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-stage123-ntt-intt-polyvec4-k2/)

| 锁定项 | 结果 |
|--------|------|
| polyvec | true polyvec4；无 zero-pad 到 6/8 |
| MIX | 生产 `blockDim=1`；单 cube；逻辑 `m=8`，Cube 硬件 pad 到 16，非假 poly |
| AIV split | 连续 `{0,1}` / `{2,3}`；每个 AIV 持有完整 poly hi+lo，无 limbsplit |
| Stage1–3 | 禁 `AscendC::Gather`；平面 `mat_c[32,128]` |

| 验收 | 结果 |
|------|------|
| `bash run.sh -r cpu -v Ascend910B4` | PASS；NTT `max=0` |
| `SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` | PASS；NTT **Total tick: 22921**；根无 stray dump |
| `F203_NTT_MODE=intt bash run.sh -r cpu -v Ascend910B4` | PASS；INTT `max=0` |
| `F203_NTT_MODE=intt SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` | PASS；INTT **Total tick: 22836**；根无 stray dump |

**W1 判定**：B4/B5/B6 均 CPU + `SIM_DIRECT=1` sim 绿；下一刀转 W2/W3 device。


## ML-KEM-512：W1 全绿 B4/B5/B6（同日续）

用户：只推 research，**不要**自动合 main；继续工作。

| ID | 目录 | 验收 |
|----|------|------|
| B4 | `pass-fix-f203-alg7-sample-ntt-k2` | CPU+SIM PASS；tick **80235**；2×2 matrix PASS |
| B5 | `pass-fix-f203-stage123-ntt-intt-polyvec4-k2` | ntt/intt CPU+SIM PASS；**22921** / **22836**；真 polyvec4 + S-1 |
| B6 | `pass-fix-f203-alg11-12-multiply-inner-k2` | multiply **9290** / inner **12603** |

至此 **W0+W1（B1–B6）全绿**。下一刀 **W2 D13–D15**。

## ML-KEM-512：W2 D15 PKE Decrypt device k2 PASS（同日续）

目录：[`pass-fix-f203-alg15-pke-decrypt-device-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg15-pke-decrypt-device-k2/)

| 锁定项 | 结果 |
|--------|------|
| I/O | `dk_pke=768B` + `c=768B` → `m=32B` only |
| 密文布局 | `c1=2×320B` (`d_u=10`)，`c2=128B` (`d_v=4`) |
| 几何 | S-1；NTT/INTT AIV0=poly0、AIV1=poly1；无 limbsplit、无 ZERO 填充；NTT S1–S3 禁 Gather |
| 来源 | 仅从活跃 768 D15 retarget；未用 `frozen/`，未写 `examples/`/stable-512 |

| 验收 | 结果 |
|------|------|
| `bash run.sh -r cpu -v Ascend910B4` | PASS；`[verify] PASS max=0 (32 bytes)` |
| `SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` | PASS；**Total tick: 168975**；根无 stray dump |

当前分支本地未见已提交 D14 k2；因此本轮未接 D14↔D15 roundtrip，按 D15 自包含 golden 完成。

## ML-KEM-512：W2 D13 PKE KeyGen device k2 PASS（同日续）

目录：[`pass-fix-f203-alg13-device-keygen-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg13-device-keygen-k2/)

| 锁定项 | 结果 |
|--------|------|
| I/O | `seed_d` + LUT → `ek_pke=800B`、`dk_pke=768B` |
| prep | Â `[4,256]`，双 AIV `2+2`；`s‖e=[4,256]`，Alg.13 中 s/e 均走 CBD-η3 |
| compute | S-1 `MIX blockDim=1`；polyvec4 NTT；`S0[8,256]`、平面 `mat_c[32,128]`；InnerProduct `1+1` |
| 打包 | ByteEncode12 `2×384=768`，`ek=ByteEncode12(t̂)‖ρ → 800` |
| 来源 | 仅从活跃 768 D13 retarget，并复用 512 W0/W1 绿线；未用 `frozen/`，未写 `examples/`/stable-512 |

| 验收 | 结果 |
|------|------|
| `KEYGEN_VERIFY=1 bash run.sh -r cpu -v Ascend910B4` | PASS；`ek_pke.bin` **800B**、`dk_pke.bin` **768B** golden PASS |
| `KEYGEN_VERIFY=1 SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` | PASS；**Total tick: 230102**；根无 stray dump |

## ML-KEM-512：W2 D14 PKE Encrypt device k2 PASS（同日续）

目录：[`pass-fix-f203-alg14-pke-encrypt-device-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg14-pke-encrypt-device-k2/)

| 锁定项 | 结果 |
|--------|------|
| I/O | `ek_pke=800B` + `m=32B` + `coins=32B` → `c=768B` only |
| 密文布局 | `c1=640B=2×320B` (`d_u=10`)，`c2=128B` (`d_v=4`) |
| prep | Â `[4,256]`；`r‖e₁‖e₂ = 2+2+1 = 5` poly；Encrypt noise 使用 CBD-η2，禁止零垫到 6/8 |
| compute | S-1 `MIX blockDim=1`；NTT(r) k=2；INTT 复用 polyvec4 物理槽 `u0,u1,v,空槽`；NTT/INTT S1–S3 无 Gather |
| 来源 | 从活跃 768 D14 retarget，并复用 512 W0/W1 绿块；未用 `frozen/`，未写 `examples/`/stable-512 |

| 验收 | 结果 |
|------|------|
| `bash run.sh -r cpu -v Ascend910B4` | PASS；`[cmp] c max=0`；`c=768B` |
| `SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` | PASS；**Total tick: 338153**；`[cmp] c max=0`；根无 stray dump |

调试记录：首轮 SIM 仅 `c2` 错（`c1` max=0），根因是 fused 路径中 `vOut` 由 AIV1 scatter，而原迁移残留让 AIV0 叠加 `e₂+μ`、AIV1 并行 pack c₂，存在跨 AIV 读写竞态。修复为：前缀 `e₂ += μ` 后 `SyncAll<isAIVOnly=true>`；AIV1 在本核完成 `vOut += e₂` 后再 pack c₂。

至此 W2 已绿 D13/D14/D15。


## ML-KEM-512：W2 全绿 D13/D14/D15（同日续）

用户授权继续；只推 research。

| ID | 目录 | SIM tick | I/O |
|----|------|----------|-----|
| D13 | `pass-fix-f203-alg13-device-keygen-k2` | **230102** | ek800/dk768 |
| D14 | `pass-fix-f203-alg14-pke-encrypt-device-k2` | **338153** | c768 |
| D15 | `pass-fix-f203-alg15-pke-decrypt-device-k2` | **168975** | m32 |

**W0–W2 全绿**。后续转 **W3 D19/D20/D21/D21ct**；其中 D20 见下一节。

## ML-KEM-512：W3 D20 KEM Encaps device k2 PASS（同日续）

目录：[`pass-fix-f203-alg20-kem-encaps-device-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg20-kem-encaps-device-k2/)

| 锁定项 | 结果 |
|--------|------|
| I/O | `ek_kem=800B` + `m=32B` → `c=768B` + `K=32B` |
| FO | 设备侧 `K‖coins = G(m‖H(ek))`；生产路径不依赖 host `coins.bin` |
| 几何 | S-1 / true k=2；复用 D14 k2 `Â[4]`、`re[5]`、INTT batch4；不补零 |
| 来源 | 仅从活跃 768 D20 retarget，并编译期引用活跃 D14 k2；未用 `frozen/`，未写 `examples/`/stable-512 |

| 验收 | 结果 |
|------|------|
| `bash run.sh -r cpu -v Ascend910B4` | PASS；`c.bin`/`K.bin` max_abs_diff=0 |
| `SIM_DIRECT=1 bash run.sh -r sim -v Ascend910B4` | PASS；**Total tick: 394978**；`c.bin`/`K.bin` max_abs_diff=0；根无 stray dump |

**W3 状态**：D20 已绿；D21/D21ct 见下一节；D19 未纳入本轮提交。

## ML-KEM-512：W3 D21 + D21ct KEM Decaps device k2 PASS（同日续）

目录：

- Delivery：[`pass-fix-f203-alg21-kem-decaps-device-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg21-kem-decaps-device-k2/)
- CT：[`pass-fix-f203-alg21-kem-decaps-device-ct-k2`](../../ascendc-tests/ml-kem/ml-kem-512/pass-fix-f203-alg21-kem-decaps-device-ct-k2/)

| 锁定项 | 结果 |
|--------|------|
| I/O | `dk_kem=1632B` + `c=768B` → `K=32B` |
| dk 布局 | `dk_pke[0:768)` ‖ `ek[768:1568)` ‖ `h[1568:1600)` ‖ `z[1600:1632)` |
| 默认 | D21=`decaps_1session`；D21ct=`decaps_2session` |
| 来源 | 仅从活跃 768 D21/D21ct retarget；Phase-D 编译期引用活跃 D15 k2，Phase-E 引用活跃 D14 k2 几何；未用 `frozen/`，未写 `examples/`/stable-512 |

| 探针 | accept CPU | accept SIM | reject CPU | reject SIM |
|------|------------|------------|------------|------------|
| D21 delivery | PASS，`K.bin` max=0 | PASS，D**163145**+E**408061**=**571206** | PASS，`K=J(z‖c)` | PASS，D**163109**+E**407438**=**570547** |
| D21ct CT | PASS，`K.bin` max=0 | PASS，D**163069**+E**407638**=**570707** | PASS，`K=J(z‖c)` 且 `reject≠accept` | PASS，D**163025**+E**408344**=**571369**，`reject≠accept` |

两树用例根均无 `core*.dump` / `profile_*_log*.toml` / stray `.dump/.log`。D21/D21ct 默认 fixture 用活跃 D14/D15 host oracle 组装 `dk_pke/ek`，再按锁定布局本地拼 `H(ek)` 与 `z`。

**W3 状态**：D20/D21/D21ct 已绿；D19 未纳入本轮提交，后续按 research 分支实际状态再处理。


## ML-KEM-512：P2 W3 全绿 D19–D21ct（同日续）

| ID | SIM tick（摘录） |
|----|------------------|
| D19 | **320247** |
| D20 | **394978** |
| D21 accept/reject | **571206** / **570547** |
| D21ct accept/reject | **570707** / **571369** |

至此 **P2（W0–W3）探针全绿**。下一刀 **W4**：先 `$规格$` customspec，再 incubating 写码；只推 research。

## 下一刀（可选，非门禁）

| 项 | 说明 |
|----|------|
| T768-post | liboqs-768 helper / device KAT / D14↔D15 RT |
| `#交付#` | 才允许建 stable-768 |
| 仓级 | T23 / T21 / T2-npu / 教材细表 |

## ML-KEM-512：P3 W4 incubating + glue（同日续）

### W4 customspec → 实现

- 七份 `*-实现方案-customspec.tex/.pdf`（E13–E15、E19–E21ct）已齐；行为基线 = 活跃 D13–D21ct k2；S-1 / T-B2 / I/O 800·768·1632·768。
- incubating 自活跃探针自包含移植（vendored `multiply/`；E20/E21 不再编译期依赖 `ascendc-tests` 路径）。
- **CPU + `SIM_DIRECT=1` sim 全绿**（含 E21/E21ct reject）；用例根无 stray dump。

### glue

- 新增 [`scripts/exp_kem512_liboqs_roundtrip.sh`](../../scripts/exp_kem512_liboqs_roundtrip.sh)。
- AscendC-only：**CPU×1 PASS**、**SIM×1 PASS**（含 reject spot-check）。
- Decaps `gen_data.py`：`C_SRC` 在场时用 host `Decrypt(dk,c)` 恢复 m 再写 `golden_v`（修 CPU 孪生 Phase-E roundtrip 假红）。
- liboqs-512 CPU：KeyGen ek/dk **max=0**；Encaps **K max=0**，**c max≠0** → glue **有条件完成**；下一刀查 Encaps 密文相对 liboqs 的缺项。

### 明确未做

- stable-512；自动合 main；liboqs Encaps c 全对齐。

## ML-KEM-512：T512 glue-c 补缺（同日续）

### 根因

- Encaps `K` 对齐、`c` 几乎全错 → `G(m‖H(ek))` 正确，Encrypt 错。
- 参数卡锁定 η1=3 / η2=2；实现曾把 Alg.14 `r‖e₁‖e₂` **5 行全走 η2**（从 768/1024 误迁）。

### 补缺

- `r`（nonce 0..1）：CBD η1=3，PRF 192B；`e₁‖e₂`：CBD η2=2，PRF 128B；GM PRF 行 stride 统一 192（XOF 前缀）。
- 同步：D14（行为基线）+ D20/D21/D21ct（编译期引用 D14）+ E14/E20/E21/E21ct；E14 customspec 已改并重编 PDF。

### 验收

| 项 | 结果 |
|----|------|
| D14 / E14 CPU+SIM | PASS；SIM tick ≈ **366k** |
| E20 CPU+SIM | PASS；SIM tick **427927** |
| `USE_LIBOQS=1` CPU×1 | KeyGen/Encaps **c+K**/Decaps accept+reject **全 max=0** |
| `USE_LIBOQS=1` SIM×1 | 同上全 PASS |

### 明确未做

- stable-512；自动合 main。
