# 2026-08-12 — main 拉取与冒烟 · decrypt SHARED_INC 修复

## 结论（状态词）

**完成（Cloud CPU+SIM）**：`main` 工具链可用；`acl_session` 缺 include 已修；**1024 stable×7** 均 `cpu` + `SIM_DIRECT=1 sim` 全绿。

规格依据：`examples/stable/…/stable-fips203-mlkem-pke-decrypt-k4-实现方案-customspec.tex`（允许 `library/shared`）。

## Git

- 分支：`main`（本修复已 commit/push）
- 改动点：三处 CMake 补 `SHARED_INC=${REPO_ROOT}/library/shared`

| 路径 | 说明 |
|------|------|
| `examples/stable/…/stable-fips203-mlkem-pke-decrypt-k4/CMakeLists.txt` | 主修复 |
| `examples/stable/…/kem-decaps-k4/decrypt/CMakeLists.txt` | 同构 vendor 副本 |
| `ascendc-tests/…/pass-…-alg15-pke-decrypt-device-k4/CMakeLists.txt` | 同源探针 |

## 根因

`main_decrypt_g4_run.cpp` 非 CPU 路径 `#include "acl_session/acl_session.hpp"`，CMake `_INCLUDES` 未含 `library/shared`。CPU 因 `ASCENDC_CPU_DEBUG` 跳过该头 → 假绿。

## 验收证据（本机 Cloud）

| 用例 | cpu | `SIM_DIRECT=1` sim |
|------|-----|---------------------|
| `stable-…-pke-keygen-k4` | PASS | PASS |
| `stable-…-pke-encrypt-k4` | PASS | PASS |
| `stable-…-pke-decrypt-k4` | PASS | PASS（`max=0`，tick≈283469） |
| `stable-…-kem-keygen-k4` | PASS | PASS |
| `stable-…-kem-encaps-k4` | PASS | PASS |
| `stable-…-kem-decaps-k4` | PASS | PASS |
| `stable-…-kem-decaps-ct-k4` | PASS | PASS |
| `ascendc-tests/add_custom` | PASS | PASS（早先冒烟） |

用例根无 stray `core*.dump` / `profile_*`。

## 环境备注

- `liboqs` 已编；`SHA3hp` / `ntt_onnx` 仍缺（不影响 1024 stable 本轮）
- incubating 同源 decrypt CMake **未改**（非本轮范围）

## 遗留

- 可选：incubating `exp-…-pke-decrypt` / `…/decrypt` 同步补 `SHARED_INC`
- Cloud Secrets：`ASCENDC_GH_PAT`（ntt_onnx / 其它私有仓）
