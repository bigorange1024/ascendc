# 2026-09-06 — Encrypt 卡死重写：纪律 + T01 PASS + 开 T02

关键字：**单刀限时** · **禁复踩坑** · **每刀前遍历 KB+DAG** · **T01 PASS** · **T02 生产 GATE 时序** · **主控不写核**

## 用户纪律（拍板）

- 单次实验不可过长；过长则反思复杂度/是否走错，勿傻等。
- 图谱已总结的失败路线禁止再来回试。
- 每安排下一刀前：沉淀 + 遍历知识库与图谱。
- SIM 能做的尽量做；真不会了再停下讨论。

## T01

- 目录：`graph-tests/toys/T01-mix-ntt13-handshake/`
- 结果：**PASS**（CPU + `SIM_DIRECT`）；SIM 可缺 401（X13）
- 教训入库：X11 冷装 CANN、X12 TPipe 作用域、X13 TRACE 噪声

## T02

- 目录：`graph-tests/toys/T02-prod-gate-timing/`
- 结果：**PASS**（CPU+SIM；主控复验 log exit0 / tick 40288）
- 要点：生产 GATE 时序 SIM 不挂；SIM 可缺 401（X13）

## T03（进行中）

- 目录：`graph-tests/toys/T03-full-fsm-ntt-gate-intt/`
- 目标：单 launch 闭环 NTT+生产 GATE+**INTT 复用 1/3**（禁 5/7）
- 墙钟 ≤25min；SIM ≤2

## 图谱/KB

- 台账已更；`D-EXP-T03` inactive，`F-T03-SIM-PASS`，`D-EXP-T04` active

## T03

- 结果：**PASS**（CPU+SIM）

## T04（进行中）

- 目录：`graph-tests/toys/T04-gate-volume-stress/`
- 目标：GATE 体量加压

## T04

- **PASS**；体量×10 仍绿 → **X14**（假循环加压不够）

## T05（进行中）

- `graph-tests/toys/T05-multi-launch-rounds/`：Host 2× launch 全 FSM

## T05

- **PASS**（2×launch）

## T06（进行中）

- GATE 真积木体量（禁 X14 空转）

## T06

- **PASS**（真 Vec MAC）

## T07（进行中）

- sampling 前置 → FSM；若再绿则停讨 NPU 战略

## T07 + 闸门

- T07 **PASS**
- **X15**：SIM 玩具族不足以复现卡死 → 主控停派，请用户定下一战略

## NPU 一次测套件（用户要求一次备齐）

- 用户明确：**不要点滴测**，一次性备齐上机代码。
- 交付：
  - `scripts/npu_hang_rewrite_one_trip.sh`（N0 KeyGen → T01–T07 → Encrypt/Encaps/Decrypt；timeout 隔离；FORCE_REBUILD；默认 `F203_L18_TRACE=1`）
  - stable Encrypt host 已接 fused-trace 轮询（与 Encaps 同开关）
  - `npu_device_map`：`graph-tests/**` → 卡 **3**（与 ascendc-tests 同卡）
  - 文档：`docs/engineering/Encrypt卡死重写-实机一次测清单.md` + `Encrypt卡死重写-sync清单.txt`
  - 无卡校验：`NPU_HANG_MANIFEST=1` 列出 N0–N10；toys→3 / stable→1
- 用户动作：sync → 一条命令 → 把终端 TYPE_BACK **打字**贴回聊天（**不能**回传任何文件）
- 已推送；反馈改为只打字（X19）


## 反馈通道更正（用户重申）

- 用户**无法回传任何文件**，最多打字报错误/TRACE 编号。
- 套件改为结束打印 **TYPE_BACK**；Agent 只收聊天打字。记入 KB **X19**。
