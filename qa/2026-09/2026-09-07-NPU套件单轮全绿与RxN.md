# 2026-09-07 · NPU_SUITE 单轮全绿 → B3 → R×N

## 回报（用户打字）

```text
C0 PASS last=111
C1 PASS last=111 host=100,101,102,105,110,111
C2 PASS last=111 host=100,101,102,104,106,105,110,111
```

## 判读（BRANCHING B3）

- 最低挂档：**无**（单轮均未挂）。
- 套件覆盖的握手壳 / 形态粘合 / Â2×2 **单轮不粘性挂**（知识库 **N7**）。
- 旧 Encrypt/Encaps 历史粘性挂仍可能在套件未覆盖结构，或需多轮才触发。

## 下一刀

1. **R×N**：`bash graph_tests/npu_suite/run_rxn_npu.sh`（默认 C2×7）。  
2. 若 R×N 仍绿 → **ENCRYPT-GAP**（只读列差异 → 单因子结构探针；禁抄、禁正确性）。  
3. 仍不做 ByteDecode / KAT。

## 工作方式补充（同日晚）

- 用户锁：本分支与其它分支不同路；**充分使用 cannbot-skills** 辅助从头 Encrypt；**不做** `.cursor/skills` vendor 草案。  
- 910B3 云主机已调通；有时限，**未授权不连**。  
- 计划见 `graph_tests/ENCRYPT_REWRITE_PLAN.md` §0。

## 同日 · 知识库+图谱 v2（cannbot 刷新）

- 用户令：引入 cannbot 后**从头刷新**知识库与图谱；此后实验/写码**充分使用**图谱 + cannbot。  
- 库 v2：`docs/notes/Encrypt-实机无卡死-知识库.md`（工程层以 cannbot 为准；§3.3 记 SYNC-03 假阳性）。  
- 图 v2：`docs/rg-encrypt-npu-hangfree.yaml`；节点压缩为事实/决策/推论；`D-graph-and-cannbot` / `D-next-rxn-or-gap`。  
- 校验：`rg_validate.py --yaml … --no-exec-refs` → **结构一致性 OK**；`rg_render` 已写 HTML。  
- 审计：e01/e13/e15/l18 `sync_audit` 均有 SYNC-03「同侧」高优候选 → **人工确认、禁止自动改绿核**。  
- 仍等用户授权才连 910B3 / 开 R×N 或 ENCRYPT-GAP 短刀。
