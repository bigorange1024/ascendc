# 2026-09-07 · NPU_SUITE 单轮全绿 → B3 → R×N

## 回报（用户打字）

```text
C0 PASS last=111
C1 PASS last=111 host=100,101,102,105,110,111
C2 PASS last=111 host=100,101,102,104,106,105,110,111
```

## 判读（BRANCHING B3）

- 最低挂档：**无**（单轮均未挂）。
- 套件覆盖的握手壳 / 形态粘合 / Â2×2 **单轮不粘性挂**（知识库 **N7**）。
- 旧 Encrypt/Encaps 历史粘性挂仍可能在套件未覆盖结构，或需多轮才触发。

## 下一刀

1. **R×N**：`bash graph_tests/npu_suite/run_rxn_npu.sh`（默认 C2×7）。  
2. 若 R×N 仍绿 → **ENCRYPT-GAP**（只读列差异 → 单因子结构探针；禁抄、禁正确性）。  
3. 仍不做 ByteDecode / KAT。

## 工作方式补充（同日晚）

- 用户锁：本分支与其它分支不同路；**充分使用 cannbot-skills** 辅助从头 Encrypt；**不做** `.cursor/skills` vendor 草案。  
- 910B3 云主机已调通；有时限，**未授权不连**。  
- 计划见 `graph_tests/ENCRYPT_REWRITE_PLAN.md` §0。

## 同日 · 知识库+图谱 v2（cannbot 刷新）

- 用户令：引入 cannbot 后**从头刷新**知识库与图谱；此后实验/写码**充分使用**图谱 + cannbot。  
- 库 v2：`docs/notes/Encrypt-实机无卡死-知识库.md`（工程层以 cannbot 为准；§3.3 记 SYNC-03 假阳性）。  
- 图 v2：`docs/rg-encrypt-npu-hangfree.yaml`；节点压缩为事实/决策/推论；`D-graph-and-cannbot` / `D-next-rxn-or-gap`。  
- 校验：`rg_validate.py --yaml … --no-exec-refs` → **结构一致性 OK**；`rg_render` 已写 HTML。  
- 审计：e01/e13/e15/l18 `sync_audit` 均有 SYNC-03「同侧」高优候选 → **人工确认、禁止自动改绿核**。  
- 仍等用户授权才连 910B3 / 开 R×N 或 ENCRYPT-GAP 短刀。

## 同日 · 合入 main 的 CANNLab 联机说明

- 用户令：把 main 上「新 Cloud Agent 秒接手」的 CANNLab 联机方式合并进本 `1534` 分支。  
- 合入：`docs/engineering/CANNLab接入与远程驱动.md`、`scripts/cannlab/agent_bootstrap.sh`、`agent_watchdog.sh`；刷新 `AGENTS.md` / `AGENT_HANDOFF.md` §60 秒上手第 6 条 / `docs/engineering/INDEX.md`。  
- 本分支上机仍：**须授权**；单卡 **`ASCEND_DEVICE_ID=0`**；默认刀仍是 NPU_SUITE R×N，不是 main 的 KeyGen 冒烟叙事。

## 同日 · stable 恢复 main + Encaps/Decaps 取证准备

- 用户令：`examples/stable` 恢复为 **main** 口径；改运行环境（`CANNLAB=1`/`NPU_SINGLE_CARD=1` → device 0）；上机时亲自多轮/交叉跑 Encaps/Decaps 看卡住。  
- 已 `git checkout origin/main -- examples/stable` 并删除本分支新增的 prep_ntt 源文件；`git diff origin/main -- examples/stable` 空。  
- 新增 `scripts/cannlab/hang_observe_encaps_decaps.sh` + 文档 §5.1。  
- **阻塞上机**：本 Cloud Agent Secrets **未注入** `TAILSCALE_AUTHKEY` / `CANNLAB_SSH_KEY`（仅有 `ASCENDC_GH_PAT`）。自查脚本确认 **MISSING**（非打码错觉）；Secret 只在 VM 启动时注入 → **新开 Agent 接手**。

## 同日 · 交接安排（推送本分支供新 Agent）

- 刷新 `AGENT_HANDOFF.md`：开场白、Secrets 自查、P0 取证顺序、用户配合清单。  
- 新 Agent：先 `present len=` 自查 → CANNLab §4 接入 → `hang_observe_encaps_decaps.sh` → 回写库/图/qa → 控制台停机。  
- 仍禁：正确性先行、抄 Encrypt、泄 Secret 明文。

## 同日 · 新 Agent 接手 CANNLab 取证（Encaps/Decaps/Encrypt）

### 联机

- Secrets 已注入；Tailscale userspace OK；实例 hostname 现为 **`cannlab-npu-1`**（IP `100.118.122.52`；旧 `cannlab-npu` 可能 offline）。
- 远端 fetch 须显式 ref（`origin` 默认只跟 `main`）；已 checkout `cursor/kem-2launch-sticky-1534@766ea37`。
- 用完须**控制台关机/停止**（Agent 提醒用户；`kill -TERM 1` 仅兜底）。

### 取证事实（Host，非正确性）

| 波次 | 结果 |
|------|------|
| Encaps×7（干净多轮） | 全 **PASS**（对拍 max=0） |
| Decaps×3 PASS → **round4 HANG TIMEOUT124** | Phase-D 已 print `launch f203_decrypt_device_fused`，无 `npu_launch` 回 |
| CROSS Encaps | launch1 prep OK → launch2 **`f203_encrypt_l18_l19` 无回** |
| 猎挂 Encaps round1（FORCE 重建后） | prep OK → launch2 `l18_l19`；Host RUNTIME：`SynchronizeExecutedTask: report three minutes timeout! stream_id=46, task_id=1, pendingNum=1` |

证据目录（远端）：`/mnt/workspace/hang_hunt_evidence_20260907_170641/`；本机快照：`/opt/cursor/artifacts/hang_hunt_20260907/`。

### cannbot（SIM/静态并行）

- `sync_audit` encaps/encrypt：死等型 SYNC-04/05 在 `alg11_vec_pipe` **仅 CPU_DEBUG** → 对 910B3 **降权假阳性**；大量 SYNC-02（非卡死类）。
- decrypt fused：脚本无 CrossCore 配对红线；下一刀对照面仍是 **SoftSync/GATE(4/8)** 与 Encaps **`l18_l19` GATE**（禁复踩「仅 GATE 充分」）。
- SIM smoke C0/C1/C2 绿（`J-sim-not-sticky` 仍立）。
- 报告：`/tmp/sync_audit_reports/FINAL_REPORT.md`。

### 同日 · 日志分析结论（不改码）

- 分析全文：`/opt/cursor/artifacts/hang_hunt_20260907/ANALYSIS.md`
- **钉死**：Encaps 挂面 = `f203_encrypt_l18_l19`（Host launch2 无 sync 回 + RUNTIME 3min）；干净卡 r11 + 污染后 r1。
- **未钉**：具体 Wait（NTT / GATE4 / INTT 复用 1·3）——本波未开 `F203_L18_TRACE`。

### 同日 · 工作法纠偏 → ENCRYPT-GAP 离线优先

- 用户纠正：禁止「想到就开机测」；须知识库+图谱+skills 推理，**toys 穷尽**，只剩必须上机项并写清测什么/期望/反馈分支；父定计划，**subagent 编码**。
- 产出：`graph_tests/ENCRYPT_GAP.md`（结构差 G1–G8、E17/E18、仅剩上机 NPU-1…4）。
- 图谱：`D-next-offline-gap-toys`；旧 `D-next-rxn-or-gap` retracted。
- **E17/E18**：subagent 编码；SIM×8 均 **PASS**（复用 1/3 vs 分离 5/6）；sync_audit 无真死等；FEEDBACK 在 `_outbox/`。

### 同日 · 上机 NPU-1…4（cannlab-npu-1=`100.86.51.84`）

| 刀 | REPORT |
|----|--------|
| NPU-1 C2×7 | `SUMMARY R×N C2x7:OK` → **B3-clean**（N11） |
| NPU-2 E17/E18×11 | 均 `PASS last=111` → H-reuse **非充分**（N12） |
| NPU-3 Encaps+TRACE | PASS×10 → **HANG r11**；`[l18-trace] stages set=0/16`（N13） |
| NPU-4 挂后 r1 | **HANG** + 空 TRACE（N14） |

证据：`/opt/cursor/artifacts/npu_wave_20260907/`；远端 `/mnt/workspace/npu{1,2,3,4}_*`。  
### 同日 · 防掉线（2026-09-08）

- 现象：subagent 长 SSH 空闲 → SOCKS/`cannlab-npu` offline；或无心跳触发 **IDLE_MIN=30** 看门狗。
- 对策：`scripts/cannlab/run_npu_ab_nohup.sh` — 远端 **nohup + 45s heartbeat**；本机短连 submit/poll/fetch。
- 当前：`cannlab-npu` Tailscale 显示 **offline** → 须 WebIDE 再跑 `agent_bootstrap.sh` 后 `submit`。

| 刀 | REPORT |
|----|--------|
| NPU-E19 标量×12 | r0–2 绿；**r3 Host 丢槽15**（N15） |
| NPU-E19b DataCopy×12 | **全绿** 0+15（N16） |
| Encaps Trace-DC×12 | 挂时 **16/16**；**0 PASS**（HANG/FAIL）（N17） |

**修订**：空 TRACE=观测假象；真挂窗 → **末 Mark 之后**。  
证据：`_outbox/FEEDBACK-NPU-E19*.md`、`FEEDBACK-NPU-ENCAPS-TRACE-DC.md`。  
**请控制台关机。**

